# SUIVI

Journal de bord de la programmation du jeu. SEUL document que Claude Code
modifie au fil des sessions.

Cinq sections, jamais d'autre. FAIT : une ligne par étape terminée, la plus
récente en tête, hash de commit si applicable — sinon la date — puis quoi, en
une phrase. EN COURS : ce qui est ouvert maintenant, un tiret par point.
DÉCISIONS : les choix pris en session qui ne sont écrits ni dans `CLAUDE.md` ni
dans le code ; une décision SORT d'ici dès qu'elle est écrite ailleurs.
PROCHAINES ÉTAPES : ce qui vient après, dans l'ordre. PIÈGES DÉJÀ PAYÉS :
autorisé explicitement par Yael, contre la règle du récit — un piège coûte deux
fois si personne ne l'a écrit. Symptôme, cause, règle. Jamais l'histoire.

## FAIT

- 2026-09-11 — banc_peuplement_arbre : `_semer_pres_de` et `_deposer_graine` fusionnes. Un seul appel par graine depuis `_process` au lieu de deux. Le tirage disque (angle + rayon sqrt(u) * `_rayon_graine`) et le traitement (garde carte, gate trouee, lecture couvert, naitre/banque) sont ecrits l'un apres l'autre dans `_semer_pres_de`, non caches. Ordre RNG preserve (angle puis rayon) -> foret identique a seed egal. Appels de fonctions divises par deux sur la reproduction. Commentaires internes + `jeu/CARTE.md` § grille dormantes referencent `_semer_pres_de` a la place.
- 2026-09-11 — `scripts/champ_saturation.gd:redeposer(centre_x, centre_z, ancien_rayon_m, nouveau_rayon_m, taille_case, ancienne_magnitude, nouvelle_magnitude)` : transition en UNE passe (retrait ancien + depot nouveau fusionnes sur l'union des empreintes). Bit a bit equivalent a `deposer(..., ancienne, -1)` puis `deposer(..., nouvelle, +1)`, invariant verrouille par nouveau cas de `scripts/test_champ_saturation.gd` (4 scenarios : rayons differents, naissance simulee, mort simulee, memes rayons). `jeu/bancs/banc_peuplement_arbre.gd` : dans `_process`, les deux `_deposer_ombrage(-1)/(+1)` sur changement de stade remplaces par un `_transitionner_ombrage` -> `_couvert.redeposer(...)`. Cas naissance/mort (un seul stade) gardent leur `_deposer_ombrage` simple. `_liberer_slot` et `_naitre` inchanges. Nombre d'appels au champ pour les transitions divise par deux. Doc : `documents/CARTE.md § champ_saturation.gd` (signature ajoutee).
- 2026-09-11 — MECANISME FRAMEWORK NEUF : `scripts/champ_saturation.gd` (RefCounted, aucun nom du monde). Depot d'un scalaire float SIGNE sur les cases d'une grille XZ autour d'un centre, decroissance Chebyshev lineaire (plein au centre, zero au bord), retrait symetrique, nettoyage sous EPS. API : `deposer(centre_x, centre_z, rayon_m, taille_case, magnitude, signe)`, `lire(x, z, taille_case)`, `nombre_cases()`. Prouve generique par `scripts/test_champ_saturation.gd` (8 cas hors domaine : symetrie signe, rayon 0, decroissance Chebyshev, bord strict, cumul, independance a taille_case, epsilon, magnitude nulle). Ecart framework trace en en-tete du fichier (precedents scripts/monde.gd:retirer et scripts/facteur_variance.gd), fiche `documents/CARTE.md` ajoutee sous autorite du prompt du tour. `jeu/bancs/banc_peuplement_arbre.gd` : `_couvert` passe de `Dictionary` a `RefCounted = ChampSaturation.new()`, corps de `_deposer_ombrage` et `_lire_couvert` remplaces par un appel au mecanisme, `_couvert.size()` -> `_couvert.nombre_cases()`, `EPS_COUVERT` retire (vit maintenant dans le mecanisme), en-tete du banc mis a jour (bloc "MECANIQUES ENCORE INLINE" retire cote couvert). Comportement observe attendu identique (meme code, meme constantes -- juste deplace). Docs : documents/CARTE.md § champ_saturation.gd, jeu/CARTE.md § banc_peuplement_arbre.
- 2026-09-11 — WARNINGS GDScript::reload eteints au chargement du banc arbre. (1) `scripts/facteur_variance.gd:tirer_entre` : params `mini`/`maxi` (masquaient les built-ins Godot `mini()`/`maxi()`) renommes en `bas`/`haut`. Ecart framework trace dans le fichier (precedent `scripts/monde.gd:retirer`), le depot Orion garde ses noms d'origine. Callers positionnels dans banc_peuplement_arbre.gd + test_facteur_variance.gd inchanges. (2) `jeu/bancs/banc_peuplement_arbre.gd:_tick_banque` : `_expirations.size() / 2` (int/int) remplace par `_expirations.size() >> 1` (meme resultat, warning parti). Comportement inchange.
- 2026-09-11 — banc_peuplement_arbre : `rayon_ombre_m` recale sur le rayon de couronne (feuillage.largeur / 2 du stade correspondant, arrondi entier) dans data/banc_peuplement_arbre.json. Les gros stades passent de 40 m a 6-7 m -- l'ombre au sol correspond a l'empreinte utile pour la germination, pas a une ombre de coucher de soleil. `cases_couvertes` chute fortement au prochain releve (empreinte ~13 cases de cote au lieu de ~81), rayon de reveil des dormantes se raccourcit -> `query` dans `[arbre.tick_banque]` baisse. `magnitude` inchangee. `_note` du JSON a jour.
- 2026-09-11 — banc_peuplement_arbre : `taille_case` passe de 20.0 a 1.0 (data/banc_peuplement_arbre.json). Le champ de couvert est desormais lu a l'echelle d'une graine (1 m par case) au lieu d'un carre de 400 m2. Portee d'ombre en metres inchangee (le decouplage `rayon_ombre_m` du commit precedent la protege). Rayon de reveil des dormantes chute fortement -- moins de reveils par tick, requetes `_tick_banque` allegees, a observer au prochain releve `[arbre] ... cases_couvertes` et `[arbre.tick_banque] ... query`.
- 2026-09-11 — banc_peuplement_arbre : portee d'ombre DECOUPLEE du quadrillage. `ombrage_par_stade` passe de `rayon_cases` (cases) a `rayon_ombre_m` (metres) dans data/banc_peuplement_arbre.json (valeurs = anciennes * 20 pour preserver le comportement a `taille_case`=20). `_deposer_ombrage` lit les metres et convertit en cases via `ceil(rayon_ombre_m / _taille_case)` au moment de la pose, jamais stocke. `_calculer_rayon_reveil` derive la portee ombrage max directement en metres (`+ _taille_case` de securite Chebyshev), `_stade_est_degageant` compare des metres. `_charger_reglages_locaux` garde la validation "9 entrees". Finesse du quadrillage et portee d'ombre desormais reglables independamment. Docs a jour : data/banc_peuplement_arbre.json `_note`, jeu/CARTE.md § banc_peuplement_arbre. VERT attendu : tests du banc arbre (a lancer par Yael, voir prompt).
- 2026-09-10 — PURGE ZONE MORTE banc_peuplement.gd : boucle 20000 iterations de recopie `_entites_collision[kk].position = positions_mutees[kk]` retiree (aucun lecteur hors banc en code executable, prouve au grep ; seul un commentaire de test la mentionne). Contrat n_coll == cols.position.size() garanti par fabrication (`_entites_collision` alloue en boucle sur `positions_final.size()` au _fabriquer_lot, aucune mutation en cours de vie) -- assignation directe `cols.position = sortie_res.positions`. `_entites_collision` reste vivant comme source du SoA + inscription unique au spawn (`_monde.ajouter`). Commentaires purges : en-tete section MONDE (l.42-52) qui decrivait `recopie cols.position -> individu.position + _monde.deplacer(individu)` (aucun _monde.deplacer executable dans le banc, prouve au grep) ; @export brancher_monde et deplacer_cpp reecrits au present ; bloc PASSE COLLISION reformule (retire "vers _entites_collision[k].position ... position vivante ... futur consommateur qui interroge _monde") ; bloc PASSE DEPLACER retire du recit "la branche GDScript _monde.deplacer_simple est retiree". Fichier banc uniquement, aucun changement de comportement collision. VERT : test_banc_peuplement_collision, test_cache_aabb, test_collision_parite_detecter, test_tick_fusionne, test_banc_peuplement. test_collision_lot_cpp reste rouge sur WDAC 4551 (blocage systeme sur la nouvelle DLL des compteurs raccourci/gjk, hors perimetre de ce morceau -- a debloquer manuellement cote systeme, la parite bit-a-bit tenait au dernier commit signe).
- 2026-09-10 — CollisionLot broadphase DEMI-VOISINAGE + TRI STABLE. Chrono en jeu a N=20000 avant : parcours=33162 us (76%% de la collision), narrowphase=7502 us, autres <1100 us. paires_dist=315790, paires_dedup=157895 (facteur 2 : chaque paire vue deux fois, hashmap vus find+insert par paire dominait). Refonte : parcours en demi-voisinage (intra-cellule j > i, 13 offsets voisins strictement superieurs a (0,0,0) en ordre lex -- chaque paire vue une seule fois). Hashmap vus SUPPRIMEE (unordered_map<int64_t, uint8_t>). Filtre distance passe de `d2 > r_source²` a `d2 > max(r_i, r_j)²` -- EQUIVALENT au pipeline actuel : les deux visites precedentes materialisaient un OU logique (vus[cle] posee APRES le filtre distance, une paire non-marquee a la premiere visite pouvait etre reajoutee a la seconde si son rayon-source acceptait). max reproduit cet OU en une passe. Analyse verifiee au disque (collision.gd l.528-547) et confirmee par le test rayons_frontiere. Tri STABLE des contacts par (min(a,b), max(a,b)) en fin de detecter, cote C++ (std::stable_sort sur vector<int> perm + reconstruction des 4 PackedArray) ET cote GDScript oracle (decorateur [lo, hi, k_insertion, contact] car Array.sort_custom n'est pas garanti stable). Rend resoudre deterministe par construction, prepare M2 (multithread). Nouveau cas test_collision_lot_cpp scenario `rayons_frontiere` : deux entites boites de tailles differentes (0.4 vs 1.2) a distance 2.5 -- verrouille que max(r_i, r_j) capte bien la paire cote broadphase (sans ce cas, le test peut etre vert meme si le max est mal cable). VERT : test_collision_lot_cpp (peuplement + multi-formes + rayons_frontiere, egalite EXACTE), test_collision_parite_detecter, test_banc_peuplement_collision, test_cache_aabb, test_raccourci_boite, test_aabb_forme, test_tick_fusionne, test_banc_peuplement. DLL C++ recompilee (purge + rebuild). Chiffre in-game a N=20000 (poste parcours) a relever par Yael au profiler Godot -- attendu : chute nette de us_parcours et paires_dist == paires_dedup (facteur 2 parti).
- 2026-09-09 — CHRONOS + COMPTEURS CollisionLot decoupes et affiches en jeu. Cinq postes qui couvrent tout le corps de detecter+resoudre sans chevauchement ni trou : us_prepasse (cache par forme + pre-passe colonnes), us_tri (counting sort), us_parcours (boucle 3x3x3 + filtres distance/dedup/masque/swept), us_narrowphase (contact_forme_paire avec sub-stepping), us_resoudre. Le parcours n'appelle PLUS contact_forme_paire directement : les paires retenues sont poussees dans un batch (std::vector<PaireCandidat>), consommees juste apres par une seule boucle narrowphase. `now()` pris aux bornes de bloc uniquement, JAMAIS par paire -- fin des centaines de milliers d'appels now() par frame qui faussaient la mesure. Compteurs temporaires _n_paires_distance / _n_paires_dedup / _n_appels_nf / _n_contacts pour distinguer cout par paire vs nombre de paires. Exposes par derniers_chronos() {prepasse, tri, parcours, narrowphase, resoudre} et derniers_compteurs() {paires_distance, paires_dedup, appels_nf, contacts}. banc_peuplement.gd imprime une ligne toutes CADENCE_RELEVE_COLLISION_FRAMES (60) frames sous actif_releve. Aucun calcul de collision change (parite bit-a-bit preservee : batch reste FIFO dans l'ordre du parcours). VERT : test_collision_lot_cpp (parite exacte peuplement + multi-formes), test_collision_parite_detecter, test_banc_peuplement_collision, test_cache_aabb, test_raccourci_boite, test_aabb_forme, test_tick_fusionne, test_banc_peuplement. DLL C++ recompilee. Chiffres in-game a N=10000 (releve BORNE + poste collision) a relever par Yael au profiler Godot.
- 2026-09-09 — PORTAGE C++ de detecter+resoudre : CollisionLot dans extension_terrain (collision_lot.h/.cpp), enregistree par register_types. Generaliste des M1 (sphere+boite+capsule+hull, orientation != Identity), raccourci boite-boite AABB alignee comme chemin rapide interne. Frontiere SoA plate (positions/velocites/orientations aplaties + masques + reponses + pool de formes formes_debut/type/tf_locale/params + hull_points), pas de boxing Variant. Convention type : composantes Vector3 en real_t (float32), scalaires temporaires en double avec promotion explicite (double)vec.dot() aux memes points que GDScript -- l'ordre des operations est reproduit. Mono-thread. banc_peuplement.gd bascule sur CollisionLot en prod (SoA stable fabrique une fois au _fabriquer_lot, positions posees par frame) -- une seule voie, plus d'appel a jeu/Proto/collision.gd. collision.gd reste l'oracle de scripts/test_collision_lot_cpp.gd (parite EXACTE des flottants sur peuplement 6 boites ET multi-formes sphere+boite+capsule+hull orient tournee). Chronos derniers_chronos() {broadphase, narrowphase, resoudre} en us. DLL C++ recompilee (purge complete + rebuild). VERT : test_collision_lot_cpp, test_collision_parite_detecter, test_banc_peuplement_collision, test_cache_aabb, test_raccourci_boite, test_aabb_forme, test_tick_fusionne, test_banc_peuplement. Chiffre in-game a N=2000 puis 10000+ (poste collision) a relever par Yael au profiler Godot.
- 2026-09-09 — SPLIT Collision.tick -> Collision.detecter + Collision.resoudre (prealable au portage C++). Une seule voie de collision, sans wrapper legacy. detecter n'interroge JAMAIS un index externe : l'appelant fournit la liste complete. banc_peuplement passe sa population fermee `_entites_collision` ; mouvement_kinematic.gd (profil complet, joueur) collecte ses voisins via `monde.choses_dans_rayon(pos, rayon_collecte)` et compose `[entite] + voisins`. Ecart framework note dans mouvement_kinematic.gd (meme geste que scripts/monde.gd:retirer). Test parite bit-a-bit neuf : jeu/Proto/test_collision_parite_detecter.gd (8 contacts, 6 positions finales strictement egales apres refactor sur cluster 6 boites). VERT : parite_detecter, banc_peuplement_collision, cache_aabb, raccourci_boite, aabb_forme, tick_fusionne, physique_simple_lot_cpp, monde_structure_simple. Docs mises a jour : COLLISION.md, jeu/CARTE.md, fiche_patron_manager_proto_2.md.
- 2026-09-08 — OCCLUSION A HAUTEUR DE LA TAILLE REELLE DES CORPS. Diagnostic : releve BORNE affichait ~6.37M cibles (~64/unite) et ~7.4M tests_faits (~1.16/cible) a N=100 000, alors qu'a opacite 1.0 le premier rang aurait du bloquer le reste. Cause : data/banc_peuplement.json avait `largeur_occlusion = 0.5`, mais les corps mobile_test font 0.8 de large (data/mesh.json:boite_simple.taille = 0.8). Le couloir d'occlusion etait plus etroit que les corps -- un occulteur lateralement decale de 0.6 (dans son propre volume physique) n'etait PAS compte, alors qu'il bouche encore la ligne de vue vers un corps derriere lui. Consequence : les cibles au-dela du premier rang passaient dans la marge non couverte et restaient visibles. Correctif geometrique : pour deux corps de rayon r (peuplement homogene), la distance laterale maximale d'un occulteur pour boucher completement la cible derriere est r_occulteur + r_cible = 2 * (taille/2) = TAILLE DU CORPS. Suppression du `@export var largeur_occlusion = 0.5` (nombre en dur) et de la cle JSON `largeur_occlusion` -- remplaces par un membre `var _largeur_occlusion: float` calcule au `_monter_pool` depuis `catalogue_mesh[mesh_ref].taille` : `_largeur_occlusion = maxf(taille.x, taille.z)`. La valeur derive donc de data/mesh.json, aucun nombre en dur cote code. Pour boite_simple : 0.8 (au lieu de 0.5). Le C++ vue_lot reoit desormais cette largeur derivee. Aucun changement de la geometrie d'occlusion (portage mot pour mot de occlusion.gd::facteur intact) -- c'est le REGLAGE de largeur qui etait sous-calibre. Alarme push_error si taille absente ou nulle dans mesh.json (chemin degrade, occlusion inerte plutot que crash). Test test_vue_cpp INCHANGE (passe `largeur = 0.5` explicite en local, teste la geometrie sans dependre du reglage banc, VERT). Parite avec occlusion.gd toujours verifiee au cas 4. Chantier : uniquement le banc + JSON, C++ intact (pas de recompilation necessaire). VERT : test_vue_cpp (6 cas), test_index_spatial_cpp, test_banc_peuplement, test_peuplement_masse, test_fatigue_seuil, test_profil_peuplement, test_monde, test_monde_structure_simple, test_objet_isolation. Chiffre in-game a N=100 000 (releve BORNE + poste vue) a relever par Yael -- attendu : cibles NETTEMENT sous 6.37M (l'occlusion bouche enfin le premier rang), vue chute sous 550 000 us.
- 2026-09-08 — COLLECTE DU VOISINAGE PAR CASE + COMPTEURS TEMPORAIRES dans vue_lot (extension_terrain/src/index_spatial.cpp). Diagnostic : le poste vue = ~690 000 us a N=100 000. Chaque unite reconstruisait son voisinage 3x3 en interrogeant niveau.cases.find(Vector3i) 9 fois, avec ~62 unites par case en foule dense -- ~9 * 62 = ~560 find hashmap par case, soit ~900 000 find/frame et des dizaines de millions de copies d'ids. Le releve montrait ~7.4M tests d'occlusion/frame et seulement ~1% economies par la borne d_j + largeur : le vrai cout n'est pas dans l'occlusion mais dans la COLLECTE brassee par unite. Correctif : reorganiser vue_lot pour iterer PAR CASE occupee (niveau.cases) au lieu de par unite. Le voisinage (2n+1)^2 planaire d'une case est le MEME pour toutes les unites qui y vivent, tant que n = ceil(rayon / arete) : une unite au bord de sa case voit au plus n cases plus loin, donc rester dans [case.x -/+ n, case.z -/+ n] suffit. Pour arete = 2 et rayon = 2 : n = 1 -> 3x3 (identique au comportement par unite precedent). Formule generalisee, aucun nombre en dur -- ceil(rayon * inv_a) dimensionne l'exploration. Structure du refactor : pour chaque case occupee, collecte UNE fois le voisinage dans le vector local partage `voisinage` (find hashmap divise par nb_unites_dans_case), puis pour chaque unite de `unites_case` applique les filtres per-unite (distance / cone / occlusion / separation) sur le voisinage commun. Le voisinage inclut les unites de la case courante elles-memes (y compris chaque cible traitee juste apres) -- filtrees par d2 <= 1e-8f dans le filtre distance (distance a soi = 0). Le cone elargi cos_moitie_elargi calcule UNE fois par appel (independant de la position/orientation, dependant seulement de rayon/largeur) -- sortis de la boucle par unite. RESULTAT PAR UNITE strictement identique (parite verifiee, meme cases visitees dans le meme ordre : dcx = -n..+n, dcz = -n..+n donne les memes cases dans le meme ordre relatif que l'ancien cx = cx_min..cx_max pour une unite au centre ou au bord de sa case). L'architecture (vue -> occlusion -> separation en une passe) NE change PAS. COMPTEURS TEMPORAIRES : mutable int64_t _vue_cibles_totales / _vue_breaks_dist / _vue_tests_faits / _vue_tests_evites, exposes via nouvelle methode Dictionary derniers_compteurs_vue() bindée, lus par le banc apres chaque frame et imprimes sous `[peuplement] BORNE cibles=... breaks=... tests_faits=... tests_evites=... (X% economises)`. A retirer une fois le gain de la borne mesure et le refactor par case valide en jeu. CAS 6 AJOUTE a test_vue_cpp.gd : 4 unites reparties sur 2 cases (deux dans case (0,0), deux dans case (1,0)), avec B au bord de sa case voyant C dans la case adjacente via le 3x3 partage. Verifie que chaque unite obtient sa direction propre correcte (A -> (-1,0,0), B -> (-1,0,0), C -> (-1,0,0), D -> (0,0,0)) depuis le voisinage partage. VERT : test_vue_cpp (6 cas dont cas 6 partage voisinage), test_index_spatial_cpp, test_banc_peuplement, test_peuplement_masse, test_fatigue_seuil, test_profil_peuplement, test_monde, test_monde_structure_simple, test_objet_isolation. DLL C++ recompilee (purge complete des intermediaires). Chiffre in-game a N=100 000 (poste vue) a relever par Yael -- attendu : chute sous 690 000 us grace a la division du nombre de find hashmap par nb_unites_dans_case (~62).
- PIEGE COMPILATION C++ (revu) : plusieurs symptomes observes sur ce chantier -- (a) fichier `~libterrain...dll` orphelin quand Godot est ouvert et verrouille la vraie DLL (le linker ecrit le tilde a cote, la vraie DLL reste ANCIENNE) ; (b) blocage WDAC "Error 4551" sur la nouvelle DLL, resolu par purge complete des .obj + .dll + .lib + .exp + rebuild propre. Regle desormais : sur toute modif du .cpp de extension_terrain, TOUJOURS purger bin/*.dll bin/*.lib bin/*.exp src/*.obj AVANT scons, ET verifier Godot ferme. Une DLL avec timestamp posterieur au source ne prouve rien si Godot en cours d'execution garde la vieille copie en memoire.
- 2026-09-08 — BORNE DE DISTANCE POUR ARRETER LA BOUCLE OCCULTEURS dans vue_lot (extension_terrain/src/index_spatial.cpp). Diagnostic : vue = ~690 000 us a N=100 000. La boucle occulteurs testait TOUS les corps de dans_rayon (~60 en foule dense apres filtre cone elargi), rejetait la plupart par t hors ]0,1[ ou distance laterale > largeur -- APRES avoir calcule la projection pour chacun. dans_rayon etant deja trie par distance croissante, on exploite ce tri pour ARRETER la boucle des qu'aucun corps suivant ne peut occulter la cible. Argument geometrique documente : un occulteur k a t dans ]0,1[ et L <= largeur, donc distance_projetee = t * d_j < d_j et par inegalite triangulaire distance(i, k) <= distance_projetee + L < d_j + largeur. AUCUN corps a distance(i, k) > d_j + largeur ne peut occulter j. Correctif : dans la boucle occulteurs, si `vk.d > vj.d + largeur`, `break` (tri croissant : tous les suivants aussi trop loin). Le `if b == a continue` reste APRES le break, pour ne pas manquer un break tout en sautant soi-meme. Ne pas confondre avec l'ancienne borne erronee `b < a` (bornait par INDEX, pas par distance) : ici on borne par DISTANCE, la parite avec occlusion.gd tient. Verification cas 5 : d_j = 0.6, K a distance 0.652, largeur = 0.5, borne = 1.1 -> K bien teste, occulte J. DISCRIMINANCE PROUVEE : rebuild avec `seuil_dist_occulteur = vj.d` (borne trop stricte, sans + largeur) -> cas 5 ECHEC (K a 0.652 > 0.6, borne active a tort, K non teste, J faussement retenu, dir = (-1, 0, 0)). Rebuild avec borne correcte `vj.d + largeur` -> VERT. Court-circuit break sous seuil (occulteur opaque proche tue le facteur) reste en place, en PLUS de cette borne. Architecture INCHANGEE (vue -> occlusion -> separation en une passe). Geometrie de occlusion.gd portee mot pour mot. Aucun nom en dur. VERT : test_vue_cpp (5 cas dont le cas 5 qui verifie que la borne ne rate pas un occulteur legitime), test_index_spatial_cpp, test_banc_peuplement, test_peuplement_masse, test_fatigue_seuil, test_profil_peuplement, test_monde, test_monde_structure_simple, test_objet_isolation. DLL C++ recompilee (purge complete des intermediaires). Chiffre in-game a N=100 000 (poste vue) a relever par Yael -- attendu : chute sous 690 000 us.
- 2026-09-08 — FILTRE CONE AVANT dans_rayon dans vue_lot (extension_terrain/src/index_spatial.cpp). Diagnostic : le poste vue = ~1 080 000 us a N=100 000. dans_rayon recevait TOUS les voisins du rayon (~250 en foule dense), etait trie en entier, la boucle d'occlusion (b != a) parcourait ces 250 comme occulteurs potentiels -- y compris les ~190 derriere l'unite qui ne peuvent geometriquement pas occulter un voisin vu. Correctif : filtre CONE applique AU REMPLISSAGE de dans_rayon, pas seulement dans la boucle candidats. Consequence : dans_rayon ne contient que les voisins DANS le cone (~60 au lieu de ~250), le tri porte sur eux, la boucle d'occlusion ne parcourt que des occulteurs eux-memes dans le cone. Gain quadratique attendu sur boucle occlusion (~60^2 au lieu de ~250^2). CONE ELARGI POUR LES OCCULTEURS : le filtre au remplissage utilise un cone ELARGI = cone_cible + atan(largeur / rayon). Argument : un occulteur K d'une cible J dans le cone strict a angle(K, orient) <= angle(J, orient) + atan(L / distance_projetee_sur_AJ), avec L <= largeur ; en prenant distance_projetee ~= rayon (borne haute realiste en regime peuplement), l'ecart angulaire max est atan(largeur / rayon). Le cone elargi couvre donc TOUS les occulteurs legitimes, en pratique. CAS LIMITE ACCEPTE : un occulteur quasiment colle au percepteur (distance_projetee << largeur) peut avoir un angle depassant le cone elargi ; ce cas n'a aucun sens physique en regime peuplement (unite obstacle collee a une autre unite). La boucle candidats garde le filtre cone STRICT sur les cibles -- seuls les corps dans le cone strict sont candidats a la separation. cos_moitie_elargi calcule UNE fois par unite via acos + atan2 + cos ; negligeable devant le voisinage. Sphere pure demandee par l'appelant (cos_moitie_angle <= -1) : garde tel quel, l'elargissement est un no-op. DISCRIMINANCE PROUVEE : rebuild avec cos_moitie_elargi = cos_moitie_angle (pas d'elargissement) -> cas 5 ECHOUE (K a angle 32.5 deg exclu de dans_rayon, J faussement retenu, dir = (-1, 0, 0)). Rebuild avec le patch complet -> VERT (K dans le cone elargi 36.5 deg, occulte J, dir nulle). Architecture INCHANGEE (vue -> occlusion -> separation en une passe). Aucun nom en dur. VERT : test_vue_cpp (5 cas dont le cas 5 discriminant deja pose), test_index_spatial_cpp, test_banc_peuplement, test_peuplement_masse, test_fatigue_seuil, test_profil_peuplement, test_monde, test_monde_structure_simple, test_objet_isolation. DLL C++ recompilee (purge complete des .obj + .dll + .lib + .exp intermediaires avant scons -- toujours obligatoire sur modif C++, voir piege documente). Chiffre in-game a N=100 000 (poste vue) a relever par Yael -- attendu : chute nette sous 1 080 000 us.
- 2026-09-08 — CORRECTION DU BUG b < a dans vue_lot + cas 5 discriminant. Aveu : le chantier "tri par distance + occulteurs plus proches" precedent avait introduit un BUG DE CORRECTION silencieux (`for b = 0; b < a`). L'argument geometrique invoque -- "un occulteur legitime est plus proche que sa cible" -- est FAUX quand la largeur du couloir est non negligeable devant la distance : distance(i,k)^2 = (t*d_j)^2 + L^2, pour L non nul et proche de largeur, distance(i,k) peut depasser d_j (contre-exemple mesure : d_j = 0.6, K a (0.55, 12, 0.35), largeur = 0.5 -- t = 0.917 dans ]0,1[, L = 0.35 <= 0.5, distance(A,K) = 0.652 > 0.6). Un tel occulteur est LEGITIME et etait silencieusement rate par b < a. Le test_vue_cpp precedent ne le voyait pas car les 4 cas avaient toutes leurs positions alignees (L = 0 strict) -- Yael a pointe cet aveu deguise dans mon message ("le tri ne peut pas changer le resultat" = le test ne teste pas le tri). Correctif : boucle occulteurs remise a `for b = 0; b < nvr` avec `if b == a continue` (tous les corps de dans_rayon sauf soi-meme). Le tri par distance croissante des CIBLES reste (utile pour maximiser le court-circuit break sous seuil : en foule dense, un corps proche opaque tue le facteur d'une cible cachee des le premier occulteur aligne, boucle sort par break). CAS 5 AJOUTE dans scripts/test_vue_cpp.gd : positions non alignees, occulteur K a distance > d_j mais dans le couloir (cone 45 deg local au cas pour que K sorte du cone et ne soit pas cible lui-meme, sinon auto-occlusion mutuelle donnerait direction nulle et le test ne distinguerait rien). Ce cas est PROUVE DISCRIMINANT : compile + test avec `b < a` -> ECHEC sur cas 5 (dir proche de (-1,0,0), J faussement retenu) ; compile + test avec `b != a` -> VERT (dir nulle, J occlus par K). Le test verifie aussi la parite avec Occlusion.facteur(A, J, [K]) sur les memes points -- facteur <= seuil dans les deux cas. VERT : test_vue_cpp (5 cas dont le nouveau cas 5 discriminant), test_index_spatial_cpp, test_banc_peuplement, test_peuplement_masse, test_fatigue_seuil, test_profil_peuplement, test_monde, test_monde_structure_simple, test_objet_isolation. DLL C++ RECOMPILEE (rm des .obj + .dll intermediaires avant rebuild pour eviter les blocages WDAC observees plusieurs fois -- toute session qui touche le C++ doit purger extension_terrain/bin/*.dll+lib+exp ET extension_terrain/src/*.obj avant scons). Le gain de perf du tri reste (court-circuit break precoce sur les cibles cachees), mais la couverture des occulteurs est desormais correcte. Chiffre in-game a N=100 000 (poste vue) a relever par Yael.
- 2026-09-08 — TRI PAR DISTANCE + OCCULTEURS PLUS PROCHES SEULEMENT dans vue_lot (extension_terrain/src/index_spatial.cpp). Diagnostic : a N=100 000, vue=~1 550 000 us/frame. En foule dense chaque unite avait ~250 voisins dans son rayon, l'occlusion testait chaque candidat contre TOUS les autres corps de dans_rayon (nvr^2). Dans la realite, un corps proche opaque BOUCHE ce qui est derriere : ces corps ne devraient jamais etre examines. Correctif : tri de dans_rayon par distance CROISSANTE (std::sort avec lambda `a.d < b.d`, sur petite liste par unite typiquement 20-50, jamais sur tout le lot), puis parcours du plus proche au plus loin. Le test d'occlusion d'un candidat vj boucle UNIQUEMENT contre les corps `b < a` (deja traites, strictement plus proches). Argument geometrique documente : un occulteur legitime k d'un voisin j a t dans ]0,1[ strict sur le segment i -> j, donc distance projetee = t * vj.d < vj.d -- en regime largeur << vj.d (case courante du peuplement), distance(i, k) reste < vj.d. Le court-circuit `break` sous seuil, deja en place, joue son role plein : en foule dense avec opacite_unite=1.0, un corps proche opaque et aligne fait facteur = 0 <= seuil des le premier occulteur, `break` cesse d'examiner cette direction. Include <algorithm> ajoute. Architecture INCHANGEE (vue -> occlusion -> separation en une passe). Geometrie de occlusion.gd::facteur portee MOT POUR MOT. Aucun nom en dur, aucun LOD par distance, aucune cadence differenciee. VERT : test_vue_cpp (parite preservee -- les cas alignes du test ont tous distance laterale = 0 strict, le tri ne peut pas changer le resultat), test_index_spatial_cpp, test_banc_peuplement, test_peuplement_masse, test_fatigue_seuil, test_profil_peuplement, test_monde, test_monde_structure_simple, test_objet_isolation. DLL C++ RECOMPILEE PROPRE (`scons --clean` puis rebuild) -- une compilation incrementale a produit une DLL que WDAC refusait (erreur 4551), le rebuild propre a debloque. Chiffre in-game a N=100 000 (poste vue sur `[peuplement]`) a relever par Yael -- attendu : chute nette sous 1 550 000 us (en foule dense, une unite n'examine plus que ses premiers rangs -- les corps plus lointains dans une direction bouchee ne sont pas examines a fond).
- PIEGE COMPILATION C++ : sur toute modif du .cpp de extension_terrain, une compilation INCREMENTALE peut produire une DLL que WDAC refuse (Windows Application Control, erreur 4551 "Une strategie de controle d'application a bloque ce fichier"). Symptome : la DLL est LINKee sans erreur, mais Godot echoue au chargement (`load_extensions`). Correctif : `python -m SCons target=template_release --clean` PUIS rebuild depuis zero. Le rebuild propre produit une DLL acceptee. Enseignement : sur toute session qui touche le C++, prevoir un rebuild `--clean` avant les tests, jamais se contenter d'un `scons` incremental si la DLL est refusee.
- 2026-09-08 — DEGRAISSAGE DE L'OCCLUSION DANS vue_lot (extension_terrain/src/index_spatial.cpp). Diagnostic : a N=100 000 entasses, le poste `vue` prenait ~4 700 000 us/frame. L'algorithme etait bon (un seul parcours du voisinage, cone + occlusion + separation en une passe) mais la boucle d'occlusion parcourait TOUT le voisinage brut comme occulteurs potentiels (nv^2, ~780x780 par unite au regime peuplement). La grande majorite de ces "occulteurs" etaient des corps hors du cone / hors du rayon qui n'auraient jamais pu occulter -- rejetes par le test t/lateral mais APRES avoir tous ete testes. Correctif : par unite, DEUX vectors locaux reutilises entre unites : (1) `voisinage` (existant) collecte le brut 3x3 planaire, (2) NOUVEAU `dans_rayon` (std::vector<VoisinVue>, struct anonyme du .cpp {id, dx, dz, d}) filtre le brut par distance strictement < rayon, avec dx/dz/d precalcules. Cette liste sert a la fois de SOURCE DE CANDIDATS (le filtre cone est applique dessus) ET de LISTE D'OCCULTEURS pour le test d'occlusion. Argument geometrique : un occulteur possible k sur le segment i->j a t dans ]0,1[ strict, donc distance(i, k) < distance(i, j) < rayon -- k est necessairement dans le rayon d'i, donc dans dans_rayon. Un obstacle plus loin que le rayon ne peut pas satisfaire t dans ]0,1[ sur un segment de longueur < rayon (rejete de facto par occlusion.gd::facteur). L'occlusion coute donc desormais nvr^2 au lieu de nv_brut^2 -- gain quadratique, ordre 25-100x sur un voisinage typique (5-10x moins de corps dans le rayon que dans le voisinage brut). Sous-optimisation supplementaire au passage : ok_x/ok_z sont directement lus depuis VoisinVue (-vk.dx, -vk.dz), aucun re-lookup pos_r[k] dans la boucle d'occlusion. La geometrie de scripts/occlusion.gd::facteur reste portee MOT POUR MOT (t dans ]0,1[, distance laterale <= largeur, cumul multiplicatif de (1 - clamp(opacite, 0, 1))), court-circuit sous seuil conserve. Aucun changement d'ARCHITECTURE (vue -> occlusion -> separation en une passe intact), aucun nom en dur (rayon, angle, largeur, seuil, opacite viennent des parametres et donnees), aucun LOD par distance, aucune cadence differenciee -- juste un degraissage interne de la boucle d'occlusion. VERT : test_vue_cpp (parite preservee -- meme resultat, seulement le cout change), test_index_spatial_cpp, test_banc_peuplement, test_peuplement_masse, test_fatigue_seuil, test_profil_peuplement, test_monde, test_monde_structure_simple, test_objet_isolation. DLL C++ recompilee (release). Chiffre in-game a N=100 000 (poste `vue` sur `[peuplement]`) a relever par Yael -- attendu : chute nette sous 4 700 000 us.
- 2026-09-08 — VUE (CONE_ORIENTE) + OCCLUSION EN C++, UN SEUL PARCOURS DU VOISINAGE (chantier "vue avec occlusion en C++"). Le chantier "passe candidats GDScript" precedent a ete JETE dans ce commit : static passe_candidats supprimee, @export candidats_actif retire, _derniers_candidats/_us_candidats supprimes, scripts/test_passe_candidats.gd + son .uid effaces, cle "candidats_actif" retiree de data/banc_peuplement.json. Aucun code mort. La sortie CSR n'a pas de consommateur reel et ajoutait un DOUBLON de parcours de voisinage (78 M paires GDScript = ~23 000 000 us/frame observes en jeu). Remplacement : UNE seule methode C++ dans IndexSpatial qui fait TOUT en un seul parcours du voisinage planaire. `vue_lot(positions, orientations, opacites, rayon, cos_moitie_angle, largeur, seuil_facteur) -> PackedVector3Array` : pour chaque unite, (1) collecte du voisinage 3x3 planaire (tous corps sauf l'unite, dans un `std::vector` local reutilise entre unites -- evite N allocations de vector par frame), (2) filtre distance HORIZONTALE strictement inferieure a `rayon`, (3) filtre cone : `orient.dot(diff_vers_voisin) / d >= cos_moitie_angle` (patron perception.gd::_percevoir_cone_oriente, cos_moitie precalcule cote banc, aucun acos en boucle), (4) test d'occlusion contre les AUTRES corps du meme voisinage courant (jamais une requete spatiale par paire percepteur-voisin -- c'est le piege n^2 documente dans le prompt) : geometrie de scripts/occlusion.gd::facteur portee MOT POUR MOT (t = (pos_k - depuis).dot(vecteur) / longueur_carre, t dans ]0,1[ strict, distance laterale <= largeur, cumul multiplicatif de (1 - clamp(opacite, 0, 1))) avec court-circuit des que facteur <= seuil, (5) contribution separation `diff * (rayon - d) / d` accumulee dans le MEME parcours (les voisins vus sont deja la, pas de re-parcours) puis normalisation en direction unitaire horizontale (Y=0). UN seul poste chrono `vue`, UNE seule frontiere par frame. AUCUN NOM DE PROPRIETE EN DUR : opacites recu comme PackedFloat32Array indexe par id, le banc aplatit la propriete en colonne AVANT l'appel -- le C++ est aveugle au nom. Rayon, angle, largeur, seuil viennent tous des donnees (@export du banc, surcharge JSON). Suppression complete de `separation_lot` C++ (un seul chemin -- l'ancien parcours redondant tombe) + de scripts/test_separation_cpp.gd. Cablage banc : le hot path fait `_index_cpp.vue_lot(cols.position, cols.direction, cols.opacite, rayon, cos_moitie, largeur, seuil)` puis `desirees[k] = directions[k] * vitesses[k]`. `cols.direction` sert d'orientation (l'unite regarde la ou elle va, direction d'errance). Nouvelle colonne `opacite` au pool (defaut = @export opacite_unite = 1.0, uniforme sur le peuplement mobile_test). Format release simplifie : `[peuplement] N= fps= errance= vue= phys+buffer= deplacer= fatigue= mm_set=` (un seul poste `vue` au lieu de deux `separation`+`candidats`). Nouveau `scripts/test_vue_cpp.gd` (4 cas neufs) : (1) voisin devant dans le cone sans obstacle -> vu et pousse, (2) voisin derriere hors cone -> ignore, (3) voisin dans le cone mais cache par obstacle opaque -> retire, (4) parite du facteur d'occlusion avec `Occlusion.facteur(...)` sur les memes points et attenuation partielle (opacite 0.5). VERT : test_vue_cpp (4 cas neufs), test_index_spatial_cpp, test_banc_peuplement, test_peuplement_masse, test_fatigue_seuil, test_profil_peuplement, test_monde, test_monde_structure_simple, test_objet_isolation. DLL C++ recompilee (release). Chiffre in-game a N=100 000 (poste `vue` sur `[peuplement]`) a relever par Yael -- attendu : UN seul poste, plus jamais 23 000 000 us (le doublon GDScript est mort). Aucun mecanisme du coeur touche (perception.gd, occlusion.gd, champ_occulte.gd, monde.gd intacts). L'ancienne SUIVI ligne "fondation GDScript pour perception de masse" est reniee par ce commit -- la fausse route a coute 23 000 000 us, la vraie voie etait de tout mettre dans un parcours natif unique, jamais un CSR intermediaire GDScript.
- 2026-09-08 — PERCEPTION DE MASSE EN FLUX, fondation GDScript (chantier "passe candidats partagee"). Diagnostic : chaque comportement (separation aujourd'hui ; occlusion, collision demain) reouvre les cases de son cote et refait le test distance, alors qu'a N=100 000 c'est le meme voisinage qui est demande plusieurs fois par frame. On pose la fondation : UNE passe unique qui produit, pour chaque unite, la liste de ses voisins REELLEMENT dans le rayon (le rouge, pas la case entiere -- rejet hors-rayon fait au bon endroit, pas apres avoir ratisse trois cases pleines pour rien). Structure de sortie CSR (Compressed Sparse Row), reutilisable par plusieurs comportements sans re-parcours : { offsets: PackedInt32Array (count+1), voisins: PackedInt32Array (total), distances: PackedFloat32Array (total, parallele -- la distance deja calculee UNE fois par paire, evite un re-sqrt cote consommateur }. offsets[i]..offsets[i+1] delimite les voisins de i. `voisins`/`distances` sont paralleles. Emplacement : `BancPeuplement.passe_candidats(cases, positions, inv_arete, rayon)` -- static, testable independamment. Ce chantier reste dans le banc plutot qu'un fichier partage neuf ; extraction dans un mecanisme du coeur quand un SECOND consommateur reel apparaitra. PATRON demi-paire, deux passes : (1) denombrement -- pour chaque paire (a, b>a) dont la distance est dans le rayon, incrementer nb_voisins[a] et nb_voisins[b] ; prefix sum sur les offsets ; (2) remplissage symetrique -- reparcourir les memes paires, meme test, ecrire voisins[offsets[i]+cursor[i]]=j et voisins[offsets[j]+cursor[j]]=i, meme distance. Deux hashmap lookups par paire au lieu d'un, mais on evite les 100 000 allocations d'Array-par-unite qu'une alternative Array-of-Arrays imposerait en GDScript. Cases lues via `_index_cpp.cases_pour_niveau(_exposant_planaire)` (le niveau planaire du chantier "degraissage separation_lot" precedent, deja rempli par deplacer_lot chaque frame -- rien de nouveau cote maintenance de l'index). Cle Vector3i(cx, 0, cz), aucun balayage Y (le niveau planaire ecrase Y=0 a l'insertion). Le seul point d'entree consomme est cases_pour_niveau + les positions -- pas de dependance a un state interne. Cablage banc : @export candidats_actif (defaut true, surcharge JSON), membre _exposant_planaire pose au _monter_pool en meme temps que ouvrir_niveau_planaire, sortie stockee dans _derniers_candidats pour futurs consommateurs. Nouvelle passe dans _physics_process APRES la passe deplacer, AVANT physique/fatigue -- chronometree sous _us_candidats, ajoutee au format release : `[peuplement] N= fps= errance= separation= phys+buffer= deplacer= candidats= fatigue= mm_set=`. AUCUN BRANCHEMENT actuel de la separation dessus : separation_lot C++ (chemin C++ optim demi-paire + planaire du chantier precedent) reste en place et VERT. Ce chantier AJOUTE la passe + la mesure + un test de parite, il ne remplace rien. La separation, l'occlusion et la collision viendront s'y brancher dans des chantiers ulterieurs, une fois la structure prouvee. Perception.gd et occlusion.gd NON touches -- ils sont ecrits par-objet et non-utilisables tels quels a N=100 000 ; leur reecriture flux est le vrai chantier de suite, ce chantier pose le socle. Nouveau `scripts/test_passe_candidats.gd` (6 cas) : CSR bien forme (offsets.size == count+1, voisins.size == distances.size, offsets[count] == total), vrai voisin retenu SYMETRIQUEMENT chez les deux, hors-rayon (piege "orange" : dans la case mais au-dela du rayon) REJETE, distance remontee == vraie distance planaire, parite complete vs oracle O(N^2) sur N=80 aleatoire, cas limite rayon=0. Aucun mecanisme du coeur touche (fichier neuf uniquement dans jeu/bancs et scripts/, aucun scripts/*.gd du coeur modifie). VERT : test_passe_candidats (6 cas neufs), test_separation_cpp, test_index_spatial_cpp, test_banc_peuplement, test_peuplement_masse, test_fatigue_seuil, test_profil_peuplement, test_monde, test_monde_structure_simple, test_objet_isolation. Chiffre in-game a N=100 000 (poste `candidats=%dus` sur `[peuplement]`) a relever par Yael -- pas de comparaison headless, seul le releve en jeu tranche.
- 2026-09-08 — DEGRAISSAGE `separation_lot` (extension_terrain/src/index_spatial.{h,cpp}). Diagnostic : a N=100 000 entasses, la passe separation prenait ~330 000 us/frame alors que tout le reste du hot path etait sous 6 000 us. L'algo global etait bon (index deja de bonne arete, choix de niveau adapte au rayon), mais deux gaspillages persistaient. (1) La boucle interne balayait l'axe Y sur cy_min..cy_max avec un hashmap.find par plan Y, alors que la separation est planaire (Y=0 en sortie, seule dx/dz entrent dans la force). Chaque find inutile sur des plans Y quasi vides est un cache miss unordered_map -- multiplie par 100 000 unites x plusieurs plans. (2) Chaque paire de voisins mesuree DEUX fois : quand id itere voisin, puis quand voisin itere id. Deux sqrt pour la meme distance, memes divisions. Correctifs, sans toucher a l'algo ni au choix de niveau. Un seul chemin de code (l'ancienne boucle est SUPPRIMEE, pas laissee a cote). Rien code en dur (ni cy=0 en litteral, ni seuil, ni rayon -- tout vient des donnees ou des parametres). (1) NIVEAU PLANAIRE. Ajout de `bool planaire` sur `struct Niveau` + nouvelle methode `ouvrir_niveau_planaire(exposant)`. `deplacer_lot` : sur un niveau planaire, la clef de case ecrase Y a 0 (`Vector3i visee(fx, planaire ? 0 : fy, fz)`) -- toutes les unites d'une meme colonne (fx, fz) tombent dans la MEME entree unordered_map, quelle que soit leur altitude. Le niveau du deplacer (arete 16, 3D) reste ouvert via `ouvrir_niveau` INCHANGE : deplacer_lot le peuple en 3D pur comme avant, aucune casse de son contrat -- seul le SECOND niveau ouvert par le banc (arete adaptee au rayon separation) est planaire. `separation_lot` exige un niveau planaire (push_error + retour a zero sinon) et lit `cases.find(Vector3i(cx, 0, cz))` sans plus jamais boucler sur cy_min..cy_max. Deux unites de meme (x, z) mais altitudes differentes se separent maintenant correctement, alors qu'avec un niveau 3D + suppression de la boucle Y elles auraient rate leur voisinage (ancien piege documente dans l'en-tete). (2) DEMI-PAIRE. Chaque paire (id, voisin > id) traitee UNE fois : filtre `voisin <= id continue` dans la boucle interne (auto-comparaison et paire deja vue sautees d'un coup). Contribution symetrique : `out_w[id] += diff * w` ET `out_w[voisin] -= diff * w`. La normalisation quittant les locales ax/az par id, une SECONDE PASSE courte sur les count sorties normalise chaque accumulateur en direction unitaire horizontale (Y=0). Cout supplementaire : N sqrt finaux, negligeables devant les N*voisins sqrt evites (une paire = un sqrt contre deux avant). Resultat mathematique final IDENTIQUE (force sur A due a B = -(force sur B due a A), la somme reconstituee est identique a l'ancienne cumulation par id). Recompile release. Callers propages : `jeu/bancs/banc_peuplement.gd:_monter_pool` passe de `ouvrir_niveau(exposant_sep)` a `ouvrir_niveau_planaire(exposant_sep)` (le niveau du deplacer EXPOSANT_INDEX_CPP=4 reste sur ouvrir_niveau 3D) ; `scripts/test_separation_cpp.gd` passe de `ouvrir_niveau(1)` a `ouvrir_niveau_planaire(1)` (verrouille le VRAI chemin de prod du banc). VERT : test_separation_cpp (parite avec oracle O(N^2) preservee -- la demi-paire ne change que l'ordre d'accumulation, pas le resultat mathematique), test_index_spatial_cpp (deplacer_lot en 3D pur inchange sur le niveau non-planaire), test_banc_peuplement, test_profil_peuplement, test_peuplement_masse, test_fatigue_seuil, test_monde, test_monde_structure_simple, test_objet_isolation. Chiffre in-game a N=100 000 (poste `separation` en us sur `[peuplement]`) a relever par Yael -- attendu : chute nette sous 330 000 us.
- 2026-09-08 — FATIGUE EN CADENCE LENTE, BRANCHEE SUR charge/seuil (chantier "fatigue evenementielle sur seuil"). Premiere barre du peuplement branchee sur le couple depense.gd + seuil_etat.gd, sans reecrire aucun mecanisme du coeur. Doctrine (design.md, CARTE.md) : au FRANCHISSEMENT d'un seuil, seuil_etat.gd POSE un drapeau UNE fois ; le comportement lit le drapeau (booleen, instantane), il ne RECALCULE JAMAIS la condition sur la jauge a chaque frame. Entre deux franchissements : cout zero. Evenementiel sur seuil, jamais scan par tick. Cablage : passe cadencee `_passe_fatigue` dans banc_peuplement.gd, UNIFORME pour toutes les unites activees (jamais fonction de la distance au joueur -- LOD par distance INTERDIT ici). Le declenchement lit `cadence_fatigue_frames` (defaut 30 = 0.5s a 60fps) et n'agit que quand ce compteur expire. Trois etapes strictement lineaires : (1) `Depense.avancer(pool.individus, delta_cadence, {})` decrement `reserves.sommeil.reserve` (cout_base surcharge a 20.0/s pour que le franchissement soit visible sur l'echelle de secondes de la demo -- @export `cout_base_sommeil_demo`, jamais une valeur en dur dans le moteur) ; (2) miroir plat pose sur chaque unite : `p.manque_sommeil = capacite - reserves.sommeil.reserve` -- necessaire car seuil_etat.gd ne sait lire qu'une cle PLATE (contrainte structurelle documentee dans son en-tete) ; (3) `SeuilEtat.avancer(individus, catalogue_seuils_etat)` compare `manque_sommeil > seuil` a l'entree "epuisement" de data/seuils_etat.json (seuil=70, etat="epuise") -- ces valeurs viennent des donnees, aucun nombre en dur dans le moteur. `Peuplement.activer` renforce : detache automatiquement `reserves` a l'activation (materialisation = presence complete, contrat COW), chaque unite activee mute son propre canal sommeil sans contaminer les autres. Comportement demo A LA BASCULE UNIQUEMENT : pour chaque id qui a bascule, lecture de `etats_actifs.has('epuise')` une fois, ecriture de `cols.vitesse[slot]` = vitesse_nominale * vitesse_epuise_facteur (0.4 par defaut) si epuise, sinon vitesse_nominale. Aucune re-evaluation entre franchissements. La colonne vitesse alimente la passe errance (`desiree = direction * vitesse`) et separation_lot -- la vitesse baisse au tick suivant sans qu'aucun code ne relise la jauge. Cablage banc : `@export nombre_actives` (defaut 100 sur les 100 000 slots -- les 99 900 autres restent en regime masse, aucune fatigue materialisee, coherent avec le chantier "regime de masse" precedent), `@export cadence_fatigue_frames`, `@export cout_base_sommeil_demo`, `@export vitesse_epuise_facteur` -- surcharges JSON dans le meme commit. Ordre du hot path (par frame) : errance/separation -> physique+buffer -> deplacer -> fatigue (chronometree, s'execute une frame sur 30) -> mm_set. Format de release enrichi : `[peuplement] N= fps= errance= separation= phys+buffer= deplacer= fatigue= mm_set=`. Aucun mecanisme du coeur touche -- ni charge.gd, ni seuil_etat.gd, ni depense.gd. Seul `scripts/peuplement.gd::activer` change (detacher de reserves, contrat de "presence complete"), ecart deja assume par ce fichier. Nouveau `scripts/test_fatigue_seuil.gd` (5 cas) : (1) reserve pleine, pas d'etat 'epuise' ; (2) decrement + miroir + SeuilEtat pose 'epuise' au franchissement ; (3) entre deux franchissements, SeuilEtat.avancer rend Array vide et NE MUTE RIEN (memoire par entree, jamais recalcul) ; (4) recharge -> franchissement descendant retire 'epuise' ; (5) le seuil 70.0, la propriete "manque_sommeil", le nom "epuise" viennent tous du catalogue seuils_etat.json -- aucun nombre en dur dans le test qui reference le mecanisme. VERT : test_fatigue_seuil (5 cas neufs), test_banc_peuplement, test_profil_peuplement, test_seuil_etat, test_charge, test_depense, test_monde, test_monde_structure_simple, test_separation_cpp, test_peuplement_masse, test_objet_isolation. Chiffre `[peuplement] fatigue=%dus` a relever par Yael en jeu (attendu : rester bas, la passe ne tourne qu'une frame sur 30 et ne fait que N decrement + N miroir + N comparaison ; le tick sans fatigue reste a 0us moyen).
- 2026-09-08 — REGIME DE MASSE EN COLONNES, PAQUET DYNAMIQUE FABRIQUE A LA DEMANDE (chantier "regime de masse en colonnes"). Diagnostic : apres le partage COW (memoire 569 Mo a N=100 000), reste un poids mort -- chaque unite portait quand meme un Dictionary `individu` complet (proprietes fusionnees, id String, position Vector3) alors que le hot path (physique_et_buffer, separation_lot, deplacer_lot C++) ne lit RIEN de ce Dictionary sous `deplacer_cpp=true` (config par defaut du banc). Toutes les colonnes du peuplement (position, velocite, desiree, direction, cap_horloge, au_sol, vitesse, slot) portent l'etat vivant ; le Dictionary etait un cablage historique. Doctrine (design.md, deux regimes presence complete / activation variable) appliquee ICI a la MEMOIRE : une barre faim/soif qui dort n'a pas a etre materialisee 100 000 fois. Correctif : deux nouvelles fonctions static sur peuplement.gd. `spawn_masse(pool, position, vitesse, direction=Vector3.ZERO, cap_horloge=0.0) -> int` : alloue un slot, ecrit les 8 colonnes (une push_back par colonne connue au module, defaut pour les autres) et les 12 floats du buffer MultiMesh, ne touche pas a individus/id_to_index/monde. Rend le slot ou -1 sur saturation. `activer(pool, catalogue, type_id, index, monde=null) -> String` : fabrique le Dictionary individu via Objet.fabriquer(paquets_partages=true) a partir de cols.position[index], attache proprietes._slot = cols.slot[index], inscrit dans pool.individus + pool.id_to_index + monde. Rend l'id ou "" en cas d'echec (pool vide, index hors bornes, fabrication refusee). INVARIANT ROMPU expres sous masse : individus.size() != cols.size() ; le hot path lit desormais `cols.position.size()` pour la borne d'iteration (valable dans les deux regimes). Contrainte : regime masse EXIGE deplacer_cpp=true (la passe deplacer GDScript oracle lirait individus[j] qui est vide sous masse) -- garde au `_monter_pool` (push_error + repli sur false). Aller simple documente : desactiver une unite (retirer son individu tout en gardant sa ligne de colonnes) est un chantier de gameplay a part, non implemente ici. Cablage banc : `banc_peuplement.gd:@export regime_masse: bool = false` (defaut = chantier COW precedent, comportement historique) + surcharge JSON dans le meme commit + branchement dans `_fabriquer_lot` (boucle spawn_masse sans construire de Dict individu, saute `_remplir_colonnes_depuis_individus`). L'ordre de tirage RNG (direction puis horloge) est PRESERVE tel qu'avant, meme dans les deux regimes -- le determinisme de la graine ne change pas. Le hot path (`_physics_process`) : rien ne bouge a l'ecran, la boucle physique + separation + rendu lit uniquement les colonnes. Nouveau `scripts/test_peuplement_masse.gd` (6 cas) : spawn_masse dans pool vide (colonnes remplies, individus/id_to_index vides, buffer pose), spawn_masse repete (slots LIFO), spawn_masse sature (rend -1 sans muter), activer paquet dynamique complet (reserves.faim.reserve=100.0, _slot correct), activer inscrit au monde, activer hors bornes rend "". Aucun mecanisme du coeur touche en dehors de scripts/peuplement.gd (fichier deja en ECART FRAMEWORK, patron autorise). VERT : test_peuplement_masse (6 cas neufs), test_banc_peuplement, test_objet_isolation, test_profil_peuplement, test_monde, test_monde_structure_simple, test_index_spatial_cpp, test_separation_cpp. Chiffre memoire statique a N=100 000 en regime_masse=true a relever par Yael sur le moniteur Memoire en jeu (attendu : chute nette sous 569 Mo -- le Dictionary individu et son _counter/id String par unite disparaissent completement).
- 2026-09-08 — PARTAGE COW DU PAQUET PAR DEFAUT (chantier "partage COW du paquet par defaut"). Diagnostic : a N=100 000 mobile_test, la memoire statique atteignait ~1 Go (plafond 1 Gio). `objet.gd:fabriquer` faisait `duplicate(true)` sur chaque paquet herite (`objet_physique + dynamique` pour mobile_test) -- `dynamique` porte ~20 sous-Dict/Array (reserves 5 canaux, deformation_etat, etats, engagement, liens_personnels, canaux_config sur ceux qui composent percevant, etc.). ~1-2 Ko duplique par unite x 100 000 = centaines de Mo. La composition d'un objet est IMMUABLE de sa vie (docs/design.md) : le paquet par defaut n'a aucune raison d'exister en 100 000 exemplaires. Correctif : nouveau parametre `paquets_partages: bool = false` (defaut = comportement historique intact, deep-copy, ZERO regression sur les 117 fichiers qui mutent `.reserves.faim.reserve` etc. sur les colons). Sous `true`, cache statique `_paquets_partages_cache` : chaque paquet (par nom) est deep-copie UNE fois au premier appel puis merge SHALLOW dans chaque instance -- les sous-Dict/Array de premier niveau sont PARTAGES PAR REFERENCE. Le Dict `proprietes` top-level reste propre a chaque instance. Nouveau helper public `Objet.detacher(proprietes, cle)` : deep-copie un sous-Dict/Array pour l'isoler avant mutation (idempotent, silencieux sur cle absente). Nouveau helper `Objet.vider_cache_paquets_partages()` pour les tests. `Peuplement.spawn` gagne `paquets_partages: bool = false` (defaut historique) et le propage. `banc_peuplement.gd:@export paquets_partages = true` (surcharge JSON dans le meme commit, feedback @export sur banc). Les ecritures que ce banc pose sur proprietes (`profil`, `_slot`, `errance_direction`, `velocite`, etc.) sont TOUTES top-level, donc sures sous le partage : le test d'isolation le prouve. AUCUN CHANGEMENT DU COMPORTEMENT DE LA SIMULATION -- position, separation, physique, deplacer, rendu inchanges. Nouveau `scripts/test_objet_isolation.gd` (5 cas) : partage effectif (mutation d'un sous-Dict vue par les autres), isolation top-level (ecritures `p[cle]` sans fuite, remplacement `p["reserves"] = {...}` sans fuite), `detacher` isole une mutation, idempotence, mode non-partage historique inchange. VERT : test_objet_isolation (5 cas neufs), test_banc_peuplement, test_profil_peuplement, test_densite_effective, test_combustible, test_banc_p1, test_fin_chantier. Chiffre memoire statique a N=100 000 a relever par Yael sur le moniteur Memoire en jeu (attendu : chute nette sous 1 Gio).
- 2026-09-08 — SEPARATION : NIVEAU D'INDEX DEDIE, ARETE ~ RAYON. Sous separation_active, `banc_peuplement._monter_pool` ouvre desormais UN SECOND niveau sur `_index_cpp` a `exposant = ceil(log2(rayon_separation))` (arete 2 pour rayon 2.0) EN PLUS du niveau du deplacer (arete 16). `IndexSpatial::separation_lot` auto-selectionne parmi les niveaux ouverts le plus petit dont l'arete est >= rayon (fallback : plus grande arete disponible), au lieu de lire aveuglement `_niveaux[0]`. `deplacer_lot` iterait deja tous les niveaux, le second est peuple sans code neuf. AVANT : niveau arete 16 pour rayon 2 -> chaque case ramassait des milliers de candidats hors rayon (~1 020 000 us / frame a N=100 000, 1 fps releve par Yael en jeu). APRES : case ~ rayon sur chaque axe, la boucle interne ne voit qu'une poignee de voisins par case. Aucun code C++ nouveau autre que la selection de niveau, aucun changement de la physique / du rendu / du deplacer. `test_separation_cpp.gd` ouvre desormais les deux niveaux et prouve que le C++ auto-selectionne bien le niveau adapte (meme sortie que l'oracle O(N^2)). VERT : test_separation_cpp, test_index_spatial_cpp, test_banc_peuplement, test_profil_peuplement, test_monde, test_monde_structure_simple, test_monde_subdivision. Recompile release. Chiffre in-game a relever par Yael sur `[peuplement] separation=%dus`.
- PIEGE : une requete de voisinage sur un index dont l'arete est >> rayon degenere en quasi-N^2 local -- l'index filtre presque rien, chaque case livre des milliers de candidats hors rayon que la boucle interne compare quand meme. Regle structurelle : l'arete d'un niveau doit suivre le rayon de la requete qui le lit ; ouvrir un niveau dedie par regime de requete si les rayons different fortement. Verifie ici a N=100 000 avec rayon 2 sur arete 16 : 1 020 000 us / frame ; avec arete 2 : chute massive.
- 2026-09-08 — SEPARATION EN LOT, PREMIERE BRIQUE D'IA DE MASSE. Nouvelle methode `separation_lot(positions, rayon) -> PackedVector3Array` sur `IndexSpatial` (extension_terrain/src/index_spatial.{h,cpp}) : pour chaque id, lit les cases touchees par le rayon autour de sa position dans le premier niveau ouvert (voisinage borne, jamais de scan global -- c'est ce pour quoi l'index est fait), cumule diff * (rayon - d) / d pour chaque voisin dans rayon (distance horizontale, y ignore), normalise, rend une direction unitaire horizontale (Y=0) ou Vector3.ZERO si aucun voisin. UNE frontiere par appel : entree PackedVector3Array, sortie PackedVector3Array. NE MODIFIE PAS l'index (const). Architecture SoA : une capacite = une colonne + une passe. La separation ecrit la colonne desiree (l'intention horizontale que la physique consomme deja), ne bouge PAS les unites. `banc_peuplement.gd:@export separation_active` (defaut false, surcharge JSON dans le meme commit) et `@export rayon_separation` (defaut 2.0). true : la passe errance est remplacee par UN appel _index_cpp.separation_lot(cols.position, rayon_separation) + ecrasement de desiree par direction*vitesse ; exige deplacer_cpp=true + brancher_monde=true (repli errance + push_error sinon). false : errance historique intacte. Seed unique de l'index en fin de _fabriquer_lot (deplacer_lot une fois) pour que la frame 0 ne lise pas un index vide. Nouveau poste `_us_separation` derriere `actif_releve`, ligne `[peuplement]` porte desormais `errance / separation / phys+buffer / deplacer / mm_set`. Recompile release. VERT : nouveau `test_separation_cpp` (parite C++ vs oracle GDScript O(N^2), N=200, rayon=2.0, epsilon=1e-4), test_index_spatial_cpp, test_banc_peuplement, test_profil_peuplement, test_monde, test_monde_structure_simple, test_monde_subdivision. Chiffre en jeu a N=100 000 (poste separation en us + FPS) a relever par Yael sur `[peuplement]`.
- 2026-09-07 — INDEX SPATIAL PORTE EN C++ (mode structure_simple). Nouvelle classe `IndexSpatial : RefCounted` dans `extension_terrain/src/index_spatial.{h,cpp}`, enregistree dans `register_types.cpp`. Miroir 1-pour-1 de `scripts/niveau_monde.gd` : `unordered_map<Vector3i, vector<int32_t>> cases`, `vector<Vector3i> case_de` indexe par id, `vector<int32_t> idx_dans_case`, `vector<uint8_t> presence`, `float inv_arete`, `int exposant`. Ids LINEAIRES 0..N-1 (le peuplement utilise le slot du pool comme id, plus aucun hashing de String par frame). API : `configurer(nombre_ids)`, `ouvrir_niveau(exposant)`, `deplacer_lot(positions)` -- UN appel par frame traite N=100 000 unites en une passe native, UN franchissement de frontiere au lieu de N. `cases_pour_niveau(exposant)` pour parite. `banc_peuplement.gd:@export deplacer_cpp` (defaut false, surcharge JSON dans le meme commit). `_monter_pool` instancie IndexSpatial + configure + ouvre le niveau EXPOSANT_INDEX_CPP=4 (arete 16). `_physics_process` : sous `deplacer_cpp=true`, la boucle `for j: _monde.deplacer_simple(individu)` est remplacee par `_index_cpp.deplacer_lot(cols.position)`. `deplacer_simple` GDScript INTACT (oracle de parite, chemin `deplacer_cpp=false`). L'index GDScript de `_monde` n'est plus tenu a jour sous `deplacer_cpp=true` -- documente dans `index_spatial.h`, aucun effet observable car le peuplement n'interroge pas `choses_dans_rayon` en jeu ; futur usage devra basculer via `cases_pour_niveau` ou porter la requete en C++. Recompile release. VERT : nouveau `test_index_spatial_cpp` (20 frames, N=500, parite bit a bit de la case par unite), test_monde, test_monde_subdivision, test_monde_structure_simple, test_banc_peuplement, test_profil_peuplement. Chiffre en jeu a N=100 000 releve par Yael sur `[peuplement] deplacer=`.
- 2026-09-07 — `monde.gd` : NIVEAU = OBJET RefCounted A CHAMPS TYPES (structure cible du portage C++). Avant : chaque niveau etait un Dictionary { "cases", "case_de", "_idx_dans_case", "_inv_arete", "_exposant", ... }, chaque acces dans `deplacer_simple` faisait un lookup de cle String hache. Nouveau : `scripts/niveau_monde.gd` (RefCounted, 6 champs typés : cases, case_de, idx_dans_case, inv_arete, arete, exposant). `_batir` retourne un `NiveauMonde.new()`. Le hot path lit `niveau_s.inv_arete`, `niveau_s.case_de`, `niveau_s.exposant` par acces de propriete d'objet (slot fixe, plus rapide qu'un lookup Dictionary hache selon la doc Godot 4). BUT REEL : figer la STRUCTURE avant le portage C++, pas le gain GDScript. Cette forme se porte 1-pour-1 en C++ comme un struct native (documente dans niveau_monde.gd). Impact code : renommage des ex-cles `_inv_arete` -> `inv_arete`, `_exposant` -> `exposant`, `_idx_dans_case` -> `idx_dans_case` ; signatures `_batir`/`_niveau`/`_ranger`/`_deranger`/`_inserer`/`_splitter`/`_merger` : niveau untype (accepte l'objet et le Dictionary heritier). Modes subdivision et structure_simple partagent la meme structure (le champ `idx_dans_case` reste vide en subdivision, cout memoire negligeable). Chiffre en jeu releve par Yael a l'ecran sur `[peuplement] deplacer=`. VERT : test_monde, test_monde_subdivision, test_monde_structure_simple, test_banc_peuplement, test_profil_peuplement.
- 2026-09-07 — `monde.deplacer` SEPAREE EN DEUX FONCTIONS. Un `if structure_simple` en tete de `deplacer` etait teste a CHAQUE appel (100 000 fois par frame), et les deux chemins (simple + subdivision) coexistaient dans le meme corps -- alourdissant le bytecode meme quand un seul chemin etait pris. Nouvelle forme : `deplacer(chose)` = chemin subdivision pur (mode par defaut). `deplacer_simple(chose)` = chemin simple pur (mode structure_simple, hot path du peuplement). Le choix se fait CHEZ L'APPELANT une seule fois : `banc_peuplement.gd` appelle `_monde.deplacer_simple(individu)` ; le mode subdivision continue d'appeler `deplacer`. `test_monde_structure_simple.gd` mis a jour pour appeler `deplacer_simple` sur le monde en mode simple. Inline miss-path (chantier precedent) SUPPRIME -- garde le chemin `_deranger`/`_ranger` propre. `scripts/test_profil_deplacer.gd` SUPPRIME : son verdict headless mesurait 99,5% de fast-path (unites bougeant de 0,05/frame) et ne predisait rien du jeu ou les unites bougent a leur vraie vitesse et exercent le miss path. Seul juge de perf desormais : le chrono `[peuplement] deplacer=` en jeu, releve par Yael. VERT : test_monde, test_monde_subdivision, test_monde_structure_simple, test_banc_peuplement, test_profil_peuplement.
- PIEGE : deux chemins dans une meme fonction hot-path appelee par frame, avec un `if` de choix en tete, alourdit le bytecode et ralentit le chemin PRIS -- pas seulement le chemin non pris. Un regime = une fonction, choix une seule fois chez l'appelant. Verifie ici sur `deplacer` : la separation en `deplacer` / `deplacer_simple` est la seule maniere propre.
- PIEGE : un test de profil headless ne predit pas le jeu -- il peut mesurer un mode d'usage (fast-path dominant) qui ne correspond pas au regime reel de la boucle (miss dominant, vraie vitesse). Un chiffre headless qui contredit un chiffre en jeu, c'est le chiffre en jeu qui gagne. Ne plus fabriquer de mesure headless pour trancher un chantier de perf : le seul juge est le chrono en jeu.
- 2026-09-07 — `monde.deplacer` : INLINING DU MISS PATH ESSAYE ET ECARTE. Objectif : eliminer les 2 dispatches `_deranger` + `_ranger` par unite qui change de case. Version testee : retrait + ajout inline dans le chemin structure_simple, avec caches locaux (`cases_s`/`idx_map`/`case_de_s`) UNIQUEMENT dans le bloc miss (jamais en tete de boucle -- l'essai anterieur avec caches en tete avait deja regresse le fast-path). Trois runs coherents sur `test_profil_deplacer.gd` : 1.457 -> 1.844 us/appel mouvement (+26%), 1.329 -> 1.749 us/appel fast-path (+31%). L'inlining REGRESSE AUSSI le fast-path, ou il n'y a pourtant AUCUN cache local ajoute : hypothese, le bytecode plus long change le dispatch de boucle GDScript. ROLLBACK a la version d'appels. VERDICT : gain < 5% (en fait, regression +26%) -- PLANCHER GDSCRIPT ATTEINT. Ce point est ecrit noir sur blanc dans l'en-tete de monde.gd (paragraphe "INLINE DU MISS PATH ESSAYE") pour ne pas se reproposer. Prochain chantier de gain sur ce hot path : portage C++ (ramenerait a ~0.1-0.2 us theoriques). VERT : test_monde, test_monde_subdivision, test_monde_structure_simple, test_banc_peuplement, test_profil_peuplement, test_profil_deplacer. Chiffre en jeu inchange (chemin d'appels restaure).
- 2026-09-07 — `monde.deplacer` : DERNIER NETTOYAGE GDSCRIPT (avant portage C++). Le chantier precedent avait deja pose le chemin dedie `structure_simple` dans deplacer (`_inv_arete` cachee, `case_de[id]` en Vector3i direct, gardes retirees) mais la lecture attentive a montre qu'un lookup residuel restait par iteration : `for exposant in _niveaux:` + `_niveaux[exposant]`. Ajoute `_niveaux_liste: Array` synchronise avec `_niveaux` (append au `_niveau()` a l'ouverture) + `_exposant` stocke dans le niveau au `_batir`. Le hot path itere directement l'Array (chaque itere EST le niveau, aucun lookup dict par iteration). Chute headless mesuree sur `test_profil_deplacer.gd` : 1.60 -> 1.457 us/appel mouvement, 1.46 -> 1.329 us/appel fast-path (-9% ce chantier, -45% cumule depuis 2.632/2.437 avant le premier chantier `structure_simple`). PLANCHER GDSCRIPT proche : le fast-path 1.33 us se decompose en dispatch de fonction (~0.35 us) + `_inv_arete` + Vector3i + `case_de.get` + comparaison (~1.0 us). Portage C++ ramenerait a ~0.1-0.2 us ; a decider en fonction du budget frame reel. VERT : test_monde, test_monde_subdivision, test_monde_structure_simple, test_banc_peuplement, test_profil_peuplement, test_profil_deplacer. Chiffre en jeu a N=100 000 releve a l'ecran par Yael sur `[peuplement] deplacer=`.
- 2026-09-07 — `monde.deplacer` : CORPS DE LA FONCTION REECRIT SOUS `structure_simple`. Le chantier precedent (structure_simple) rendait _ranger/_deranger rapides sans toucher au corps de deplacer -- deplacer restait a ~1.8 us/appel (mesure jeu). Nouveau `scripts/test_profil_deplacer.gd` isole les sous-etapes du corps sur N=100 000 : `_case_pour` (contenant pow) = 0.77 us/appel, `case_de.get(id, [])` (allocation d'Array vide comme default) = 0.91 us/appel, gardes en tete ~0.5 us/appel. TROIS CORRECTIONS dans monde.gd : (1) `_inv_arete` precalcule et stocke DANS chaque niveau a `_batir` -- zero pow dans le hot path ; (2) sous `structure_simple`, `case_de[id]` stocke un `Vector3i` DIRECT (plus un Array de taille 1) -- zero alloc par appel ; (3) chemin dedie dans `deplacer` sans les 2 gardes en tete (contrat clair : sous structure_simple, l'appelant a inscrit via ajouter()). Chute mesuree HEADLESS sur `test_profil_deplacer.gd` : 2.632 -> 1.679 us/appel (-36%). En jeu : chiffre a relever a l'ecran par Yael via `[peuplement] deplacer=`, la chute headless ne se transpose pas telle quelle a une frame reelle. VERT : test_monde, test_monde_subdivision, test_monde_structure_simple, test_banc_peuplement, test_profil_peuplement, test_profil_deplacer. Chronos externes (fichier de test), aucun chrono dans monde.gd -- retirables sans commit.
- PIEGE CORRIGE : le cout de deplacer a N=100 000 (~180 ms) N'ETAIT PAS dans la subdivision (contrairement au PIEGE precedent). Il etait dans le CORPS de deplacer : `_arete = pow(2.0, exposant)` a chaque appel (0.77 us via _case_pour), `.get(id, [])` alloue un Array vide comme default a chaque appel (0.91 us), gardes en tete non pertinentes sur un hot path connu (0.5 us). Le PIEGE precedent reste vrai pour populations DENSES (la subdivision reforme l'index) ; celui-ci s'ajoute pour toute population qui bouge chaque frame. Les deux couches doivent tomber ensemble pour tenir 100 000. Diagnostic reproductible via `scripts/test_profil_deplacer.gd`.
- 2026-09-07 — `monde.structure_simple` : GRILLE SIMPLE POUR LES POPULATIONS QUI BOUGENT TOUTE CHAQUE FRAME. Flag @property sur monde.gd (defaut false = comportement historique intact, subdivision adaptative). true : `_ranger` et `_deranger` prennent une branche COURTE (Array<id> par case, swap-remove O(1) via `_idx_dans_case` inverse, aucun chemin recursif, aucun test SPLIT/MERGE, aucun `_inserer` recursif). `deplacer` reste tel quel : compare `chemin_actuel[0]` a la visee, appelle `_deranger`+`_ranger` sur miss -- les deux prennent leur branche courte sous structure_simple. `_batir` alloue toujours le dict `_idx_dans_case` (cout memoire nul quand structure_simple=false). `banc_peuplement.gd:_monter_pool` pose `_monde.structure_simple = true` juste apres `Monde.new()`. Nouveau `test_monde_structure_simple.gd` : trois cas de parite (ajout+rayon, deplacer, retirer) prouvent que les deux modes rendent EXACTEMENT les memes ids sur choses_dans_rayon apres les memes operations. Aucun mecanisme du coeur autre que monde.gd touche, aucun C++. VERT : test_monde, test_monde_subdivision, test_monde_structure_simple, test_banc_peuplement, test_profil_peuplement. Chiffre du poste `deplacer` a N=100 000 sous structure_simple a relever a l'ecran.
- PIEGE : la subdivision adaptative (SEUIL_SPLIT/MERGE/PROFONDEUR_MAX) est concue pour des tas DENSES et STATIQUES (siege, tas de ressources). Appliquee a une population qui bouge TOUTE chaque frame, elle re-remanie l'index a chaque appel deplacer -- mesure : ~1.8 us/appel a N=100 000 = 179 ms/frame = 90% de la frame, 1 fps. Le cout n'est PAS dans le langage, il est dans la STRUCTURE (chemin[id] a reallouer, _inserer recursif au ranger, test SPLIT sur chaque case dense, test MERGE sur chaque retrait). Un populatoin mobile doit ouvrir `structure_simple=true` sur son Monde.
- 2026-09-07 — INDEX SPATIAL REBRANCHE + POSTE `deplacer` MESURE. `banc_peuplement.gd` gagne `@export brancher_monde: bool = true` (surcharge `data/banc_peuplement.json` dans le meme commit). true : `_monde = Monde.new()`, spawns inscrivent chaque unite dans l'index (deja passait `_monde` mais l'en-tete le disait faussement -- en-tete refait, section MONDE ajoutee). Nouvelle passe par frame dans `_physics_process`, entre phys+buffer et `multimesh_set_buffer` : boucle sur count, `individu.position <- cols.position[i]` (miroir transitoire, une frame -- la colonne reste la verite tenue par la physique) puis `_monde.deplacer(individu)`. Sans cette recopie, `monde.gd:deplacer` verrait la position d'avant la physique, l'index divergerait de la position vivante. false : `_monde = null`, aucun spawn n'inscrit dans l'index, aucun `deplacer` par frame -- comportement de l'ancien banc. Chrono nouveau poste `_us_deplacer` derriere `actif_releve`, ligne `[peuplement]` porte desormais `errance / phys+buffer / deplacer / mm_set`. Aucun mecanisme du coeur touche (monde.gd/peuplement.gd inchanges), physique C++ intacte. Chiffre du poste `deplacer` a N=100 000 a relever a l'ecran par Yael pour trancher son portage C++. VERT : test_banc_peuplement, test_profil_peuplement, test_peuplement_colonnes, test_monde.
- 2026-09-07 — CHARGEMENT INITIAL DES UNITES : PUSH RS UNIQUE AU LIEU DE PAR SPAWN. `scripts/peuplement.gd:spawn` gagne un parametre `pousser: bool = true` (defaut : comportement historique, necessaire pour spawn ISOLE en jeu ou l'unite doit apparaitre immediatement). `pousser=false` ecrit les 12 floats du slot dans `pool.buffer` sans envoyer au RenderingServer. `banc_peuplement.gd:_fabriquer_lot` boucle `Peuplement.spawn(..., false)` puis appelle `Peuplement.pousser_buffer(_pool)` UNE fois en fin de lot -- N=100 000 payait N * multimesh_set_buffer(1,2M floats) = O(N^2) sur le buffer entier, desormais 1 seul push. Chrono de creation ajoute autour de `_fabriquer_lot`, meme gate `actif_releve`, imprime `[peuplement] creation N=%d en %d us (push RS unique final)`. `retirer` reste inchangee (retrait isole en jeu, pas un lot). Contrat de `spawn` documente en tete de la fonction. Nouveau cas 6 dans `test_banc_peuplement.gd` : verrouille que `spawn(..., false)` ecrit bien le slot dans `pool.buffer` (base identite + position) sans toucher au RS. VERT : test_banc_peuplement (6 cas), test_profil_peuplement, test_peuplement_colonnes, test_tick_fusionne, test_physique_simple_lot_cpp. Chiffre du chargement N=100 000 a relever a l'ecran.
- 2026-09-07 — PHYSIQUE DU PROFIL SIMPLE PORTEE EN C++. Nouvelle classe `PhysiqueSimpleLot : RefCounted` dans `extension_terrain/src/physique_simple_lot.{h,cpp}`, enregistree dans `register_types.cpp`. Miroir NATIF de `banc_peuplement.gd::physique_et_buffer` (elle-meme miroir de `pas_simple_lot`) : gravite, terminale, composition horizontale, trois tests sol via table plate + repli, snap, au_sol, buffer MultiMesh (3 floats/slot). REPLI SUR MISS : le C++ N'APPELLE JAMAIS `carte.sommet_sous` (autre fonction du coeur, hors perimetre) -- chaque indice qui rate au moins un des 3 tests sol est renvoye dans `indices_a_repasser`, le C++ ne mute PAS ses colonnes ni son slot buffer, GDScript rejoue via `physique_et_buffer_indices` (nouveau helper static, miroir du corps sur un sous-ensemble d'indices). extension_terrain regroupe desormais tout le hot path natif du peuplement + terrain, une seule .dll, un seul SConstruct, un seul register_types. `banc_peuplement.gd:_physics_process` derriere `@export utilise_cpp` (defaut false -- GDScript reste l'oracle et le rollback) ; surcharge `utilise_cpp` ajoutee a `data/banc_peuplement.json` dans le meme commit. `pas_simple_lot` / `physique_et_buffer` GDScript INTACTS. Recompile release (`python -m SCons target=template_release`). VERT : nouveau `test_physique_simple_lot_cpp` (30 frames, 200 agents, parite a 5e-4 pres), `test_tick_fusionne`, `test_banc_peuplement`, `test_profil_peuplement`, `test_peuplement_colonnes`. Gain in situ a l'ecran par Yael (`[peuplement]` sous `utilise_cpp=true` vs `false`).
- 2026-09-07 — CHRONOS DE RELEVE POSES SUR LE HOT PATH DE `banc_peuplement.gd`. Trois postes chronometres avec `Time.get_ticks_usec()` dans `_physics_process` : (1) passe errance, (2) `physique_et_buffer`, (3) `RenderingServer.multimesh_set_buffer`. Une ligne par SECONDE (jamais par frame) au format `[peuplement] N=%d fps=%d errance=%dus phys+buffer=%dus mm_set=%dus`, moyenne par frame sur la seconde ecoulee. Patron des compteurs de `monde.gd` repris : accumulateurs + remise a zero apres chaque impression. `@export var actif_releve` (defaut true) : sous false, aucun `Time.get_ticks_usec` ni impression, banc strictement identique -- surcharge `data/banc_peuplement.json` ajoutee dans le meme commit (feedback @export sur banc = surcharge JSON obligatoire). `nombre_individus` deja pilote par le JSON (10 000 aujourd'hui), pool alloue a `nombre_individus * 2` (l.163) : le passage a 100 000 tient uniquement si `Peuplement.creer_pool` accepte 200 000 slots -- aucun plafond dur cote banc, plafond eventuel a chercher dans `scripts/peuplement.gd:creer_pool` si le test de charge le refuse. AUCUN mecanisme du coeur touche (mouvement_kinematic.gd/monde.gd inchanges). VERT : test_banc_peuplement, test_profil_peuplement, test_peuplement_colonnes.
- 2026-09-07 — WRAPPER PAR CANDIDAT SUPPRIME. `perception.gd:_obstacles_depuis_candidats` retiree. Dans `_percevoir_propagation_obstacles`, la liste d'obstacles passee a `Occlusion.facteur` par source est batie en appendant directement `entree.chose` de chaque entree du couloir -- une seule Array par source, references vers les choses vivantes, ZERO Dictionary alloue par candidat. Contrat d'`occlusion.gd:facteur` explicite en en-tete : obstacle duck-type exposant `position` (obligatoire), `id` et `proprietes` (facultatifs) -- couvre a la fois la chose du monde brute (Objet.fabriquer, format `{id, position, proprietes, ...}`) et le dict plat `{position, proprietes}` de champ_occulte. Aucun changement de comportement : le test de parite couloir (`test_banc_occlusion.gd:_parite_couloir_vs_sphere_entiere_sur_les_trois_cas_pieges`) reste VERT, meme facteur d'occlusion source par source. VERT : test_perception, test_occlusion, test_champ_occulte, test_banc_occlusion, test_banc_absorption_sonore, test_monde.
- 2026-09-07 — PARITE COULOIR VERROUILLEE. `test_banc_occlusion.gd` porte un cas `_parite_couloir_vs_sphere_entiere_sur_les_trois_cas_pieges` : un percepteur, deux sources (une devant, une derriere), six obstacles couvrant les trois cas pieges (PILE a la demi-largeur, juste au-dela, derriere le percepteur) plus un obstacle dans le couloir et un lateral hors couloir. Chemin reel = `Perception.percevoir` via `monde.choses_dans_couloir`. Oracle = recalcul sur place qui passe la sphere ENTIERE (`monde.choses_dans_rayon`) a `Occlusion.facteur` -- l'ancien comportement. Egalite prouvee sur les ids retenus ET sur le facteur par source (`is_equal_approx`). Cas pieges assertes nommement. Seuil du canal fixe a 0.001 (avec seuil 0.0 tout candidat passe force=0 -> attenuee=0, 0<0 faux, test trivial). Aucun fichier du coeur touche. VERT : test_banc_occlusion, test_banc_absorption_sonore, test_perception, test_monde.
- 2026-09-07 — OCCLUSION SORTIE DU O(n^2). `monde.choses_dans_couloir(depuis, vers, demi_largeur)` ajoute -- bbox du segment epaissi a resolution `_exposant_pour(2 * demi_largeur)`, test capsule-vs-AABB par case, distance point-segment par candidat, compteurs `requetes`/`cases_lues`/`candidats_mesures` alimentes. `perception._percevoir_propagation_obstacles` ne normalise plus toute la sphere en obstacles : une requete couloir par source, liste bornee au tube percepteur->source. `occlusion.facteur` inchange (geometrie pure). En-tetes des 3 fichiers a jour, notes de cout mises a jour, ECART FRAMEWORK note dans `monde.gd`. `jeu/CARTE.md` : `choses_dans_couloir` ajoute a la ligne monde.gd. Tests VERT : `test_monde` (+ nouveau cas cout `_choses_dans_couloir_ne_lit_que_les_cases_traversees` -- N=2000 hors couloir, 3 dedans, `candidats_mesures < 50`), `test_occlusion`, `test_perception`, `test_banc_p1`. Exception frontiere (CLAUDE.md § Frontiere) au su de Yael.
- 2026-09-07 — CPP : CONTENEURS DE MESHING REUTILISES ENTRE BAKES (cache thread-local facon godot_voxel). Avant : `bake_tuile_a` declarait localement 3 `unordered_map<int,BucketItem>` et 4 `vector<int32_t>` -- allocation + destruction complete a chaque tuile ; les `vector<float>` des BucketItem etaient recrees eux aussi. Nouveau : les 7 conteneurs sont MEMBRES `mutable` de `MesheurTuile` (declares dans `mesheur_tuile.h`, `BucketItem` nested private du meme). En debut de `bake_tuile_a` on `clear()` les 4 vecteurs de sortie et on `clear()` SEULEMENT le `buffer` de chaque BucketItem des 3 maps -- vider la map elle-meme jetterait la capacite des buffers accumulee sur les tuiles precedentes, on garde les entrees. `clear()` sur `vector` conserve la capacite, `clear()` sur le buffer d'un BucketItem aussi. Apres quelques tuiles, plus aucune reallocation. Sortie deja copiee en `PackedFloat32Array`/`PackedInt32Array` par `items_to_dict`/`to_packed_int`, donc les vider au bake suivant ne casse rien cote appelant. En-tete `mesheur_tuile.h` mis a jour. Recompile release. Tests a verifier par Yael a l'ecran (Yael a explicitement demande de ne pas relancer la suite entiere -- pas de regression fonctionnelle attendue, sortie inchangee bit a bit).
- 2026-09-07 — CPP : ecrire_instance ECRIT 16 FLOATS EN UN INSERT au lieu de 16 push_back. Avant : chaque instance (face de cube + mini-cube) passait par 16 `buffer.push_back(...)` = 16 appels + 16 tests de capacite. Le buffer est deja reserve par bucket, donc la realloc n'est plus le cout ; le cout restant est le par-appel. Nouveau : tableau local `const float v[16] = { ... }` sur la pile avec les 16 valeurs dans l'ordre exact du layout MultiMesh TRANSFORM_3D + color (b.rows[0].xyz, origin.x, b.rows[1].xyz, origin.y, b.rows[2].xyz, origin.z, r, g, bl, a), puis `buffer.insert(buffer.end(), v, v + 16)` -- le bloc de 16 floats ecrit d'un coup. Ordre des 16 valeurs identique a avant, layout et signature de la fonction inchanges. Recompile release. Poste "cpp" mesure : baisse attendue sur les tuiles a beaucoup d'instances, nulle sur les tuiles vides. Tests a verifier par Yael a l'ecran.
- 2026-09-07 — CPP : cellules RETIRE de BucketItem, capacite du buffer PRE-RESERVEE. Avant : chaque face faisait 16 push_back sur `bucket.buffer` + 3 push_back sur `bucket.cellules`, sur deux `std::vector` non-reserves -- reallocations qui doublent la taille (recopie complete) plus la tuile est chargee. `cellules` ne servait qu'a fournir `count = size/3` a la teinte, or `count = buffer.size()/16` deja disponible. Nouveau : `BucketItem` ne porte plus que `buffer` ; `idx_start` et `count_faces` derives de `buffer.size()/16` avant/apres emission des 6 faces ; helper local `obtenir_bucket(map, item)` reserve idempotente `buffer.reserve(taille*taille*couches_max*6*16/4)` au premier acces d'un item (capacity == 0). La borne est le quart de la borne haute -- peu d'items concentrent la masse ; reserver plein pour chaque coute plus cher que ce qu'on economise. `ecrire_cellule` reste utilise pour `cellules_occl` et `cellules_teintables` (inchange). Recompile release. Tests : `test_rendu_terrain_multimesh._s2_rebuild_par_tuile` reste ROUGE preexistant (identique a HEAD, deja documente L25) ; autres phases (S3/S4/S5) VERT ; teinte identique a l'ecran (meme index 6-tuples). Poste "cpp" mesure en baisse a verifier par Yael (chronos toujours en place).
- 2026-09-07 — CPP : MULTIMESH REPASSE COTE GDSCRIPT (rollback ECARTE de la construction Ref<MultiMesh> en C++). Construire le MultiMesh dans `bake_tuile_a` a fait monter le total au lieu de le baisser (tuiles legeres ~72-80 us avant, ~100-160 us apres, mesure Yael). Doc `MultiMesh.set_instance_count` : « clears and (re)sizes the buffers » -- allocation RenderingServer poussee en C++, puis le `Ref<MultiMesh>` traversait la frontiere Variant comme objet Godot avec RID deja alloue, plus cher que le PackedFloat32Array brut qu'il remplacait. `set_buffer` recopie le buffer dans les deux cas -- aucune recopie n'a ete economisee. Retour au contrat precedent : `bake_tuile_a` sort par item `{ buffer: PackedFloat32Array }`, `_bake_mm_depuis_buffer` reconstruit `MultiMesh.new()` avec l'ordre obligatoire (use_colors AVANT instance_count, buffer APRES). `_mmi_depuis_buffer` / `_rs_depuis_buffer` re-lisent `entree["buffer"]` et deduisent `count = buffer.size() / 16`. En-tete `mesheur_tuile.h` et commentaires GDScript remis a l'ancien contrat. Recompile release. Tests : `test_rendu_terrain_multimesh._s2_rebuild_par_tuile` reste ROUGE preexistant (identique a HEAD, deja documente L25) ; chronos confirment le retour au niveau d'avant le chantier Ref<MultiMesh>. ECARTE DOCTRINAL : ne pas re-tenter de construire le MultiMesh en C++, la traversee de frontiere Variant + l'allocation RS deportee coutent plus que la double recopie qu'on cherchait a supprimer.
- 2026-09-07 — CPP : MULTIMESH CONSTRUIT EN C++. Avant : `bake_tuile_a` sortait par item `{ buffer: PackedFloat32Array, cellules: PackedInt32Array }` que le GDScript reposait sur un MultiMesh neuf via `mm.buffer = ...` -- le buffer traversait deux fois la frontiere (packing cote C++, pose cote GDScript). Nouveau : le C++ construit un `Ref<MultiMesh>` par item, pose `set_transform_format(TRANSFORM_3D)` / `set_use_colors(true)` / `set_instance_count(count)` / `set_buffer(...)` en natif, et sort `{ mm: MultiMesh, count: int }`. `_bake_mm_depuis_buffer` ne fait plus `MultiMesh.new()` ni `mm.buffer = ...` : il pose seulement `mm.mesh` (le C++ n'a pas acces au `mesh_library`). `_mmi_depuis_buffer` et `_rs_depuis_buffer` lisent `entree["mm"]` et `entree["count"]`. La reference forte cote GDScript reste (rangee dans `_tuiles` / `instances_rs` / `mm_par_item_*`) -- indispensable, sinon GC -> RID invalide (issue godotengine/godot#80479). En-tete de `mesheur_tuile.h` mis a jour, ainsi que les 2 gros commentaires GDScript qui decrivaient l'ancien format `{buffer, cellules}`. Recompile release. Tests : `test_rendu_terrain_multimesh._s2_rebuild_par_tuile` reste ROUGE preexistant (identique a HEAD, deja documente L25) ; autres phases (S3/S4/S5) VERT ; le MultiMesh construit en C++ arrive vivant en GDScript (aucun plantage teinte / occludeur / RS direct). Gain effectif sur le poste "cpp" a mesurer a l'ecran par Yael (chronos toujours en place).
- 2026-09-07 — BLOB : merge GDScript SUPPRIME. Avant : `_blob_tuile_a` mergait les 9 entrees d'index (self + 8 voisins) en 4 Dictionary (`volumes`/`particularites`/`masques_sous_cube`/`pv_sous_cubes`) via `Dictionary.merge()`, que le C++ remontait ensuite en 4 unordered_map -- chaque paire copiee deux fois. Nouveau : le blob passe un `Array` des 9 entrees d'index brutes (entrees vides omises) sous une seule cle `entrees_index` ; `bake_tuile_a` itere ces 9 entrees a l'entree et peuple ses unordered_map natifs directement en une passe. Chaque cellule vivant dans UNE seule tuile (clefs disjointes), iterer 9 entrees peuple exactement les memes maps que le merge produisait -- comportement du bake strictement identique. Une copie de moins par paire du cote GDScript (le C++ fait la meme lecture sur les memes paires). Gain effectif sur le poste "blob" non mesure ici -- a l'ecran par Yael. En-tete de `mesheur_tuile.h` mis a jour. `jeu/CARTE.md` et `jeu/design.md` non touches (ils ne decrivent pas le contrat du bake). Recompile release. Tests : `test_rendu_terrain_multimesh` `_s2_rebuild_par_tuile` reste ROUGE (preexistant a HEAD, verifie par git stash, deja documente L25), autres phases du test (S3/S4/S5) VERT ; `test_carte_terrain`, `test_carte_sous_cube` VERT ; `test_persistance` ROUGE preexistant a HEAD (verifie par git stash).
- 2026-09-07 — TEINTE : PLAGE CONTIGUE au lieu de deroule par face. Avant : `_pousser_candidats_teinte` faisait un append PackedInt32Array PAR FACE (idx_start..idx_start+count deroules) et reassignait `bucket["idxs"] = bidxs` par cellule -- piege copy-on-write PackedArray, ~1 us par face x N cellules. Nouveau : stocker (cellule, idx_start, count) UNE fois par cellule teintable, PAS par face -- le deroule se fait au tick teinte. Format bucket : `{cellules: Array[Vector3i], idx_starts: Array[int], counts: Array[int]}` (3 Array paralleles, 1 append de scalaire par cellule, pas de PackedArray append + reassign). Index de tuile compact : `index_tuile[cellule] = {mm: MultiMesh, idx_start: int, count: int}` (une cellule appartient a UN seul bucket -- par_forme XOR par_forme_sol -- donc une seule entree, pas de tableaux paralleles ici). `_tick_teinte` fait 3 acces cle par cellule (mm/idx_start/count) puis boucle entiere `for i in range(count): mm.set_instance_color(start + i, couleur)`. Comportement teinte strictement identique -- meme cellules, memes couleurs, memes plages du buffer touchees.
- 2026-09-07 — TEINTE : TABLEAUX PARALLELES au lieu d'Array de Dict. Avant : `_pousser_candidats_teinte` allouait un `Dictionary{cellule, idx}` PAR FACE et `_agreger_teinte` un `Dictionary{mm, idx}` PAR FACE, puis `_tick_teinte` lisait `face["mm"]` / `face["idx"]` -- deux acces cle String par face a chaque tick 1 Hz. Nouveau : `teinte_normal[item]` = `{"cellules": Array[Vector3i], "idxs": PackedInt32Array}`, `_teinte_par_tuile[tuile][cellule]` = `{"mms": Array[MultiMesh], "idxs": PackedInt32Array}`. `_pousser_candidats_teinte` et `_agreger_teinte` remplissent les tableaux paralleles, `_tick_teinte` fait 2 acces cle par cellule ("mms", "idxs") puis iteration entiere `mms[i].set_instance_color(idxs[i], couleur)`. `_pousser_teinte` retire (n'a plus d'appelant). Zero allocation Dict par face au bake, zero acces cle String par face au tick. Comportement teinte strictement identique -- meme cellules, memes couleurs, seule la structure interne change. Chronos laisses en place pour mesure.
- 2026-09-07 — VERIFICATION post-refonte teinte : aucune trace residuelle de `_extraire_teinte` (definition, appels) ni du short-circuit `cellules_teintables.is_empty()` dans `rendu_terrain_multimesh.gd`. Le short-circuit n'a jamais ete pose : il a ete propose puis remplace directement par la vraie refonte C++ (le C++ rend `teinte_candidats_normal/sol` prets). Le seul chemin de teinte est desormais `_pousser_candidats_teinte` sur les 6-tuples C++. Seule mention restante dans le repo : entrees historiques dans SUIVI.md.
- 2026-09-07 — 3 OPTIMISATIONS finales du bake. (1) TEINTE : `_extraire_teinte` SUPPRIME. Le C++ rend maintenant `teinte_candidats_normal` et `teinte_candidats_sol` en sortie -- PackedInt32Array de 6-tuples (item, x, y, z, idx_start, count) par cellule cubique propre visible. Le GDScript pousse la teinte directement via `_pousser_candidats_teinte` sans re-parcourir tous les faces de `par_forme[item].cellules`. Une entree par cellule teintable au lieu de N entrees × 6 faces × 2 buckets. (2) PROFIL : les 3 lookups `_ressources.call("profil_de_cellule" / "quantite_a", cellule)` remplaces par appels TYPES DIRECTS `_ressources.profil_de_cellule(cellule)` / `_ressources.quantite_a(cellule)` -- Godot skip le dispatch string, ~2-3x plus rapide par cellule. (3) C++ CATALOGUES : `is_cubic`, `hauteur_par_item`, `mesh_transforms`, `bases_ortho` deviennent des MEMBRES de `MesheurTuile`, montes UNE fois via `configurer_catalogues(dict)` appele lors de l'instanciation dans `_phase_parser`. Ces 4 boucles de parsing (~15-30 us) sont retirees de `bake_tuile_a`. Le blob par tuile ne porte plus que les donnees SPECIFIQUES (volumes, particularites, sous_cubes, pv, scalaires). Build release, prints temporaires poses puis retires avant commit.
- 2026-09-07 — PREMIER CHARGEMENT DU TERRAIN RENDU en SYNCHRONE. Le _ready appelle maintenant `_amorcer_synchrone` apres `_rafraichir_vers` : draine `_file_creation` d'un bloc, chaque tuile bakee entierement (3 phases inline `_phase_parser` + `_phase_baker_instances` + `_phase_baker_occluder`) sans passer par le pipeline. L'ecran est vide au tout premier chargement, le joueur n'interagit pas -- aucun a-coup a lisser, aucune raison d'etaler sur des minutes. Le pipeline et le debit adaptatif restent en place pour tout ce qui vient APRES en jeu (avance du joueur, tuiles decouvertes une par une). `_creer_tuile` inchange -- c'est `_amorcer_synchrone` qui bypasse la file au premier tour. Terrain complet visible en une fraction de seconde au lancement au lieu de tranche par tranche. Print temporaire pose puis retire avant commit.
- 2026-09-07 — MODE "GROS" du drain pipeline : 3 phases GROUPEES dans la meme frame quand la file est au-dessus de `seuil_pipeline_debit_haut`. Avant : chaque tuile n'avancait que d'UNE phase par frame -> a debit 20, ~6-7 tuiles FINIES par frame seulement (20 avances / 3 phases). File se remplissait plus vite qu'elle ne se vidait. L'etalement en 3 phases sert a lisser le pic EN JEU ; au premier chargement l'ecran est vide, aucun a-coup a lisser -- le triple etalement multiplie le temps de chargement pour rien. Nouveau : au-dessus du seuil, `_phase_parser` + `_phase_baker_instances` + `_phase_baker_occluder` s'executent d'affilee dans une meme iteration du drain, tuile FINIE en un tour. En-dessous du seuil (regime de jeu, file courte), on garde le comportement 1 phase/tuile pour ne pas piquer. Plafond `tuiles_avancees_par_frame_haut` releve de 20 a 40 (40 × ~550 us de bake ≈ 22 ms, tient dans une frame de premier chargement sans figer). Print temporaire `[pipeline] taille= debit= gros=` pose puis retire avant commit.
- 2026-09-07 — DEBIT DU PIPELINE INTRA-TUILE ADAPTATIF, meme principe que le debit de creation. Avant : `tuiles_avancees_par_frame = 1` fixe -> anneau initial (~450 tuiles × 3 phases = 1350 avancees) prenait des minutes a se remplir. Ajout de 2 @export : `tuiles_avancees_par_frame_haut` (defaut 20, plafond regime remplissage) et `seuil_pipeline_debit_haut` (defaut 40, taille de `_tuiles_en_pipeline` a laquelle le plafond est atteint). Helper `_debit_pipeline_pour_file(taille)` calque `_debit_pour_file` : rampe lineaire entre `tuiles_avancees_par_frame` (plancher, defaut 1 -- regime de jeu, aucun a-coup en avancant) et le plafond. Le drain pipeline dans `_process` lit ce debit au lieu de la constante. Anneau initial se vide desormais en 1-2 s au lieu de minutes ; regime de jeu inchange (file courte -> debit bas). Print temporaire pose puis retire avant commit.
- 2026-09-07 — COLLISION INITIALE ETALEE. Avant : `terrain_visible.gd:_ready` appelait `_rafraichir_vers(centre)` qui posait synchrone tout le disque (~55 000 cellules a rayon 50) -- pic long au premier chargement. Nouveau : `_amorcer_pose_etalee(centre)` pose SYNCHRONE un NOYAU minimal (`rayon_noyau_initial`, defaut 2 cellules = 13 colonnes) autour de l'observateur pour porter le joueur au spawn, et enfile le RESTE du disque dans `_a_poser` -- draine par `_process` sur les images suivantes au meme rythme que le drain deja verrouille pour le jeu (`colonnes_par_image`). `rafraichir()` (statique) est INCHANGEE -- c'est le chemin des tests L137/153/199/228/257 et de `_rafraichir_vers` qui n'est plus appele au _ready mais reste dispo. Test `_etalement` de `test_terrain_visible.gd` adapte au nouveau contrat (les deux assertions "premier affichage complet" et "files vides au _ready" avaient sens avec l'ancien synchrone ; nouvelles assertions : noyau strictement plus petit que le disque, noyau + reste == disque, `_a_effacer` vide) ; l'aller-retour sans drainage repointe (15,0) au lieu de (4,0) pour sortir le noyau du disque vise et vraiment prouver le travail de `_retargeter`. Chrono mesure avant commit (ancienne synchrone 5.1 ms | nouvelle noyau+file 0.6 ms au test, disque r=10) puis retire. Test terrain_visible VERT ; rouges preexistants inchanges (test_carte famille emprise deja documente L23).
- 2026-09-07 — DEBIT DE BAKE VARIABLE selon la taille de `_file_creation` (et `_a_supprimer`) : rampe LINEAIRE entre `tuiles_par_frame` (file vide, defaut 1) et `tuiles_par_frame_haut` (file >= `seuil_file_debit_haut`, defaut 15/20) au lieu d'un basculement binaire au seuil. Sans rampe, la file passait d'un debit x1 a x15 sur une tuile de plus ou de moins -- saut visible en jeu quand on longe le seuil. Rampe posee dans un helper `_debit_pour_file(taille)` appele par les deux drains (creation + suppression), robuste a `bas > haut` (les tests poussent `tuiles_par_frame = 1000` : le PLANCHER gagne via `maxi(bas, haut)`). Aucune modif du rayon, du bake, de l'index spatial ou du MultiMesh. Test `_s2_rebuild_par_tuile` de `test_rendu_terrain_multimesh.gd` deja rouge AVANT ce chantier (verifie par git stash), inchange -- pas de regression introduite ici.
- 2026-09-07 — 3 OPTIMISATIONS bake, mesurees. (1) BLOB : tables INVARIANTES (bases_orthogonales / items_cubiques / items_hauteur_cle-val / mesh_transforms / centre_offset) sorties du blob par tuile -- calculees UNE fois au `_ready` dans `_construire_caches_invariantes`, stockees en membres, referencees par le blob (PackedArray copy-on-write / Dictionary par reference, aucune copie tant qu'aucun mutateur). (2) C++ : `hauteur_par_item.find(item)` sortie de la lambda `emettre_face` (appelee jusqu'a 6x par cellule) vers UNE lecture par cellule, capturee par valeur dans la lambda -- 5 lookups economises par cube propre. `mesh_transforms` monte en `std::unordered_map<int, Transform3D>` a l'entree (comme `particularites`), remplace `mesh_transforms.get(item, Variant())` par cellule non-cubique -- supprime la derniere frontiere Variant de la boucle. (3) TEINTE : `_cache_reserve_cellule` (Dict[Vector3i, int]) peuple en meme temps que `_cache_profil_cellule` dans `_phase_parser` (`profil.get("reserve")` est un acces Variant, reserve ne change pas pour un profil donne) ; `_tick_teinte` lit `_cache_reserve_cellule.get(cellule, 0)` au lieu de `profil.get("reserve")`. Purge du cache alignee avec les 2 autres dans `_supprimer_tuile`. Build release. Prints temporaires poses puis retires avant ce commit ; mesure a l'ecran par Yael.
- 2026-09-07 — NETTOYAGE post-voie C : retrait de 2 helpers morts (`_masque_col`, `_masque_couvrant_col`) datant de l'ancien `_blob_tuile_a` (qui parcourait la tuile). Depuis la refonte C++ puis la voie C, plus rien ne les appelle -- la visibilite se calcule intégralement en C++ (`masque_col` inline + boucle 12x12). `visible_bits_col` (static) est GARDEE : verrouillee par 2 tests (`test_rendu_terrain_multimesh.gd:286/298`, `test_ecosysteme.gd:_test_visible_bits_col`). Note : la voie A (copier tout carte.volumes/particularites/etc par tuile) n'a JAMAIS ete ecrite -- annulee au moment ou je commencais, remplacee directement par C. Rien de A dans le code.
- 2026-09-07 — VOIE C : INDEX SPATIAL PAR TUILE, coupe le cout du blob. Chronos separes (blob / cpp / teinte) ont prouve que ~95% du temps de bake (mesure : 4400 us sur ~5000) partait dans `_blob_tuile_a` qui fouillait carte.masque / sous_cubes / pv / _masque_couvrant_col des centaines de fois par tuile ; le C++ (~70 us) et la teinte (~85 us) etaient deja rapides. Voie A envisagee (copier les dicts globaux carte.volumes / particularites / _masques_sous_cube / _pv_sous_cubes par tuile) ECARTEE : la carte vise l'immense (3M km² streamee), scanner tout le dict global par tuile crevait a grande echelle. Voie C retenue et implementee : `_index_par_tuile[Vector2i]` porte, par tuile, SES colonnes/cellules non-defaut (4 sous-dicts : volumes, particularites, masques_sous_cube, pv_sous_cubes). Construit une fois au `_ready` (dispatch de chaque dict de carte vers sa tuile owner) ; maintenu incrementalement dans `_absorber_modifications_carte` (`_mettre_a_jour_index_pour_colonne` scanne les 63 rangs d'UNE colonne modifiee, cout constant independant de la taille de la carte). Le blob assemble par `_blob_tuile_a` merge les 9 entrees d'index (tuile + 8 voisins) -- aucun parcours de tuile, aucun lookup carte.* en boucle. Le C++ recoit ces 4 dicts tuile-locaux (petits), les monte en `std::unordered_map` natifs une fois a l'entree, calcule visibilite + couvrants + emission en natif via un helper `masque_col` inline. Frontiere franchie UNE fois par tuile. Sortie C++ inchangee (buffer PackedFloat32Array par item + cellules + 4 derives). Le C++ reste en release (target=template_release, les deux slots windows.x86_64 pointent sur la .dll release). Mesure a faire par Yael a l'ecran (prints `bake tuile / blob / cpp / teinte` temporaires en console -- seront retires au commit une fois valides).
- 2026-09-06 — REFONTE DU PORTAGE C++ : 1 seul parcours + MultiMesh construit via buffer (fin des 2 foirages architecturaux qui rendaient le C++ ~60% plus lent que le GDScript pre-portage). DEUX FOIRAGES CORRIGES : (1) QUATRE parcours de la tuile la ou le GDScript en faisait UN -- `_phase_parser` recalculait la visibilite pour occl/min-max/cache teinte, `_blob_tuile_a` recalculait les masques 12x12 puis re-parcourait pour particularites/sc/pv, et le C++ recalculait la visibilite une 3e fois pour emettre ; le commentaire assumait de ne pas partager les derives "pour minimiser le contrat", faux. (2) La SORTIE C++ reconstruisait un Variant par instance (Array<Transform3D> par item, Array<Dictionary{transform,couleur}> par mini-cube) -- des centaines d'append Variant par tuile, exactement le pattern denonce par godot-cpp issue #1063 ("Variant en hot path 10-50x un type natif"). REFONTE : `MesheurTuile::bake_tuile_a` rend maintenant, PAR ITEM, un buffer plat `PackedFloat32Array` (16 floats/instance : 12 transform + 4 color, layout TRANSFORM_3D pour `MultiMesh.buffer`) + `cellules` parallele ; ainsi que les derives `cellules_occl`, `couche_min`, `couche_max`, `cellules_teintables`, tous produits dans SON UNIQUE parcours. `_phase_parser` GDScript ne parcourt plus la tuile -- il monte le blob (`_blob_tuile_a` inchange), appelle le C++, lit `_ressources.profil_de_cellule` sur la liste de cellules teintables qu'il recoit (pas par re-parcours), extrait les paires teinte des `cellules` paralleles, construit le Dict `cellules_occl` depuis le PackedInt32Array de sortie. `_phase_baker_instances` consomme le nouveau format via 3 nouveaux helpers (`_bake_mm_depuis_buffer` / `_mmi_depuis_buffer` / `_rs_depuis_buffer`) qui posent le buffer entier via `mm.buffer = ...` en UN appel natif -- ZERO append Variant par instance cote GDScript. Les 6 anciens helpers (`_bake_mm_forme` / `_bake_mm_mini_cubes` / `_mmi_de_forme` / `_mmi_mini_cubes` / `_rs_de_forme` / `_rs_mini_cubes`) et les 3 helpers `_appliquer_cpp_a` / `_integrer_cpp` / `_integrer_cpp_mini` sont retires. Compilation `target=template_release` (au lieu de `template_debug` qui compilait -O0, pas d'inlining, unordered_map non optimise) ; `terrain.gdextension` : les DEUX slots `windows.x86_64` (debug ET release) pointent sur la .dll release, pour que l'editeur (qui utilise le slot debug) tourne aussi en release -- sans ca, toute mesure de perf en editeur est faussee. La .dll debug reste sur disque pour redeploiement futur si besoin. Mesure a faire par Yael a l'ecran (prints `bake tuile : N us` temporaires en console, seront retires au commit une fois validation visuelle et chiffres releves).
- 2026-09-06 — ETAPE (d) du portage C++ : retrait du flag `utilise_cpp_phase0` et des 3 chemins GDScript morts (mini-cubes, non-cubes, cubes propres) de `_phase_parser`. Le C++ (MesheurTuile.bake_tuile_a) est le chemin UNIQUE de meshing depuis cette etape ; plus de fallback. Simplifications : `_ajouter_face` / `_ajouter_mini_cubes` / `_voisin_couvre` supprimes, tableau `_faces` (6 orientations de face) retire (les 6 bases vivent maintenant en C++ dans face_base / face_normale), variables mortes (`orientation`, `pos`, `cible`, laterales `nxp/nxm/nzp/nzm`) retirees de la boucle. Le parcours GDScript ne conserve QUE : (1) visibilite pour cellules_occl (phase 2 occluder) et couche_min/max (phase 1 AABB), (2) peuplement des caches `_cache_profil_cellule` / `_cache_quantite_cellule` pour cubes propres teintables. `_appliquer_cpp_a` appele desormais inconditionnellement en fin de boucle. `verification.tscn` : ligne `utilise_cpp_phase0 = true` retiree (propriete inexistante). Chargement clean, pas de parse error.
- 2026-09-06 — ETAPE (c) du portage C++ CLOSE, deja wiree dans (a) : la teinte des cubes propres passe par le mapping face->cellule que le C++ rend (PackedInt32Array `cellules` parallele aux transforms). `_integrer_cpp` (rendu_terrain_multimesh.gd) pousse une paire (item, cellule, offset+k) dans le bucket teinte pour chaque face dont la cellule est dans `_cache_profil_cellule`. Le cache est peuple par la boucle GDScript de `_phase_parser` quel que soit le flag -- gardee volontairement a (a) pour rendre (c) possible sans code neuf. i_instance = position dans par_forme[item] apres append = index MMi (car `_bake_mm_forme` itere par_forme[item] in-order). Mini-cubes intacts (couleur PV posee au bake par le C++), non-cubes non teintables (jamais dans _cache_profil_cellule). Validation visuelle (Yael) : flag ON et flag OFF donnent le meme rendu teinte -- variation faible mais identique dans les deux modes. La faible amplitude visible est un choix de design existant (TEINTE_GAIN_VIDE = 0.2, formule lineaire gain = 0.2 + 0.8 * q/cap), pas un bug du portage.
- 2026-09-06 — FIX du clignotement au rebuild de tuile apres modification carte (tir, casse). `_absorber_modifications_carte` faisait `_supprimer_tuile` PUIS `_creer_tuile` -- l'ancien rendu etait detruit tout de suite, le neuf arrivait 1+ frames plus tard quand `utilise_pipeline_intra_tuile` etait vrai (le pipeline etalait le bake sur 3 frames), laissant un trou visible. Nouveau flow : snapshot de l'ancien contenu et de l'ancien partiel pipeline, detache de _tuiles / _tuiles_en_pipeline, bake SYNCHRONE des 3 phases (hors pipeline etale, meme quand le flag est vrai -- le pipeline ne sert qu'aux entrees/sorties disque, un rebuild ponctuel coute < 5 ms), `_phase_baker_occluder` ecrit `_tuiles[t]` = neuf, puis liberation des snapshots ancien via 2 helpers extraits (`_liberer_contenu_tuile` + `_liberer_contenu_pipeline`). `_supprimer_tuile` refactore pour deleguer aux memes helpers (comportement identique, plus court). Invalidation de `_teinte_precedente` sur les cellules du nouvel index teinte pour que `_tick_teinte` reposela couleur sur les nouveaux MMi (sinon cache "gain identique" skip et le mini-cube neuf reste blanc). Meme frame : le neuf est en scene avant que l'ancien soit `queue_free` -- zero trou, difference visuelle 1 cellule negligeable, pas de fuite de RID ni de nœud. Test visuel (au tir) = a faire par Yael.
- 2026-09-06 — ETAPE (b) du portage C++ de `_phase_parser` : MINI-CUBES et NON-CUBES ajoutes a `MesheurTuile::bake_tuile_a`. Portage face-par-face identique -- mini-cube (cote/3) par bit du masque sous-cube, couleur blanc->noir selon pv/MAX_PV_SOUS_CUBE ; non-cube (rampe, cylindre, sphere) = base_orthogonale x mesh_transform, une instance par cellule. Contrat de blob etendu cote GDScript (`_blob_tuile_a`) : `pv_par_cellule` (quintuples x,y,z,sous_idx,pv, remplace l'ancien triple `cellules_pv`), `bases_orthogonales` (PackedFloat32Array 24x9 precalculee via `_regle.get_basis_with_orthogonal_index`), `mesh_transforms` (Dictionary item->Transform3D pour les items NON-cubiques via `mesh_library.get_item_mesh_transform`), `max_pv_sous_cube`. Retour C++ : `par_forme_mini` (Dict {item -> Array<Dictionary{transform, couleur}>}, meme format que `_ajouter_mini_cubes` GDScript, injection directe dans `par_forme_mini` du bucket) ; non-cubes fusionnes dans `par_forme` (memes items ids que le chemin GDScript). Cote GDScript, les branches `_ajouter_mini_cubes` et non-cube (`else:` de _phase_parser) sont GATEES derriere `utilise_cpp_phase0` -- flag OFF garde le chemin historique complet pour comparaison visuelle et rollback safe (option C tranchee par Yael, l'etape (d) retirera les chemins morts apres validation). Recompilation clean, `verification.tscn` charge sans erreur flag off. Test visuel flag on = a faire par Yael (basculer le @export sur le noeud de rendu de la scene testee, notamment casser un bloc et voir les mini-cubes, regarder une rampe).
- 2026-09-06 — ETAPE (a) du portage C++ de `_phase_parser` (rendu_terrain_multimesh.gd). `MesheurTuile::bake_tuile_a(entree)` (extension_terrain/src/mesheur_tuile.{h,cpp}) prend en charge les faces cubiques PROPRES d'une tuile (sous_cubes plein, aucun PV), algo face-par-face IDENTIQUE au GDScript (visible_bits_col, _voisin_couvre reduit a un test de bit sur les couvrants, hauteur par item, Y-scale des faces laterales, dessus abaissee pour un item plus court). Contrat de blob assemble UNE fois par tuile (frontiere franchie une seule fois) : masques + couvrants 12x12 (tuile + 4 anneaux), particularites/sous_cubes_partiels/cellules_pv linearises en PackedInt32Array restreints a la tuile, tables item->cubique / item->hauteur, centre_offset (`_regle.map_to_local(Vector3i(0,0,0))`). Sortie : Dict { par_forme, par_forme_sol } avec, par item, { transforms: Array of Transform3D, cellules: PackedInt32Array parallele }. Le C++ monte les entrees en std::unordered_map/set a l'entree, calcule sur std::vector<Transform3D>, convertit UNE fois a la fin (patron godot_voxel, pas de SurfaceTool). Gate cote GDScript par `@export utilise_cpp_phase0` (defaut FAUX -- chemin historique intact) : les 6 `_ajouter_face` du bloc "cube propre" sont sautes quand le flag est vrai, la teinte est repoussee apres la boucle dans `_appliquer_cpp_a` a partir du mapping face->cellule que le C++ rend et de `_cache_profil_cellule` (peuple par la boucle quel que soit le flag). Mini-cubes, non-cubes, occluder cells, cache profil restent en GDScript pour cette etape. Recompilation clean (`python -m SCons platform=windows arch=x86_64 target=template_debug`), .dll charge, `bonjour()` OK, verification.tscn charge sans erreur (flag off = comportement identique). Test visuel flag on : a faire par Yael a l'ecran (basculer `utilise_cpp_phase0` sur le noeud de rendu de la scene testee).
- 2026-09-04 — `carte_terrain.masque()` sans appel de fonction interne : `dans_emprise` inlinee (test direct des bornes) et `masque_de_base` remplacee par le cache `_masque_base` (recompute uniquement quand `couches_pleines` change, via son setter -- robuste au chargement de ressource, contre le piege @tool + _init). `dans_emprise()` et `masque_de_base()` restent publiques.
- 2026-09-04 — HOT PATH BANC PEUPLEMENT APLATI. `banc_peuplement._physics_process` appelle `Mouvement.pas_simple` et `Peuplement.ecrire_transform_index` en direct — plus de `Tick.tick_entite`, plus de `Callable.call`, plus de `match profil` String, plus de lookup `id_to_index` par String. `Mouvement._pas_simple`/`_pas_minimal` deviennent publiques (`pas_simple`/`pas_minimal`) — `pas()` avec match reste pour les appelants qui passent un profil String. `Peuplement.ecrire_transform_index(pool, index)` sauté le hash. `carte_terrain.sommet` et `sommet_sous` cachent `masque(col)` une fois en tête et lisent `_masques_sous_cube` en direct (plus de rappel de `masque` / `est_pleine` / `sous_cubes` en boucle). Comportement du banc identique.
- 59c2fb0 — RENDU PROTO : hystérésis de rayon (`rayon_rendu_entrer`/`_sortir`, idem balles) dans les deux managers — le nœud s'instancie SOUS le rayon d'entrée, se libère AU-DELÀ du rayon de sortie ; l'écart tue le flip-flop au bord. + ENNEMIS DE TEST STATIQUES : `ennemis.ajouter_statique(pos)` + capture des marqueurs de scène « ennemi_test_statique » (`manager_proto._capturer_ennemis_statiques_test`) — immobiles (gravité + snap sol via Tick+Mouvement profil « simple », zéro IA) pour éprouver le pipeline rendu/streaming/hystérésis sans que l'IA les bouge. Producteurs et cubes : vertical exclusivement via `Mouvement.pas`.
- 8504a51 / 8c2b9c5 / 7d90e8e — MOUVEMENT KINEMATIC PARTAGÉ : `scripts/mouvement_kinematic.gd` + `scripts/tick.gd` factorisent gravité, snap au sol, blocage terrain/pente et plafond voxel (fin du saut à travers un bloc) en UN pas partagé (profil « simple »). Joueur, cubes ennemis et producteurs délèguent leur vertical au framework au lieu de le recalculer chacun. Écrit dans `scripts/` (cœur) au su de Yael — exception frontière (voir `CLAUDE.md` § Frontière).
- 2026-09-02 — EXCEPTION JOUEUR RETIRÉE : `collision.gd` est désormais l'autorité de déplacement du joueur. `jeu/unites/personnage.gd` passe de `CharacterBody3D` à `Node3D` — plus de `move_and_slide`, `velocity` ni `is_on_floor`. Le joueur est une ENTITÉ DATA du Monde comme les cubes : sa position est mue chaque frame (60 Hz) par `manager_proto_2._pas_joueur` en donnée pure — intention lue par APPEL DIRECT au Personnage (`intention_mouvement(delta)`, pas de course d'ordre entre `_physics_process`), gravité+saut data, TERRAIN par `_deplacement_horizontal_valide` (factorisé cubes+joueur, `carte.sommet` + blocage pente), et collision joueur↔cubes par `collision.gd` (GJK/EPA) à 60 Hz (à re-mesurer si N grand). Le manager est SEUL écrivain de la position (recopiée dans le nœud en fin de `_process`) ; le Personnage ne pilote que lacet/tangage/HUD. Dimensions et vitesses en donnée (capsule r0.4 h1.8, yeux 1.7, marche 4, saut 8.5, gravité 18), injectées au nœud par `configurer_depuis_donnees`. `personnage.tscn` : `Node3D`, `Hitbox` retiré, `Corps`→`corps_visible`, `marqueur_debug` (cube rouge à l'origine = position data exacte). Bug latent fermé : la forme capsule data porte `transform_locale` +hauteur/2 (origine aux pieds), sinon elle cognait les cubes par le sol. Fichiers dans `jeu/` seulement, aucun script du cœur touché. Vérifié : `test_personnage` VERT (nouveau contrat Node3D/1.8 m/marqueur à l'origine), `verification.tscn` compile et tourne, trace `joueur pos/vel/au_sol` 1/s prouve la position data ; rendu à vérifier à l'écran. Rouges préexistants inchangés (`test_carte`, `test_collision_terrain`, `test_scene_carte` — famille emprise 64/150 ; `test_carte_prototype` hang headless). DETTE : `jeu/CARNET_DE_JEU.md` §3.5 (capsule « 1 m », CharacterBody3D) et le commentaire `arme_tir.gd:8` (« parent = CharacterBody3D ») sont à actualiser ; l'orientation du joueur reste locale (capsule Y-symétrique) — à revoir si le joueur porte une forme orientée.
- 2c39564 — COLLISION GÉNÉRALISTE EN DONNÉE PURE : `jeu/Proto/collision.gd` (RefCounted, statique, aucune physique Godot) — fonction de SUPPORT unifiée (sphere/boite/capsule/hull, dispatch au seul endroit), GJK, EPA, `tick` (broadphase via `monde.choses_dans_rayon` + swept), `resoudre` (séparation le long de la normale). À réutiliser pour tout objet interactif ; spec complète dans `jeu/Proto/COLLISION.md`. Câblée dans `manager_proto_2` (cubes = boîte, joueur = capsule), et sert d'autorité de collision joueur↔cubes (voir entrée « exception joueur retirée » ci-dessus).
- 2026-09-02 — EXTRACTION DE `manager_proto.gd` EN DEUX MODULES RefCounted : `jeu/Proto/tas.gd` (matière granulaire — spawn, empilement, sandpile, index de colonne, meshes) et `jeu/Proto/ennemis.gd` (spawn, IA chasse/creuse/ramasse/pose, faim, repousse, corps-à-corps, barres). Déplacement pur, aucun comportement changé ; le manager garde le rendu, le dégât de contact, l'effondrement et l'HUD, et itère `_ennemis.ennemis()`. Le passage RefCounted (pas de `get_tree()`) impose des injections : Callables `fournisseur_limite`/`effondrer`/`percepteur_position`, API tas élargie (`monde`/`sommet_effectif`/`spawn_sous_cube_libre`/`index_ajouter`/`index_retirer`/`mesh_pour_matiere`/`ajouter_collision_cube_libre` publics), compteur d'id des outils (pelle/bêche) repris au manager. `_pas_permis` et `_ennemi_a_obstacle_devant` étaient morts — le premier supprimé, le second déplacé mais toujours inutilisé. Contenu uniquement dans `jeu/Proto/`, aucun script du cœur touché. Vérifié : compile + scène principale 240 frames headless, zéro erreur/warning ; comportements à valider à l'écran.
- 2026-09-02 — PROTOTYPE ENNEMI (`manager_proto.gd`) VALIDÉ PUIS ARRÊTÉ. Les ennemis fonctionnent correctement en jeu : déplacement en data pur, chute/gravité, répulsion inter-ennemis, et creusage du terrain (coups sur les sous-cubes via `carte_terrain`). Le prototype d'IA ennemie du `manager_proto` s'arrête ici — la suite part sur le type d'IA prévu (classifieur ancré LLM, voir `CLAUDE.md` § Connexion LLM). Aucun nouveau développement d'IA ennemie dans `manager_proto`.
- 352f8a0 — `couvert.gd` apparition sans gaspillage : la pose initiale des plantes n'a lieu que SANS observateur. Avant, `_ready` posait les milliers de plantes dans les lots MMI puis cherchait l'observateur à la ligne suivante, et `_bascule_rendu` radiait à la frame d'après tout ce qui était hors rayon — deux autorités de rendu qui se contredisent. Mesuré sur `verification.tscn` : 1000 lignes / 619 MultiMeshInstance3D posées au chargement, 100 % radiées ; après correctif, `_ready` pose 0 ligne / 0 lot, état stable identique. Observateur trouvé AVANT la pose ; graines toujours posées (non streamées) ; `_liberer_les_semis_disparus` lit la survie dans `_etat.plantes` (plus dans `_poses`, désormais conditionnel) ; `get_tree()` null-safe (`_ready` détaché des tests ne plante plus) ; fonction morte `_plante_par_id` retirée. Verts : test_plante, test_couvert_carte, test_cache_vivantes, test_arbre_massif.
- 9888199 — RENDU DU TERRAIN LOINTAIN REFONDU en MultiMesh PAR FORME + FACE CULLING. `jeu/terrain/rendu_terrain_multimesh.gd` remplace le rendu par tuiles de GridMap (`Proto/terrain_streame.gd`, debranche de `verification.tscn`) : un MultiMeshInstance3D par forme × par tuile ; chaque cube (`BoxMesh`) pose UN QUAD par face EXPOSEE au lieu d'un cube plein de 12 triangles — une face n'est cachee que par un CUBE plein (une rampe/cylindre/sphere, qui ne remplit pas sa cellule, laisse la face visible). Position/orientation par les conversions natives d'un GridMap de reference (`map_to_local`, `get_basis_with_orthogonal_index`, `get_item_mesh_transform`) — jamais calculees a la main. Formes lues dans `bloc.tres` (ajouter une forme = un item, zero ligne ici) ; rampe/cylindre/sphere gardent leur mesh complet. `custom_aabb` serre a la hauteur reelle de la tuile (frustum culling — un AABB haut de toutes les couches ne se cullait jamais). Occlusion culling activee (`project.godot`) : `ArrayOccluder3D` sur le relief emergent (couche > sommet de base) dans le terrain, et sur les canopees d'arbres dans `couvert.gd` (reconstruit seulement quand la population rendue change). LOD par distance (`visibility_range_end`) et taille de tuile 10 (culling plus fin), les deux reglables sur le noeud. Mesure : primitives dessinees 181254 → 41514 (face culling), 82 draw calls. `terrain_visible.gd` (GridMap proche, rayon 15) ne DESSINE plus (`sans_mesh`) — il ne porte QUE la collision qui suit le joueur.
- f9b1e2c — `couvert.gd:_bascule_rendu` throttle : le scan `choses_dans_rayon` ne se relance qu'au tick (`joues > 0`) ou quand l'observateur s'ecarte de `seuil_rescan_metres` (4 m), plus a chaque frame. Meme defaut deja evite ailleurs (tick sorti de la frequence d'images).
- 6273e08 — `bloc.tres` : formes cylindre (id 7) et sphere (id 8), vraies primitives (`CylinderMesh`/`SphereMesh`) avec leurs `CollisionShape`. Doc `jeu/terrain/AJOUTER_UNE_FORME.md` : recette et pieges pour ajouter une forme sans casser l'existant.
- 2026-08-25 — INSPECTEUR RESSOURCES sur terrain dynamique : `manager_proto.gd` expose `quantite_a`/`preleve` (public, O(1), défaut `capacite_case`) et rejoint le groupe `ressources_terrain` ; `InspecteurBloc` + `StockJoueur` + `BarreRessource` ajoutés à `verification.tscn`. La réserve par cellule vit en donnée (défaut + Dict des écarts), sans le scan GridMap statique de `ressources_terrain.gd`. Aucun surcoût par frame : lecture Dict O(1), crédit au clic seul.
- 2026-08-25 — CEINTURE DE CARTE : GridMap de cellules → StaticBody3D + 4 BoxShape3D, sur les trois scènes qui la portaient : `Proto/verification.tscn`, `Proto/proto_terrain.tscn`, `terrain/carte_prototype.tscn`. `jeu/terrain/murs_limite_boite.gd` (`@tool`, quatre boîtes déduites de l'emprise par `faces()` : faces internes à ±`demi_cote·cote`, épaisseur 10 m poussée VERS L'EXTÉRIEUR — la barrière ne bouge pas, seule la face externe recule —, hauteur 22 couches = 44 m). `murs_limite.gd` RETIRÉ DÉFINITIVEMENT (plus aucune référence code). Mesuré sur verification (demi_cote 250) : mémoire de la ceinture 15,7 → 0,68 Mo (~23×), 44088 formes de collision → 4. Verrouillé par `test_ceinture_infranchissable.gd` (barrière 4 côtés, tunneling projectile 100 m/s CCD, jitter, glissement), paramétrable `-- scene=` ; couvre trois bugs Godot loin de l'origine, dont AUCUN ne se manifeste (variance 0, glissement libre) ni sur GridMap avant ni sur boîtes après : #75537 (jitter CharacterBody vs grande StaticBody à ±500 m), #39095 (tunneling corps multi-formes), #69683 (snapping). DETTE : `test_carte_prototype.gd:113-177` vérifie encore la ceinture EN GridMap (early-return si pas un GridMap) → obsolète depuis ce refactor, à réécrire en contrôle fonctionnel.
- 2026-08-25 — RENAME `TerrainLointain` → `TerrainStreame`, fichier `terrain_visible_multimesh.gd` → `terrain_streame.gd` : le nom disait « horizon » alors que le système rend le sol streamé AUTOUR du joueur (rayon 120 = 240 m, sans collision). Nœud dans `verification.tscn` et `proto_terrain.tscn`, fichier + uid, preload de `test_ecosysteme.gd`, commentaires de `terrain_visible.gd`, refs docs `PROTOCOLE_MULTIMESH.md` et `METHODE_DIAGNOSTIC_PERF.md`.
- c571a3d — compte de voisins passé en SIGNAL EN ÉCRITURE (incrémental), pas seulement en lecture. Avant : `rafraichir_plante` rescannait `voisinage()` → `choses_dans_rayon()` à chaque foyer (naissance, mort, ET changement de stade). Après : `rafraichir_plante` ne fait plus que l'ombre ; le compte est maintenu par `maj_voisins_naissances` (+1 par naissance sur les voisins, le nouveau-né pose son compte par un scan) et `_maj_voisins_incremental` (−1 par mort), appelés une fois par tick à §8 ; `poser_voisins_initial` scanne une fois à `etat_initial` ; `retirer` fait le −1. Changement de stade = zéro écriture du compteur. `couvert.gd` Peuplement appelle `maj_voisins_naissances` (source unique). Mesuré : requêtes `choses_dans_rayon`/tick 185 → 10, candidats mesurés 13247 → 659, temps tick 28 → 24 ms. Verrouillé par `test_compteur_voisins.gd` (compteur == scan de contrôle après chaque naissance/mort/stade/retirer/séquence longue). Commentaire `vegetation.gd:445` corrigé — il prétendait faussement « AUCUNE requête », il disait signal en lecture mais scan en écriture ; il dit maintenant le vrai.
- c571a3d — cache des vivantes : `etat.plantes` devient la liste toujours-propre (purge immédiate des mortes en §2b via `assign` en place, rejets intégrés après la boucle §7, plus de purge paresseuse à §8). `encore` alias `plantes` directement — L.666 passe de scan O(N) à O(1). §7 refactoré : les rejets s'accumulent dans `nouveaux`, intégrés après la boucle (sinon ils seraient itérés/reproduits le même tick). Verrouillé par `test_cache_vivantes.gd` (aucune morte dans `etat.plantes` après chaque opération). Gain sur `vivantes` : 11.7% → 2.3% au profileur ; sur le temps de tick, dans le bruit (les scans `vivantes` n'étaient pas le goulot — c'était `choses_dans_rayon`, voir entrée du dessus).
- c571a3d — PIÈGE : un commentaire de code peut mentir sur le coût réel. `vegetation.gd:445` affirmait le compte de voisins « porté en signal, AUCUNE requête », alors que la LECTURE seule l'était — l'ÉCRITURE était un scan. La règle « signal, pas question » de la doctrine ne s'appliquait qu'à la moitié. Leçon : tracer l'écriture, pas croire le commentaire. Symptôme : `choses_dans_rayon` à 34% du tick au profileur. Cause : `voisinage().size()` recalculé à chaque foyer. Corrigé (entrée du dessus).
- c571a3d — arbre du couvert monté à 6 stades (enfant/ado/jeune/adulte/vieux/pourri) via `espece.gd` ouvert de 3 à 6 stades optionnels ; silhouette procédurale `maillage_arbre` (CylinderMesh tronc + cône canopée) ; collision proportionnelle au stade (rayon = rayon_collision × stature/stature_max) ; `utilise_ombre` @export défaut false (court-circuite le calcul d'ombre pour une espèce isolée) ; noeud `Peuplement` (`peuplement.gd`) qui pose N plantes déjà à un stade cible, étalé par `budget_par_frame` avec aléa d'apparition, câblé dans `couvert.gd:_consommer_file_peuplement`. Posé dans `jeu/Proto/verification.tscn` sous `Terrain/Couvert`.
- c571a3d — troncs du couvert migrés de StaticBody3D vers PhysicsServer3D pur (RID, aucun nœud d'arbre de scène). `couvert.gd` : shape RID par stade fabriquée au `_ready` et partagée par tous les corps de cette espèce, body RID créé par `_poser_corps` (mode STATIC, layer/mask 1 explicites, `body_set_space(get_world_3d().space)`), free_rid immédiat par `_liberer_corps`, purge finale au `NOTIFICATION_PREDELETE`. Suppression : `_corps_solide`, `_poser_tronc`, `_liberer_tronc`, `_porteur`, `_liberer_porteur`, var `_noeuds`, var `_troncs_actifs`, const `NOM_TRONC`. Gain mesuré : ~3 ms/frame vs ~16 ms/frame pour 2827 corps actifs (banc `banc_collision_serveur.gd`, isolation --sb / --ps). Verrouillé par `banc_migration_tronc.gd` : `body_get_space` rend un RID égal à `world_3d.space` (7a), `intersect_ray` touche le tronc (7b), `body_get_space` après `free_rid` rend un RID invalide (7d). 7c (Frappe.subir_frappe) non applicable — les troncs n'ont ni vie ni méthode subir_frappe, la destruction externe passe par `vegetation.gd:retirer(id)` sur un id data. Commentaire dans le code pour le futur : ajouter `body_attach_object_instance_id` si un raycast doit un jour identifier l'arbre touché.
- c571a3d — méthodologie du chantier collision, à recopier pour tout futur chantier de perf : (1) mesurer avant de trancher entre plusieurs options — banc synthétique dédié avant compromis gameplay ; (2) isoler les mesures dans des runs séparés (arguments CLI --sb/--ps) pour distinguer warm-up moteur vs interférence entre approches ; (3) instrumenter le compte réel d'objets (`_racine.get_child_count()`, `bodies_rid.size()`) plutôt que le compteur générique `PHYSICS_3D_ACTIVE_OBJECTS` qui vaut 0 pour bodies statiques ; (4) copier la signature exacte de chaque méthode PhysicsServer3D depuis la doc officielle avant d'écrire la ligne, jamais de mémoire ; (5) pairer strictement `body_create` → `free_rid` et `shape_create` → `free_rid` au `NOTIFICATION_PREDELETE`, un RID orphelin fuit invisiblement ; (6) `body_set_space(get_world_3d().space)` obligatoire immédiatement après `body_create`, sans quoi le corps existe mais n'est dans aucune simulation, silencieusement.
- c571a3d — culling collision et rendu MMI par distance à l'observateur dans `couvert.gd` : hors du rayon `rayon_collision_metres` (60 m défaut), la ligne MMI est radiée et le corps physique libéré. La donnée continue de vivre dans `vegetation.gd`. Utilise `monde.choses_dans_rayon` (indexé par case, coût suit le rayon pas la population). Sortie du rayon = `_radier` + `_liberer_corps`. Rendu MMI et collision cyclés dans `_bascule_rendu` par tick.
- c571a3d — collision proportionnelle au stade dans `couvert.gd:_preparer_les_rendus` : rayon effectif du cylindre = `rayon_collision × stature_stade / stature_max_espece`. Adulte (stature max) garde `rayon_collision`, enfant à stature 2 m descend à ~0.21 m au lieu de 1.5 m. Élimine l'aberration "arbrisseau qui bloque 3 m de diamètre".
- c571a3d — silhouette arbre stylisée procédurale dans `couvert.gd:maillage_arbre` : `CylinderMesh` builtin Godot pour tronc + cône (top_radius=0) pour canopée, deux surfaces séparées dans un ArrayMesh, deux matériaux (marron `Color(0.35,0.22,0.13)` + couleur de l'espèce). Auto-sélection dans `_preparer_les_rendus` : `rayon_collision > 0` → arbre stylisé, sinon touffe. Aucun @export ajouté sur `espece.gd`, gate arithmétique.
- c571a3d — `espece.gd` ouvert de 3 à 6 stades (stades 4-6 optionnels, ignorés si nom vide OU durée nulle) pour permettre à l'espèce arbre d'exposer les 6 étapes de Yael (enfant/ado/jeune/adulte/vieux/pourri, statures 2/5/9/14/10/7). `champs()` filtre les stades vides pour rester générique. Tests existants intacts : `test_plante.gd`, `test_couvert_carte.gd` continuent avec 3 stades sans changement.
- 2026-08-24 — hook TSCN INTERDIT (bloc F de `on-pre-tool.sh`) retiré sur ordre de Yael. Checks 2 et 3 de `on-stop.sh` (completion sans bash / livraison 3 blocs) aussi retirés. Backups datés dans `~/.claude/hooks/premier-rts/*.bak-20260824-*`. Mémoires correspondantes nettoyées.
- 2026-08-23 — audit `Proto/terrain_visible_multimesh.gd` : masque de visibilité par colonne cale `r_start` sur `r_top` voisin (rang du bit le plus haut), pas sur la contiguïté du masque → faces latérales absentes dès qu'un voisin porte grotte/pont/surplomb, contredit `terrain/carte_terrain.gd:34-38` ; audit seul, aucun correctif appliqué.
- 2026-08-23 — pattern CHUNKS/TUILES pour MultiMesh grande zone : Godot 4 n'a pas de frustum culling par instance (godot-proposals#10669), un MultiMesh unique sur 300 k+ instances vertex-shade tout à chaque frame (mesuré : fps 8 en V1 naïve). Solution éprouvée (plugin Spatial Gardener) = découper l'espace en chunks carrés, un MultiMeshInstance3D par chunk avec `custom_aabb` serré à sa boîte → Godot cull chaque chunk indépendamment via frustum. Recette dans `jeu/PROTOCOLE_MULTIMESH.md`, implémentation dans `jeu/Proto/terrain_visible_multimesh.gd`.
- 2026-08-23 — frise en marche réglée : terrain lointain repasse d'un GridMap dynamique (retarget = reconstruction d'octants, pics 80–115 ms mesurés au profileur) à un `terrain_visible_multimesh.gd` (chunks MultiMeshInstance3D, un mesh unifié par chunk, couleur par instance via `use_colors` + `vertex_color_use_as_albedo`, `custom_aabb` serré au chunk). Balayage taille_chunk mesuré : draw_calls chutent 791→115 sans gain fps → confirme que le volume géométrique est le vrai mur, pas les draw calls. À l'écran, la frise est absente.
- 2026-08-23 — rayon rendu entités découplé de rayon terrain visuel : `manager_proto.rayon_rendu=60` (skins entités), `TerrainLointain.rayon_cellules=120` (240 m sol visible), `Terrain.rayon_cellules=15` (30 m sol physique). Trois rayons indépendants pour trois fonctions : voir loin, marcher, interagir.
- 2026-08-23 — terrain split visuel/physique via deux GridMap frères : `Terrain` (biblio avec collision, rayon 15) + `TerrainLointain` (biblio dépouillée via `set_item_shapes([])`, rayon 120, `rayon_interne` aligné dynamiquement sur Terrain par NodePath). Boot 14,4 s → 1,9 s mesuré (316k corps physiques → 5k). Écart framework documenté dans `jeu/terrain/terrain_visible.gd` (second écart après `scripts/monde.gd:retirer`).
- 2026-08-23 — balles simulées en donnée dans `manager_proto` : `_balles: Array`, `spawn_balle(pos, dir)` appelée depuis `arme_tir.gd`, `_tick_balles` avance et teste collision segment (anti-tunneling) contre carrés en data, purge cible touchée. Peau visuelle `balle_violette.tscn` avec `set_script(null)` pilotée par le manager, cyclée dans rayon_rendu. Aucune logique dépend du rendu.
- 2026-08-23 — collision inter-carrés en donnée via spatial hash (bucket cellule `RAYON_REPOUSSE=0.6 m`) : paires internes + voisins droite/bas sans double-comptage, push-apart horizontal seul (gravité gère Y). Scale O(N·k) tenable N=1000. Ne s'applique pas aux carrés physique-actifs (le RigidBody gère). Particules d'impact code-only (fade+shrink 0,35 s) au hit, exclusion tireur↔balle sur 1 m depuis point de départ.
- 2026-08-23 — chantiers de design tranchés notés dans `jeu/Proto/A_PREVOIR.md` : rayon englobant par entité, buckets par ordre de grandeur pour filtre visibilité, cycle skin différencié petit/géant, cas géant animé évité, décor statique intégré au système sans exception, LOD terrain, cache directionnel absent dans retarget, gestion pop-in téléport.
- 2026-08-23 — patron entités split donnée/rendu documenté dans `jeu/Proto/PATRON_ENTITES.md` : recette pas-à-pas pour ajouter futurs ennemis/ressources sans dupliquer l'infrastructure. Helpers réutilisables (`_gerer_freeze_kinematic`, `_snap_sol_via_carte`, `_sol_present_sous`) + patrons de bascule rendu (data mobile via velocity, data statique). Invariants listés (KINEMATIC obligatoire, signal detruit, pathfinding consulte carte)
- 2026-08-23 — producteur pilote par velocity + freeze conditionnel (patron scalable N=1000) : téléport par frame remplacé par `linear_velocity` en zone safe, `noeud.global_position = data.position` uniquement en zone buffer (freeze KINEMATIC). Évite 60k `set_global_position`/s à l'échelle
- 2026-08-23 — pattern extensible split donnée/rendu dans `manager_proto.gd` : helper `_gerer_freeze_kinematic(rb, data, en_zone_safe)` réutilisable pour toute entité physique. Trois zones (hors safe / safe+sol / safe+timeout+snap). Applicable aux futurs ennemis mobiles avec sync données→nœud chaque frame (patron producteur déjà en place) + `carte_terrain.est_pleine(colonne, couche)` consultée pour éviter pathfinding dans les murs
- 2026-08-23 — split donnée/rendu du terrain (cubes verts sculptés dans `proto_carte.tres`) : vérifié fonctionnel via framework `terrain_visible` + `carte_terrain`. Item `bloc_vert` (id 6) stocké dans `particularites` de la carte, restitué à l'identique après cycle éloignement/retour du joueur. Sculpture persistante entre sessions, aucune régression, aucune intervention du proto nécessaire
- 2026-08-22 — géniteur v2 : copie du géniteur SANS gestation mother cube (`geniteur_v2.gd/.tscn`) ; banc `test_ennemi2 Mother box` bascule sur v2 ; v1 intact sur disque (piste mother cube en stock)
- 2026-08-22 — stocks du géniteur renommés : `perso → privee` (barre violette), `accessible → public` (barre bleue) ; API `stock_public_courant()` + `preleve_stock_public()` ajoutée
- 2026-08-22 — gestation des générateurs passe sur le stock PUBLIC : `gestation_generateur_public.gd` (seuil 15 = 10 % public, coût 15, max 8 vivants, durée 60 s) ; l'ancien gestation_energie reste inutilisé mais sur disque
- 2026-08-22 — stockeur ajouté : `stockeur.gd/.tscn` cube orange 1 m, vie 3, 20 min de vie, capacité 100 nourritures ; `gestation_stockeur.gd` sur stock PRIVÉ (seuil 30 = 20 %, coût 30, max 4, durée 60 s) ; chasse carrés rouges dans 30 m, patrouille vers géniteur au-delà de 50 m, suit à 10 m quand plein
- 2026-08-22 — cadavre de générateur PÉRISSABLE : Timer 300 s (5 min) au `_mourir()` déclenche `_decomposer()` (retrait monde + queue_free), fenêtre courte de cannibalisation
- 2026-08-22 — carré rouge PÉRISSABLE : Timer 600 s (10 min) au `_ready()` déclenche `_mourir()`, empêche accumulation infinie
- 2026-08-22 — fix boucle silencieuse « générateur mange cadavre plein » : dans `_faire_colle`, source invalide + `_cout_paye_pour_ce_cycle = true` → transition ETAT_POND (ponte anticipée qui reset matiere_cumulee) au lieu d'ETAT_ATTENTE ; sans ce shortcut, prochain cycle demande reste = 0 → pris = 0 → boucle géniteur↔ATTENTE
- 2026-08-22 — exception collision géniteur↔générateur POSÉE AU `_ready()` du générateur (pas seulement au `_mourir()` cadavre) ; sans elle, RigidBody masse 5 pousse RigidBody masse 30 non-freeze au contact. Même patron dans `stockeur.gd:_ready` avec groupes `geniteur/generateur_energie/ressource/stockeur/protogeniteur`
- 2026-08-22 — accélérateur `controle_vitesse.gd` : touches `+` / `-` (×2 / ÷2), `0` reset, plafond ×10, plancher ×0.25, OSD Label jaune haut-gauche
- 2026-08-22 — protogéniteur ÉCRIT mais NON BRANCHÉ : `protogeniteur.gd/.tscn` (cube bleu 3×3×3, auto-produit 2/s de stock_public, suit géniteur à 60 m) + `gestation_protogeniteur.gd` (seuil 120 = 80 % privé, coût 120, max 3, durée 240 s = 4 min) ; le nœud n'est pas dans `geniteur_v2.tscn`, à cabler quand la boucle vers guerrier sera fermée
- d09a008 — écosystème ennemi, bugs latents fermés : `monde.deplacer` appelé par toute entité mouvante (géniteur, mother cube, carré rouge), `MondePartage` posé au banc `test_ennemi2 Mother box` (sans lui perception morte), collision géniteur↔cadavre exclue, perception mother cube et générateur throttlées à 2/s, barre de vie mother cube compensée du scale, propriété `rayon` publiée sur l'entité
- 8818c58 — cadavre-mange : le générateur enrolé perçoit la source la plus proche parmi géniteur et cadavres (filtre `stock_puisable > 0`), cumule matière sur plusieurs sources jusqu'à `cout_prelevement`
- 8d69362 — mother cube perception + mange + croissance : vue 30 m sur `nourriture > 0`, une frappe par seconde au contact, scale lerp base → 70 m (gain 5 % décroissant avec la taille), vitesse lerp 10 → 2,5 m/s ; géniteur devient double-stock (`_stock_perso` 150 + `_stock_accessible` 150, extraction 50/50, barre violette visible)
- a3b3337 — banc `test_ennemi2 Mother box` : générateur enrolé marche vers le géniteur, prélève 10 stock au contact, pond un carré rouge derrière lui toutes les 60 s ; premier morceau de la mother cube (3 vies, inscrit au monde)
- 1215c83 — générateur d'énergie à deux phases : 3 vies vivant → cadavre 7 vies (mesh terni, freeze, groupe `ressource`, inscrit au monde avec `stock_puisable = stock_cadavre_initial`) → `queue_free` à 0
- b44cfb4 — figeage géniteur fermé : `_choisir_cible` filtre distance ≥ 4 m et écart vertical ≤ 5 m, exclut l'emprise 3×3 (la case sur laquelle il vient d'arriver n'est plus une cible valide)
- c797e78 — générateur d'énergie RigidBody3D amorti (masse 5, damping 2/2, `lock_rotation`) ; gestation en couronne libre autour du géniteur (essais sur 8 angles avec décalage random, rayon 6-8 m) → géniteur cherche nouvelle cible en cas d'échec
- 2140f0a — géniteur : extraction bloquée tant qu'il n'est pas au sol (garde `HAUTEUR_MAX_AU_SOL = 3.5 m`) ; banc de gestation générateur d'énergie enfant (seuil 20, gestation 20 s, coût 20, max vivants 4)
- bbab9b6 — BANC TEST_ENNEMI complet : gisement (500 métal, se détruit à zéro),
  transporteurs qui prospectent (perception+saillance, vision 5 m, marche
  aléatoire, cognent, rapportent), cube violet (vie 50, stock 200 max) qui se
  reproduit à 30 métal, transporteur qui cherche un autre cube quand sa mère est
  pleine, mode combat DÉCLENCHÉ à la frappe et PERSISTANT jusqu'à la naissance
  du soldat (aucun timer), signal aux voisins dans 15 m, soldat rouge (20 vie,
  vitesse 3, dégâts 10) territorial (rayon 10 m du cube parent), mémoire de
  cible via `scripts/lien_personnel.gd` du framework (magnitude 1.0, décroissance
  0.4/s, plancher 0.05 → ~3 s de traque après perte de vue), joueur (100 vie,
  barre HUD 2D, reload de scène à zéro). Composant `PresenceDansMonde`
  générique. Framework composé partout, rien inventé sur les mécaniques.
- LOGBOOK.md : le mécanisme qui nommera au joueur ce que le système produit,
  décide les 4 conditions (rare, composé, conséquent, nommable) et écarte deux
  fausses pistes (IA narrateur, journal joueur). Rien d'implémenté encore
- `jeu/Ennemie/` et `jeu/terrain/test_ennemi.tscn` déplacés dans
  `jeu/Outil de jeu/` (regroupement du banc), tous les paths mis à jour dans les
  13 fichiers concernés. Vérifié : la scène charge, ses 14 nœuds sont là
- LE RAFRAICHISSEMENT DU TERRAIN EN JEU S'ETALE SUR PLUSIEURS IMAGES.
  `terrain_visible.gd` posait et effaçait toute la couronne d'un seuil franchi
  en un seul appel synchrone — MESURÉ à 12,5 ms pour 796 colonnes au rayon et
  au pas de jeu (rayon 50, pas 4), soit les trois quarts d'une image à 60
  i/s, à CHAQUE fois que le joueur avance de huit mètres : le freeze régulier
  en marchant. `_retargeter`/`_avancer_file` mettent deux files (`_a_poser`,
  `_a_effacer`) à jour sans jamais les vider ni les recréer — un demi-tour
  avant la fin d'un étalement annule le travail en attente au lieu de le
  refaire — et `_process` n'en draine que `colonnes_par_image` (120, sous
  2 ms) par image. `rafraichir()` reste inchangée, synchrone, verrouillée par
  les tests existants ; c'est elle qui pose le tout premier affichage
- L'ARCHIVISTE MANQUAIT DANS `carte_100km2.tscn` : aucun noeud ne portait
  `archiviste.gd`, donc rien de ce qui était sculpté ne s'écrivait jamais sur
  le disque, quel que soit le temps passé — seul `Ctrl+S` de la scène
  transportait quoi que ce soit, et de façon coûteuse (voir plus bas). Nœud
  ajouté, cablé sur `carte_100km2.tres`
- LA MAQUETTE N'A PLUS DE RELIEF PAR DEFAUT : `relief` (faux par defaut) pose
  chaque echantillon au sommet par defaut de la carte, quelle que soit la
  sculpture reelle. Personne ne marche sur la maquette, elle sert a s'orienter,
  pas a previsualiser un denivele. Avant ce drapeau, un ecart de quelques
  couches sur une carte presque plate s'etirait sur toute la gamme de couleur
  et se peignait en blanc pur, comme un sommet. `etendue_minimale` (plancher
  d'ecart avant d'atteindre la teinte du haut) reste pour le jour ou `relief`
  repasse a vrai
- 4c1116c — LE CURSEUR CHARGE ENCORE APRES REOUVERTURE : `fenetre_chargee`
  redevient exporte ; la protection contre le creusement automatique n'est plus
  le drapeau mais `_colonnes_chargees`, transmis à `enregistrer_ce_qui_est_pose`
  pour n'effacer que ce qu'on sait avoir charge. Corrige l'effet de bord de
  9500437 : tirer le repere ne chargeait plus rien apres reouverture de la
  scene, sans aucune erreur pour le dire
- 2026-08-20 — CARNET DE JEU : `jeu/CARNET_DE_JEU.md`, l'index de ce que le jeu
  sait faire — les quatre couches, les scènes lançables, les cinq domaines, les
  leviers de réglage, ce qui est écrit sans être appelé, et où se prend chaque
  décision
- e1371d7 — CE QUE LA SCÈNE PORTE EST REPRIS AU LANCEMENT. `terrain_visible`
  effaçait le GridMap sans le lire : tout ce qui n'avait pas transité par
  l'éditeur était perdu, et ce transit tenait à quatre conditions dont aucune ne
  se signale quand elle manque. Il PREND, il écrit dans la carte, PUIS il
  efface. Sculpter sans avoir chargé de fenêtre s'enregistre aussi
- 4b09b41 — UNE SEULE COUCHE ÉCRIT LE MONDE : `jeu/monde/registre.gd` porte
  « ai-je changé ? », `archiviste.gd` porte « alors je t'écris ». Ajouter une
  sorte de donnée au monde ne demande aucune ligne d'écriture — voir
  MANUEL_CARTE.md
- 21393bb — LA CARTE EST UN VOLUME : chaque colonne porte le MASQUE de ses
  couches pleines, pas sa hauteur. Grottes, ponts, surplombs et niveaux séparés
  deviennent représentables, ce dont dépend tout le terrain destructible
- 5f41a86 — ENREGISTRER CONSERVE CE QUI EST SCULPTÉ HORS DES COLONNES CHARGÉES.
  La garde de a054506 bloquait toute colonne non chargée, y compris celles que
  la grille PORTE : le bouton creusait avant, il oubliait après. Elle ne
  s'applique plus qu'aux colonnes absentes de la grille. Mode d'emploi et index
  du terrain : `jeu/terrain/MANUEL_CARTE.md`
- a054506 — ENREGISTRER NE CREUSE PLUS CE QUI N'A JAMAIS ÉTÉ CHARGÉ. Le
  chargement retient les colonnes qu'il a réellement traitées ; l'enregistrement
  n'écrit que celles-là. Une colonne absente du GridMap comptait comme VIDE, et
  « absente » avait deux causes que rien ne distinguait — creusée par le
  sculpteur, ou jamais chargée. Voir PIÈGES
- e39ef42 — CARTE DE 100 km² : `carte_terrain.gd` porte l'emprise et le sommet
  de chaque colonne, sans nœud ni GridMap ; vierge elle pèse 185 octets pour
  25 millions de colonnes, contre 16,5 octets par cellule pour un GridMap plein.
  `terrain_visible.gd` ne rend qu'un disque autour de l'observateur — traverser
  9,6 km laisse 19 747 cellules, du premier au dernier pas. `outil_fenetre.gd`
  sculpte par fenêtre de 600 m, posée LÀ OÙ ELLE EST sur la carte, et déplacer
  le curseur enregistre la zone quittée avant de rendre la suivante ;
  `maquette.gd` montre les 100 km² d'un coup en ne relisant que ce qui est
  sculpté. `jeu/objets/objets_visibles.gd` fait basculer un objet de donnée à
  nœud près de l'observateur : 10 000 objets semés, 5 nœuds vivants au plus.
  Chaque carte porte son emprise, ce qui ferme le premier point EN COURS

- 2026-08-19 — DISTANCE DE RENDU PAR ESPÈCE : `espece.gd` porte un
  `distance_rendu` que `couvert.gd` pose en `visibility_range_end` sur CHAQUE
  `GeometryInstance3D` du corps instancié — un `.glb` est un `Node3D` qui en
  porte plusieurs plus bas, la poser sur la racine ne toucherait rien. Godot
  cesse de dessiner au-delà et c'est la CAMÉRA qui décide : aucun objet n'a
  besoin de savoir où est le joueur, donc rien à remplir sur mille types. À zéro
  rien n'est écrit — une scène existante ne change pas de comportement. Elle ne
  libère AUCUN nœud et ne touche PAS le tronc solide : un arbre qu'on ne voit
  plus barre toujours le passage. Mesuré à côté : un nœud de plante pèse 5,1 Ko
  (herbe) à 9,6 Ko (arbre), et `visibility_range_end` n'en rend pas un octet
- 2026-08-19 — GRANDE CARTE 300×300 : `jeu/terrain/grande_carte.tscn`, scène
  NEUVE — 630 000 cellules d'emprise plus 26 488 de ceinture, 10,7 Mo. Aucun
  outil neuf : `DEMI_COTE` passe de 64 à 150 et les quatre outils s'ouvrent d'un
  coup, c'est la seule déclaration de l'emprise. Deux outils gagnent
  `vers=<chemin>` — sans lui, une carte d'une autre taille ne s'obtient qu'en
  écrasant celle qui existe. Et `generer_carte.gd` RELIT la bibliothèque quand
  elle est sur le disque au lieu de la réécrire : il ne sait construire qu'un
  item, `bloc.tres` en porte trois, et les écraser aurait vidé toute cellule
  portant `limite` ou `rampe` — dans `carte.tscn` comme ailleurs. Mesuré :
  chargement 710 ms, instanciation 508 ms, relevé 217 ms pour 91 204 colonnes,
  contre 306/105/40 et 16 900 sur l'ancienne. Coût de démarrage, jamais de tick
- 2026-08-19 — VISÉE À LA SOURIS : le déplacement horizontal fait pivoter le
  CORPS, le vertical incline les YEUX SEULS — une capsule inclinée glisse sur le
  terrain au lieu de s'y tenir. L'inclinaison s'accumule (une souris rend un
  déplacement, jamais une position) et reste sous le quart de tour, sinon
  l'image se retourne et le lacet part à l'envers de ce que le joueur voit. Le
  curseur se prend au PREMIER CLIC et jamais au démarrage — capturé d'office il
  est pris avant qu'on ait rien demandé, à chaque lancement ; Échap le rend.
  Quatre fonctions statiques de plus, verrouillées sans souris ni moteur physique
- 1a764b4 — PERSONNAGE ARPENTEUR : `jeu/unites/personnage.tscn`, une capsule d'un
  mètre — une demi-cellule, et c'est ce rapport qui donne l'échelle à tout le
  reste. Haut/bas avancent, gauche/droite TOURNENT ; sans souris, tourner est le
  seul moyen de regarder ailleurs. Le calcul sort de `_physics_process` en
  fonctions statiques, verrouillées sans clavier ni moteur physique
- 45b3a4a — TRONC SOLIDE : une espèce qui déclare un `rayon_collision` reçoit un
  cylindre de ce rayon, haut comme la stature de son stade, échangé avec le
  modèle. C'est le fût, pas l'arbre — on circule sous la canopée. À rayon nul
  aucun corps n'est posé, et c'est le défaut : l'herbe reste traversable sans
  qu'une ligne ne la nomme
- 8818c26 — DISPERSION ET DENSITÉ PAR ESPÈCE : où tombent les rejets et combien
  de voisines une plante supporte étaient communs, si bien qu'un tapis d'herbe
  serré aurait fait des arbres serrés. Le rayon de comptage reste commun — il
  choisit le rayon de rafraîchissement de tout le couvert
- 7b715cb — REQUETE SPATIALE corrigee dans `scripts/monde.gd` : les choses sont
  rangees par case, a plusieurs resolutions nees a la demande, et une requete ne
  lit plus que les cases que son rayon touche. Mesure : 512 076 tests de distance
  par tick a 1 000 plantes, tombes a ~7 100. Le contrat change — une chose qui
  bouge le declare par `deplacer()`. Mesures completes :
  `jeu/plantes/MESURES_COUVERT.md`
- 7b715cb — Premier commit du contenu, et depot distant du jeu :
  `https://github.com/Yenb/jeu-premier-rts`

- 2026-08-18 — Couvert MULTI-ESPÈCES et pression écologique : une RESSOURCE par
  espèce (`jeu/plantes/arbre.tres`, `herbe.tres`, décrites par `espece.gd` et
  `stade.gd`), déclarées sur le nœud Couvert et réglées à l'inspecteur —
  sélecteur de fichier compris pour les modèles ; un semis les réclame par son
  champ `Type`. Trois contraintes distinctes — la densité borne le
  nombre, l'OMBRE PAR STATURE tranche qui l'emporte, la TROUÉE décide qui peut
  s'installer où. Une herbe basse n'ombrage jamais un arbre et se fige sous
  n'importe lequel ; un rejet d'arbre renonce dans un tapis d'herbe dense, un
  rejet d'herbe non. Plafond de couche par espèce, et touffe fabriquée à
  l'exécution pour une espèce sans modèle
- 2026-08-18 — SUCCESSION : la mort ne regarde plus la lumière. Deux horloges —
  l'âge de CROISSANCE se fige à l'ombre et commande le stade, l'âge RÉEL avance
  toujours et commande la fin. Les confondre rendait les dominées IMMORTELLES
  (mesuré : une herbe sous un arbre encore vivante dix vies plus tard, âge zéro) :
  elles occupaient leur colonne et le plafond de densité pour toujours, et la
  forêt ne pouvait plus reprendre un sous-bois qui ne se vidait jamais. Mesuré
  après : à réglages identiques, les arbres passent de 0 à 4, et de 0 à 30 en
  desserrant leur trouée — la prairie s'installe vite, la forêt la remplace
  lentement, et une trouée d'arbre mort se recouvre d'herbe avant de redevenir
  forêt
- 2026-08-18 — UN SEMIS PAR ESPÈCE : `jeu/plantes/arbre.tscn` et `herbe.tscn`,
  scènes dérivées de `plante.tscn`, chacune avec son espèce déjà renseignée et son
  propre carré coloré. Le game designer choisit ce qu'il plante en choisissant la
  scène, plus en tapant un nom — une faute de frappe retombait en silence sur
  l'espèce par défaut. Les RÉGLAGES restent sur le nœud d'espèce sous le Couvert,
  un seul endroit par espèce : poser cent semis ne fait pas cent copies d'une
  durée à tenir d'accord
- 2026-08-18 — Couvert OPTIMISÉ d'un facteur ~4000 : le tick passe de la
  fréquence d'images à un pas FIXE de 2 s (mesuré : les éclaircies durent 9 s en
  médiane, un pas plus grand les raterait et ferait sous-pousser les plantes en
  concurrence), et l'ombre devient une valeur PORTÉE par chaque plante, relue
  seulement autour des trois événements qui peuvent la changer — mort, changement
  de stade, naissance. Mesuré : 1,1 ombre relue par tick au lieu de toutes.
  `vegetation.gd:retirer` est le geste qui ôte une plante du dehors en prévenant
  son voisinage ; `test_plante.gd` compare à chaque tick l'ombre portée à l'ombre
  recalculée entièrement. Le compte de voisins est porté de la même façon et
  vérifié par le même test — `peut_pousser` ne fait plus aucune requête
- 2026-08-18 — PRÉCHAUFFAGE du couvert : `couvert.gd` exporte un nombre de ticks
  joués avant la première image, rendu compris ; le joueur arrive sur une
  végétation déjà vécue au lieu d'un terrain nu. Aucune mécanique neuve — c'est la
  fonction de tick existante appelée en boucle. `test_plante.gd` verrouille que
  N ticks d'avance plus M normaux rendent le même état que N+M normaux, ce qui
  prouve surtout que `_process` ne simule rien de son côté. Le pas de temps est
  DÉCOUPÉ en tranches déduites des durées les plus courtes (`pas_maximal`) :
  sans ça un pas de 30 s rendait trois fois plus d'herbe qu'un pas de 1 s et
  sautait des stades — accélérer changeait le monde. Le premier test comparait
  N + M ticks à N+M ticks au MÊME pas : il prouvait une tautologie
- 2026-08-18 — Couvert VÉGÉTAL, premier gameplay du jeu : `jeu/plantes/` — des
  graines posées à la main qui traversent trois stades, changent de modèle 3D à
  chacun, déposent une graine ramassable au sol, se reproduisent au seul stade
  mature et meurent de vieillesse ; une plante dominée par une voisine plus
  avancée se fige et repart quand celle-ci meurt ; aucune mécanique écrite,
  `vegetation.gd`
  compose `senescence.gd` (dont le facteur nul fige un dominé), `stade.gd`,
  `seuil_etat.gd`, `gestation.gd` et
  `monde.gd` sur des catalogues LOCAUX au format des partagés, et
  `surface_terrain.gd` traduit le GridMap en cases une seule fois. Tous les
  réglages sont exportés par `couvert.gd` et surchargent `vegetation.json` sans
  que la simulation connaisse le nœud ; verrouillé par
  `jeu/plantes/test_plante.gd`, qui refuse aussi que l'inspecteur et le fichier
  divergent
- 2026-08-17 — Bloc RAMPE : `jeu/terrain/bloc.tres` porte un troisième item, un
  prisme triangulaire de 2 m plus sombre que le bloc plein, avec une
  ConvexPolygonShape3D qui épouse ses six sommets ; verrouillé par
  `jeu/terrain/test_rampe.gd`, qui mesure la pente au rayon sur une rampe posée
  et exige que le demi-tour du GridMap l'inverse
- 2026-08-17 — Limite de carte INVISIBLE : `jeu/terrain/generer_murs.gd`, script
  headless relançable qui pose 22 couches de murs sur les quatre bords, une
  cellule au-delà de l'emprise sculptable, avec l'item qui collisionne sans se
  rendre ; il relit la scène pour prouver que la sculpture est intacte
- 2026-08-17 — Terrain solide : `jeu/terrain/bloc.tres` porte deux items, le
  bloc visible et la limite sans maillage, tous deux avec une BoxShape3D de 2 m
  que le GridMap applique à toutes ses cellules d'un coup ; verrouillé par
  `jeu/terrain/test_collision_terrain.gd`, un témoin qui tombe et un témoin
  lancé vers le bord
- 2026-08-17 — Philosophie de facturation, personnalisation des unités, races
  comme pente et asymétrie des cartes documentées : `jeu/GAME_DESIGN.md`
- 2026-08-17 — Mécaniques du terrain destructible documentées (conservation
  des masses, cohésion par matériau, fissure puis effondrement, étayage) :
  `jeu/GAME_DESIGN.md` § Terrain destructible
- 2026-08-17 — Remplissage des contours : `jeu/terrain/outil_remplissage.gd`,
  script `@tool` qui lit une couche et comble les zones qu'elle enferme ;
  gestes partagés des outils dans `jeu/terrain/terrain_commun.gd`
- 2026-08-17 — Sculpture en masse : `jeu/terrain/outil_sculpture.gd`, script
  `@tool` qui remplit ou creuse une boîte de cellules depuis l'inspecteur
- 2026-08-16 — Terrain sculptable : GridMap de cubes de 2 m centré sur
  l'origine, bloc plein de 128×128×7 couches, `jeu/terrain/` — scène
  principale du projet
- 2026-08-16 — Création de `jeu/GAME_DESIGN.md`, principes fondateurs posés
- 2026-08-16 — Création du `CLAUDE.md` et du `SUIVI.md` du jeu
- 2026-08-16 — Architecture LLM décidée : micro-modèle stratégie + micro-modèle
  discussion (plus tard) + agents mémoire côté code
- 2026-08-16 — Modèle choisi : Llama 3.2 3B via Ollama
- 2026-08-16 — Connexion LLM prouvée (`banc_llm_connexion.gd`, réponse reçue)

## EN COURS

- Push du tampon MultiMesh via `RenderingServer.multimesh_set_buffer(mm.get_rid(), buffer)` au lieu de `mm.buffer = buffer` (setter de noeud). Aux 5 sites : `creer_pool`/`spawn`/`retirer`/`pousser_buffer` cote peuplement, et `_physics_process` cote banc. Comportement identique, gain marginal (evite l'intermediaire du setter).
- `pas_simple_lot` aligne sur `physique_et_buffer` : `floori()` + precalcul `inv_cote = 1.0/cote` et `trois_sur_cote = 3.0/cote`, aux 3 lectures de sol. Perf-neutre (hors chemin chaud, mais evite la divergence avec la copie du banc).
- HOT PATH ROUND 11 (revise) : physique + buffer fusionnes en UNE passe via `Banc.physique_et_buffer(cols, buffer, count, gravite, delta, carte)`, l'intention (errance) reste sa propre passe (deviendra couche IA hors tick dans le vrai jeu). Optim errance : `desiree[i]` reecrit UNIQUEMENT a l'expiration de l'horloge -- `_remplir_colonnes_depuis_individus` initialise `desiree = direction * vitesse` au spawn pour preserver l'invariant frame 1. Reglages : `floori()` au lieu de `int(floor())`, precalcul `inv_cote = 1.0/cote` et `trois_sur_cote = 3.0/cote`. `pas_simple_lot` inchange (commentaire MIROIR aux deux endroits). Verrouille par `scripts/test_tick_fusionne.gd` : parite bit-a-bit 3-passes vs 2-passes, 200 agents x 30 frames.
- HOT PATH ROUND 9 : `_cache_sommet` (Dict) converti en `_table_sommet` (`PackedFloat32Array` de taille `(2·demi_cote)² · 9`, NAN = case non calculee). Index arithmetique `lin*9 + ix + iz*3`, `lin = (cx+demi_cote) + (cz+demi_cote)*(2*demi_cote)`. `marquer_sale` fait `fill(NAN)`, setter sur `demi_cote` reallouee. Accesseur `table_sommet()` expose la table en lecture. `pas_simple_lot` lit la table en INLINE pour ses 3 requetes de sol (`sommet_sous` seulement en repli sur miss/plafonne). Couplage index carte<->mouvement commente aux deux sites. Test `_juger_demi_cote_realloue` ajoute.
- HOT PATH ROUND 8 (jeu) : les colonnes paralleles vivent dans `peuplement.pool.colonnes` (nom -> PackedArray typé), declarees par le consommateur, alignees au spawn (append defaut) et au retrait (swap-remove sur chaque colonne). `Mouvement.pas_simple_lot(cols, count, gravite, delta, carte)` fait une passe unique sur ces colonnes. Peuplement reste agnostique (aucun nom, aucune lecture). Piege CoW tenu partout. Verrouille par `scripts/test_peuplement_colonnes.gd` (alignement apres retrait du milieu) et `scripts/test_pas_simple_lot.gd` (parite bit-a-bit vs `pas_simple` sur 10 frames, 3 agents).
- HOT PATH ROUND 8 : tick en passe unique `Mouvement.pas_simple_lot(state Dict, gravite, delta, carte)` sur PackedArrays paralleles (`positions`/`velocites`/`desirees`/`au_sols`) ; le banc construit `_etat_mouvement` au `_fabriquer_lot` et n'accede plus a `proprietes` ni n'appelle `pas_simple` par agent dans le hot loop. Piege CoW tenu par depack en tete / repack en queue (Packed*Array passe par valeur). Ordre fixe -- ce banc ne spawn/retire pas pendant le run.
- HOT PATH ROUND 7 : transforms MultiMesh ecrites dans un `PackedFloat32Array` partage cote `Peuplement.creer_pool` (`pool.buffer`, layout `TRANSFORM_3D` : 12 floats/slot, `[xx,xy,xz,ox,yx,yy,yz,oy,zx,zy,zz,oz]`). Le banc ecrit seulement les 3 floats d'origine par agent dans la boucle puis pousse le tampon UNE FOIS au `mm.buffer` en fin de `_physics_process`. `spawn`/`retirer` posent la base identite / vident le slot dans le meme tampon. `pousser_buffer(pool)` disponible pour d'autres consommateurs. Piege CoW tenu : `pool.buffer` reassigne apres chaque mutation locale.
- HOT PATH ROUND 6 : `_cache_sommet` dans `carte_terrain.gd` -- cle `Vector3i(cx, cz, ix+iz*3)`, valeur = hauteur reelle du sommet de la sous-cellule. Lu par `sommet` et `sommet_sous` en tete. Vide par `marquer_sale()` (surcharge, `super.marquer_sale()` + `clear`). Exclusions : profil `_hauteur_profil` (surface continue), sommet_sous plafonne (y_max coupe le vrai sommet). Verrouille par `jeu/terrain/test_cache_sommet.gd` : plat, sculpte (invalidation), rampe (continuite), surplomb (garde y_max).
- HOT PATH ROUND 5b : errance rangee dans `individu.proprietes` (`errance_direction`, `errance_cap_horloge`) au lieu d'un dict membre `_errance` indexe par id String -- fin du hash de id par frame et de la fuite latente au retrait. Determinisme RNG preserve (meme ordre de tirage : direction puis horloge, au spawn).
- HOT PATH ROUND 5 : `monde.deplacer` coupe cote banc (index spatial non lu par ce banc -- `pas_simple` recoit `null`). Boucle du banc en `for individu in individus`, plus de `range()`. Dans `sommet` / `sommet_sous` : boucles `for iy in range(2,-1,-1)` remplacees par `while` decroissant, et fast-path `_masques_sous_cube.is_empty()` qui court-circuite le `dict.get` par couche.
- HOT PATH ROUND 4 : `_hauteur_profil` n'est plus appele sur cellule sans particularite -- garde inline `if masque_sc == PLEIN and particularites.has(cellule)` dans `sommet` et `sommet_sous`. Terrain plat = 0 appel.
- HOT PATH ROUND 3 : `masque`, `rang_le_plus_haut`, `item_de` inlinés dans `sommet` / `sommet_sous` / `_hauteur_profil` ; `ecrire_transform_index` inliné dans la boucle du banc (`mm` caché hors boucle, `set_instance_transform` en direct). Ces quatre noms doivent disparaître du top profiler pour la boucle physique.
- OMBRES : À OPTIMISER, JAMAIS À COUPER (décision Yael). Le `DirectionalLight3D`
  en PSSM 4 splits fait rendre chaque objet jusqu'à 5× (compté par « primitives
  dessinées » du moniteur). Optimiser = alléger la géométrie qui projette +
  régler cascades/distance/résolution ; SUPPRIMER l'ombre ou baisser les splits
  en dur est ÉCARTÉ. Lié au chantier rendu terrain/arbres (cubes pleins GridMap
  du sol + `couvert.gd:maillage_arbre` à 64 segments par défaut).
- CANEVAS DE BASE APPLIQUÉ à l'herbe, au lichen et au préchauffeur.
  `manager_herbe.gd` + `manager_lichen.gd` remplacent `cube_herbe.gd` +
  `cube_lichen.gd` en scène active (fichiers cube gardés sur disque pour
  patron cube violet). Le préchauffeur pousse ses brins survivants dans
  le manager via `ajouter_avec_age(pos, age)` au lieu d'instancier des
  Node. Mesuré : 26 nœuds au total dans la scène (au lieu de ~90 000
  avec l'ancien pattern à 22 000+ individus), soit 1 560 dispatches/s
  d'engine au lieu de 5.4 M. Règle non négociable dans CLAUDE.md
  § « Canevas de base des populations massives ». Toute future
  dérogation exige un tableau justificatif chiffré et deux sources
  internet.
- CHANTIER LOCALITÉ SPATIALE FERMÉ pour l'herbe et le lichen. Le
  compteur de voisins passe par un CHAMP SCALAIRE
  (`jeu/Outil de jeu/champ_spatial.gd` + wrappers `champ_herbe.gd`,
  `champ_lichen.gd`) : chaque cube inscrit +1 à sa naissance, retire -1
  à sa mort, et LIT sa case et les 8 adjacentes pour tester la
  saturation. Pattern d'automate cellulaire, éprouvé depuis 40 ans en
  écologie computationnelle. Aucun `get_nodes_in_group`, aucune Area3D,
  aucun scan par tick. Coût par événement : O(1). Coût par lecture :
  O(1) constant (9 cases). Le fichier `prechauffeur_herbe.gd` garde
  son événementiel interne (compteur `voisins` maintenu aux naissances
  et morts dans sa liste de brins), à migrer vers `champ_spatial` le
  jour où il devient un vrai goulot.
- `jeu/plantes/vegetation.gd` EST MAL DIMENSIONNÉ POUR SON BESOIN — indexation
  par colonne GridMap, reconstruction complète du Monde à chaque événement, et
  requêtes spatiales imbriquées dans `rafraichir_autour` (`choses_dans_rayon`
  appelé, puis `ombragee` refait `choses_dans_rayon` pour chaque plante
  trouvée). Le système « marche » parce que le CPU encaisse, pas parce qu'il
  est bien pensé — à N=50 000 individus ou sur mobile, le mur arrive d'un
  coup. Chiffres à N=500 herbes, ~30 événements par tick :

  | Ce que le code FAIT | Coût |
  | --- | --- |
  | Reconstruction Monde (`monde_des_vivantes`) à chaque tick avec ≥1 mort/naissance | ~500 insertions |
  | `colonnes_prises` — balayage complet à chaque tick | ~500 |
  | Production : boucle sur TOUTES les plantes, chacune cherche colonne libre | ~500 × k_anneau |
  | Gestation posée : teste stade+voisins sur TOUTES | ~500 |
  | Gestation avancée : tick sur TOUTES | ~500 |
  | Rejets : pour chaque naissance, `trouee_suffisante` fait `choses_dans_rayon` | ~30 × k |
  | `rafraichir_autour` : 30 foyers × ~15 plantes touchées, ET pour CHACUNE des 15, `ombragee` refait un `choses_dans_rayon` — **requête imbriquée** | **~6 750** |
  | **Total code actuel** | **~10 000 op/tick** |

  | Ce qu'une herbe voudrait FAIRE | Coût |
  | --- | --- |
  | Chaque herbe qui tente de se reproduire (~50/500 par tick) fait UNE requête locale sur ses voisines dans son rayon (k≈30) | ~50 × 30 |
  | **Total logique locale** | **~1 500 op/tick** |

  Facteur ≈ 7× à N=500. Se dégrade avec N (reconstruction linéaire, requêtes
  imbriquées aussi) — à N=2 000 le rapport devient ~20×. La différence
  conceptuelle : le code actuel MAINTIENT un état spatial global pour
  répondre vite à toute question, alors que les herbes ne posent chacune
  qu'UNE question, locale, quand elles la posent. Sur des choses qui bougent
  peu et pondent rarement, maintenir coûte plus cher que recalculer à la
  demande. À restructurer avant tout chantier qui remet l'herbe en jeu

- LA CHAÎNE ÉDITEUR N'EST PARCOURUE PAR AUCUN TEST — voir PIÈGES, « ce qu'un
  banc headless ne voit pas ». Trois défauts s'y sont succédé sans qu'aucun ne
  rougisse
- DEUX FICHIERS DE `carte_prototype.tscn` NE VIENNENT PAS DE CETTE SESSION :
  `semer_objets.gd`, `carte_prototype.tres`. À rattacher à un chantier avant d'y
  écrire — un fichier, un écrivain. (`murs_limite.gd` rattaché puis retiré au
  refactor ceinture du 2026-08-25, voir FAIT.)
- `carte_100km2.tscn` embarque les cellules du GridMap dès qu'on enregistre la
  scène en cours de sculpture — 7,8 Mo par sauvegarde, pour un contenu que la
  carte reconstruit. « vider » avant Ctrl+S le ramène à 3 Ko ; rien ne le force
- LES TRACES DE DIAGNOSTIC sont actives : `journal` exporté sur les outils

- UNE EMPRISE, DEUX CARTES DE TAILLES DIFFÉRENTES — à trancher avant tout
  chantier de terrain. `DEMI_COTE` est un nombre unique et vaut désormais 150,
  alors que `carte.tscn` porte une ceinture à 64 : `test_collision_terrain.gd`
  passe de VERT à ROUGE parce qu'il cherche le mur de l'ancienne carte à la
  nouvelle emprise. Deux issues, aucune bricolable au passage — refaire
  `carte.tscn` à 300×300 et n'en garder qu'une, ou sortir l'emprise du
  générateur pour qu'elle se lise SUR LA SCÈNE, chaque carte portant la sienne
- `jeu/terrain/test_carte.gd` est ROUGE (12 échecs) : il verrouille le compte
  exact de cellules, l'absence de relief, l'emprise stricte et une bibliothèque
  à un seul item — que la carte actuelle dément point par point. À recentrer sur
  ce qui reste invariant : pas de grille, caméra,
  lumière, et chaque item de la bibliothèque porte sa forme de collision
- `jeu/terrain/generer_carte.gd` reconstruirait une bibliothèque à un seul item
  SANS collision : relancé avec « forcer », il rendrait le terrain traversable
  et effacerait la limite de carte, en silence

## DÉCISIONS

- STOCK DU GÉNITEUR : deux stocks nommés PRIVÉE (violet) et PUBLIC (bleu),
  jamais « perso » ni « accessible ». Le stock PRIVÉ finance les
  gestations qui restent proches du géniteur (stockeur, protogéniteur) ;
  le stock PUBLIC finance les générateurs et se puise par eux à distance.
- SEUIL = COÛT sur les gestations enfants du géniteur : le seuil de
  déclenchement d'une gestation est aussi son coût. 10 % pour un
  générateur (15 sur public), 20 % pour un stockeur (30 sur privé),
  80 % pour un protogéniteur (120 sur privé). Une gestation ne se
  déclenche qu'à saturation partielle du stock qui la finance.
- CADAVRE ET CARRÉ ROUGE SONT ÉPHÉMÈRES. Sinon la cannibalisation
  intra-espèce nourrit la colonie même quand le joueur en tue —
  transformant chaque kill du joueur en neutre pour la colonie. Le
  pourrissement (5 min cadavre / 10 min carré rouge) préserve la valeur
  tactique de l'attaque.
- LE STOCKEUR EST UN PRÉ-CÂBLAGE : sans consommateur final il empile
  100 nourritures qui ne servent à rien encore. Le CONSOMMATEUR PRÉVU
  est le GUERRIER (puise stockeur, va attaquer le joueur). Le stockeur
  est posé avant le guerrier pour que la chaîne de production existe
  déjà quand on cablera le guerrier.
- LE PROTOGÉNITEUR EST EN ATTENTE de la fermeture du cycle par le
  guerrier — sans consommateur final, empiler un producteur
  supplémentaire (auto-produit énergie, suit géniteur à 60 m) ne révèle
  rien. Les scripts sont écrits, le nœud n'est pas branché dans la scène.
- PHILOSOPHIE DE DEV : le projet appartient au genre SIMULATION
  ÉMERGENTE (Dwarf Fortress, RimWorld). Les systèmes se construisent
  AVANT que le gameplay se stabilise ; le gameplay ÉMERGE de leurs
  interactions. Pas de playtest à chaque itération unitaire, plutôt
  itération sur les systèmes puis observation périodique de ce qui sort.

- TOUT EST DONNÉE ; LE RENDU ET LA COLLISION SONT UNE COUCHE TEMPORAIRE. Vaut
  pour TOUT objet à venir, pas seulement le terrain. Loin du joueur, un objet
  n'a ni nœud ni corps physique : il est une position et des propriétés, et
  l'altitude du sol sous lui se LIT dans la carte — `carte_terrain.gd:sommet`,
  temps constant, mesuré à 0,66 µs, sans rien de chargé. Près du joueur il
  devient un nœud avec collision, posé à cette même altitude ; quand le joueur
  s'éloigne le nœud est libéré et la donnée reste. La simulation ne dépend
  d'aucune des deux couches et ne s'arrête jamais. `terrain_visible.gd`
  applique déjà exactement ça au terrain — traverser 10 km y laisse le compte
  de cellules identique au premier pas.
  CHARGER LA COLLISION PARTOUT EST ÉCARTÉ : c'est revenir aux 175 millions de
  cellules que toute cette architecture existe pour éviter.
  DEUX PIÈGES À TENIR LE JOUR OÙ UN OBJET BASCULE. La hauteur portée par la
  donnée et le sol rendu viennent de la MÊME source, sinon l'objet apparaît
  enfoncé ou en l'air. Et un objet qui devrait tomber pendant que personne ne
  le regarde n'a AUCUNE physique pour le faire : sa chute se calcule en
  données, ou elle n'a pas lieu — décision de game design, pas de technique.
  CE QUE ÇA COÛTE, ET POURQUOI C'EST TENABLE : le compte de nœuds vaut
  `π·r² × densité`, donc doubler le rayon coûte quatre fois et agrandir la carte
  coûte zéro. À la densité d'herbe mesurée (0,0185 plante/m²) : 23 plantes à
  20 m, 581 à 100 m. Deux millions d'agents portent le même nombre de nœuds que
  deux mille — le seul terme qui suive la population totale est la donnée.
  RIEN N'EST À DÉFAIRE POUR Y ARRIVER : `monde.gd` et `vegetation.gd` sont des
  Dictionary et des fonctions, sans Node ni `_process`. « Tout simulé, rien
  rendu » est l'état de DÉPART. Les couches à nœuds sont `couvert.gd`,
  `terrain_visible.gd` et `objets_visibles.gd` ; le nœud lit la donnée, la
  donnée n'a jamais connaissance du nœud.

- LE NŒUD COÛTE DE LA MÉMOIRE, ET C'EST TOUT. Mesuré sur les formes que
  `couvert.gd` fabrique : `Node3D` seul 1,4 Ko ; + `MeshInstance3D` (une herbe)
  5,1 Ko ; + `StaticBody3D` + `CollisionShape3D` (un arbre) 9,6 Ko. En temps,
  11,7 µs pour fabriquer un arbre — poser mille plantes coûte 12 ms, une fois.
  Donc le mur du rendu est la MÉMOIRE, jamais la fabrication
- `visibility_range_end` CACHE, IL NE LIBÈRE PAS. Propriété de tout
  `GeometryInstance3D`, la caméra décide seule, aucun objet n'a besoin de savoir
  qui est le joueur — c'est la réponse généraliste pour ne pas DESSINER. Mais le
  nœud reste, et ses 9,6 Ko avec lui. Tenir l'échelle demande de SUPPRIMER le
  nœud, ce qui exige de savoir où est l'observateur : par GROUPE
  (`get_first_node_in_group`), jamais un champ à remplir sur chaque type
- OPTIMISER EST UN CRITÈRE PERMANENT, pas un chantier de fin. Les cartes
  grandiront, et ce qui tient à cent objets casse à mille : toute mécanique
  écrite désormais s'évalue AUSSI sur son coût à grande échelle, et se mesure
  avant d'être déclarée tenable. Deux leçons déjà payées, à ne pas repayer — un
  tick calé sur la fréquence d'images faisait soixante évaluations par seconde
  pour des plantes qui changent toutes les vingt-cinq secondes ; et un état
  spatial redemandé à chaque tick découvrait 99,6 % du temps qu'il ne s'était
  rien passé. Les deux se voient en MESURANT, jamais en relisant le code

- Le terrain est un GridMap : un objet d'AFFICHAGE et d'édition, que la
  simulation ne lit pas. Le pont vers les cases-objets qu'exige `design.md` est
  écrit et tient dans un seul fichier — voir `jeu/plantes/surface_terrain.gd`,
  seul du jeu à connaître GridMap. Les cases n'entrent PAS dans le Monde — non
  plus parce que `monde.gd` balaierait tout, il range par case et son coût suit
  le rayon, mais parce qu'y verser le terrain mettrait une case-objet PAR
  COLONNE dans chaque case que le rayon touche : un comptage de plantes paierait
  le terrain de son rayon, où les colonnes dépassent les plantes de plusieurs
  ordres. Le relevé se lit par colonne, en temps constant, sans aucun rayon à
  parcourir. La collision du GridMap n'entame rien :
  elle n'est lue que par le moteur physique de Godot, jamais par un mécanisme
- Le résumé d'état est une photo complète à chaque appel, pas d'historique
  cumulé
- Le modèle de discussion est séparé du modèle de stratégie
- Plan de repli : si la machine du joueur ne tient pas, mêmes modèles sur
  serveur, même code d'appel

## PROCHAINES ÉTAPES

1. GÉNÉRALISER `prechauffeur_herbe.gd` — actuellement il ne préchauffe
   que ses propres graines (Marker3D enfants + N aléatoires autour de
   lui). Utile seul, sans intérêt à côté d'un `semeur.tscn` qui pose déjà
   500 graines réparties : le préchauffeur devrait pouvoir s'aligner sur
   les positions posées par le semeur et faire tourner leur simulation
   avant l'instanciation, plutôt qu'ajouter ses propres graines à côté.
   Fichier gardé sur disque, retiré de la scène en attendant. À faire
   quand on voudra un tapis avec une HISTOIRE (naissances, morts,
   trouées) plutôt qu'un semis uniforme
1. OBJETS EN DONNÉES, NŒUDS PRÈS DU JOUEUR : le mécanisme qui fait basculer un
   objet de donnée à nœud (rendu plus collision) quand le joueur approche, et
   l'inverse quand il s'éloigne. La règle est tranchée — voir DÉCISIONS, « tout
   est donnée » — et le terrain l'applique déjà ; rien ne le fait encore pour
   les objets. Patron à recopier : `couvert.gd`, seule couche à nœuds du jeu
1. Trancher le point ouvert de `jeu/GAME_DESIGN.md` § Terrain destructible :
   chercher si un mécanisme du cœur porte déjà la connexité de voisinage, sinon
   ouvrir le chantier framework
2. Câbler le ramassage des graines par les unités : `vegetation.gd:ramasser` et
   `couvert.gd:retirer_graine` sont posées et testées, personne ne les appelle —
   `consommer.gd` est le mécanisme qui attend, il transfère entre deux choses
3. Second banc LLM avec grammaire GBNF contrainte
4. Carré rouge (joueur) + carré violet (IA) + caméra scrollable
5. Premier Modelfile agent stratégie avec system prompt
6. Boucle complète : état → résumé → modèle → clé → action visible
7. GESTATION MOTHER CUBE MISE EN STOCK — `test_ennemi2 Mother box` pointe
   désormais vers `geniteur_v2.tscn` (copie sans nœud `GestationMother`).
   `geniteur.gd`, `geniteur.tscn` et `gestation_mother_cube.gd` restent
   intacts sur disque, réutilisables. Le gameplay mother cube est une
   piste à reprendre quand la lignée du géniteur v2 sera stabilisée

## PIÈGES DÉJÀ PAYÉS

- AUCUN ENNEMI NE SPAWN, LE CODE N'A RIEN BOUSILLÉ. Symptôme : le jeu tourne,
  aucun ennemi n'apparaît à la zone de spawn. Cause : case `Spawn Ennemis Actif`
  décochée dans l'inspecteur du nœud manager (celui qui porte `manager_proto.gd`),
  groupe **Ennemis**, juste au-dessus de `Intervalle Spawn Ennemi`. Décochée =
  `_tick_spawn_ennemis` early-return, aucun spawn. Règle : ouvrir l'inspecteur,
  recocher la case. Les ennemis déjà présents ne sont pas purgés par la case —
  décocher stoppe l'apparition, pas ce qui est là.
- LE GÉNÉRATEUR POUSSE LE GÉNITEUR AU CONTACT. Symptôme : les 4 générateurs
  s'enfoncent dans le collider du géniteur, le géniteur dérive lentement au
  lieu de rester posé. Cause : RigidBody masse 5 vs RigidBody masse 30
  non-freeze, `distance_contact_geniteur = 4 m` avec collider géniteur 3 m
  de demi-largeur — le check « arrivé » s'évalue par tick et la vélocité
  3 m/s avale les 0,5 m de marge avant que le stop ne s'applique. Règle :
  `add_collision_exception_with(self)` au `_ready()` du générateur — le
  patron existait déjà au `_mourir()` pour le cadavre, il fallait
  simplement l'appliquer aussi à la naissance. Généralisable : toute
  entité amie qui approche le géniteur ou une autre amie doit avoir
  l'exception croisée dès `_ready()`.
- GÉNÉRATEUR MANGE UN CADAVRE PLEIN PUIS RESTE BLOQUÉ EN BOUCLE
  SILENCIEUSE. Symptôme : après avoir vidé un cadavre (stock 10 =
  cout_prelevement 10), le générateur oscille au contact du géniteur sans
  pondre. Cause : `_matiere_cumulee = 10` et `_cout_paye_pour_ce_cycle =
  true` restent tels quels quand la source meurt en COLLE (jamais reset,
  intentionnel pour cumul multi-sources). Cycle suivant : le générateur
  arrive au géniteur avec `reste = cout_prelevement - matiere_cumulee =
  0`, `preleve(0) = 0`, `if pris <= 0: return ATTENTE`. Règle : dans
  `_faire_colle` branche source invalide, si `_cout_paye_pour_ce_cycle`
  est vrai, transition ETAT_POND (ponte anticipée qui reset dans
  `_faire_pond`) au lieu d'ETAT_ATTENTE. Préserve le cumul multi-sources
  quand cout_paye est faux.
- STOCKEUR RESTE PLANTÉ APRÈS AVOIR MANGÉ LOIN DU GÉNITEUR. Symptôme :
  stockeur mange un carré rouge éloigné, ne perçoit plus de nourriture,
  reste immobile indéfiniment ; pendant ce temps le géniteur s'est
  déplacé, les nouveaux carrés rouges sont hors perception. Cause :
  `_faire_cherche_nourriture` immobile si `vus.is_empty()`. Règle :
  `_appliquer_patrouille` qui fait revenir le stockeur vers le géniteur
  au-delà de `rayon_patrouille` (50 m). Le stockeur reste dans la zone
  de production, la perception 30 m suffit à repérer les carrés rouges
  quand il y retourne.
- GODOT FULLSCREEN BORDERLESS IGNORE `ShowWindow SW_MINIMIZE`. Symptôme :
  `IsIconic` retourne true mais la fenêtre reste peinte ; `CopyFromScreen`
  capture Godot quand même. Règle : ne pas dépendre du minimize pour
  isoler visuellement ; demander à l'utilisateur de mettre en arrière-plan
  volontairement si nécessaire.

- TROIS COPIES DE LA MÊME CHOSE, ET UNE SEULE QUE LE JEU LIT. Le GridMap de la
  scène, la carte, le fichier de scène : on sculpte dans le premier, le jeu ne
  lit que la deuxième, et Ctrl+S écrit la troisième. Symptôme : le travail
  disparaît au lancement, trois fois de suite, pour trois raisons différentes.
  Règle : ce qui n'est pas la source de vérité doit être REPRIS avant d'être
  effacé, jamais jeté — et le chemin de reprise ne doit dépendre d'aucune
  condition qui ne se signale pas quand elle manque.
- UN MÉCANISME QUI N'A DE SENS QU'À GRANDE ÉCHELLE NE DOIT PAS CONDITIONNER LE
  RESTE. L'enregistrement exigeait qu'une fenêtre soit chargée — un geste qui ne
  sert que sur une carte de 100 km². Sur 200 m, tout tient à l'écran, personne
  ne le fait, et le travail était perdu sans une erreur.
- ÉCRIRE UNE FENÊTRE ENTIÈRE CREUSE CE QU'ELLE NE PORTE PAS. Symptôme : le
  terrain devient un damier de zones vides, et tout ce qui est sculpté
  disparaît — mesuré à 259 530 colonnes creusées, zéro relief monté. Cause : une
  colonne absente du GridMap compte comme VIDE, et « absente » a deux causes
  qu'il faut distinguer — creusée par le sculpteur, ou jamais chargée. Règle :
  ce qu'un chargement n'a pas traité ne s'écrit pas ; l'ensemble des colonnes
  posées se retient, il ne se déduit pas de l'emprise demandée.
- UN CENTRE RELU APRÈS UNE ATTENTE N'EST PLUS CELUI QU'ON A POSÉ. Une pose
  étalée sur plusieurs frames rend la main entre deux tranches, et ce qui
  pilotait a pu changer pendant. Règle : figer au début ce sur quoi on travaille,
  et ne jamais le relire à la fin pour annoncer ce qu'on a fait. Aucun test ne
  tient ce point — lancé depuis un test, le chargement se termine avant qu'on
  ait pu bouger quoi que ce soit.
- UN TEST QUI VISE UN POINT FIXE FINIT PAR MESURER LE TRAVAIL DU SCULPTEUR. Un
  rayon tiré à l'origine, un plafond qui exige une carte vierge : les deux
  rougissent le jour où la carte est travaillée, sans qu'aucun code n'ait
  changé. Règle : mesurer un invariant du code — coût par colonne sculptée,
  sol sous le personnage — jamais un état du contenu.
- CE QU'UN BANC HEADLESS NE VOIT PAS. `Engine.is_editor_hint()` est faux hors
  éditeur : tout ce qui ne s'exécute que dans l'éditeur — construction d'une
  vue d'outil, script `@tool` sur une ressource — n'est parcouru par aucun test
  lancé en ligne de commande. Règle : le dire AVANT de montrer un test vert, et
  vérifier le mode du script (`Script.is_tool()`) au lieu d'attendre l'erreur
  « placeholder instance ».

Ce que le prochain chantier de couvert doit savoir avant d'écrire une ligne. Un
seul de ces pièges a coûté une demi-session ; tous ont été payés une fois.

- FAUX VERT — un test qui imprime `OK` alors qu'un objet ne s'est pas
  instancié. `Couvert.new()` rend `null` quand le script ne compile pas, et les
  jugements qui en dépendaient étaient SAUTÉS sans un mot. Règle : un objet qui
  ne s'instancie pas ROUGIT, il ne se saute jamais.
- SCRIPT QUI NE REND PAS LA MAIN — un `--headless --script` qui dépasse la
  minute n'est pas lent : il ne compile pas, le `SceneTree` ne quitte jamais.
  Ne pas attendre, lire l'erreur.
- TEST TAUTOLOGIQUE — le préchauffage comparé à lui-même AU MÊME PAS DE TEMPS
  prouvait seulement que l'addition est commutative. Mesuré ensuite : un pas de
  30 s donne TROIS FOIS plus d'herbe qu'un pas fin. Règle : un test
  d'équivalence qui ne fait pas varier ce qu'il prétend rendre indifférent ne
  teste rien. D'où `pas_maximal` et le découpage en tranches.
- MORT BRANCHÉE SUR LA MAUVAISE HORLOGE — voir la ligne FAIT du 2026-08-18.
  Symptôme trompeur : tous les tests verts, et zéro arbre à l'écran.
- CADAVRE QUI FAIT ENCORE DE L'OMBRE — `monde.gd` garde les disparues un tick.
  Toute lecture du monde double-filtre les mortes. Précédent framework :
  `banc_parasites_reproduction.gd`.
- FENÊTRE FERTILE PLUS COURTE QUE L'INTERVALLE — une espèce fertile à 25 s et
  morte à 100 s n'a que 75 s pour semer ; un intervalle de 80 la rend stérile,
  verte à tous les tests de cycle. Le test de reproduction exige une SECONDE
  plante, jamais un cycle qui se déroule bien.
- MODÈLE GLB NON CENTRÉ — les `.glb` sont sortis du modeleur avec l'origine à
  côté (jusqu'à 20 m), les plantes sautaient en grandissant. Invisible aux
  tests : `couvert.gd` n'en avait aucun. Recentrage à l'exécution, et une
  assertion de géométrie dans le test.
- UNE JUSTIFICATION RECOPIÉE PÉRIME EN SILENCE. « Les cases n'entrent pas dans
  le Monde parce que `monde.gd` balaie linéairement » vivait dans quatre
  fichiers ; l'index spatial a rendu la raison fausse partout d'un coup, et rien
  n'a rougi — aucun test ne lit un commentaire. Elle a servi une fois à conclure
  qu'une carte plus grande était hors de portée, sur des chiffres d'avant
  l'index. Règle : une décision cite le MÉCANISME qui la fonde à UN seul
  endroit ; les autres y renvoient. Et un relevé de mesures dit en tête sur quel
  moteur il a été pris — sans quoi il se relit comme un état courant.
- UN COÛT DOCUMENTÉ N'EST PAS UN COÛT CORRIGÉ. `monde.gd` portait en tête
  « COUT LINEAIRE par requete : signalé, jamais contourné en silence », et cette
  phrase est ce qui a laissé passer le défaut des mois : elle transforme un bug
  en état connu et assumé, et se lit comme une décision déjà prise. Règle : un
  coût se corrige, ou se plafonne par un test opposable — jamais par une note.
- AUCUN TEST NE MESURAIT UN COÛT. `test_volume_docs.gd` rougit quand un `.md`
  grossit de quelques milliers d'octets ; rien ne rougissait quand un tick passait
  de 1 ms à 500. Un test de correction ne peut pas voir ça — le résultat EST juste,
  il est seulement ruineux. D'où les compteurs exposés par `monde.gd` (requêtes,
  cases lues, candidats mesurés) : ils existent pour qu'un test puisse poser un
  plafond de coût.
- OPTIMISER SANS MESURER — l'hypothèse « les colonnes prises sont le goulot »
  était fausse : 163 ms → 155 ms. Mesurer d'abord, toujours.
- DEUX ÉCRIVAINS SUR `carte.tscn` — la sauvegarde de l'éditeur ouvert écrase le
  fichier écrit à côté. Quand Godot est ouvert, les valeurs se dictent, elles ne
  s'écrivent pas.
